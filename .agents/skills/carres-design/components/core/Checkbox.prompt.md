17px checkbox — flame fill for a real choice, blue fill for row multi-select.

```jsx
<Checkbox defaultChecked />
<Checkbox variant="select" defaultChecked />  {/* row selection */}
```

Blue means selection only; never use the blue variant for a normal on/off choice.
