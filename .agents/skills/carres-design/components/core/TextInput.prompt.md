Standard labelled text field — white, hairline border, flame focus ring.

```jsx
<TextInput label="Customer" placeholder="Search name…" />
<TextInput label="PO number" mono defaultValue="PO/2606-079" />
```

Set `mono` for code-like values (PO, phone, SKU, dimensions) so the slashed zero applies.
