import { chromium } from "/Users/chaichiewlim/Desktop/Carres-Portal-v2/node_modules/.pnpm/playwright@1.59.1/node_modules/playwright/index.mjs";
const b = await chromium.launch();
const U = "http://localhost:5299/sales-order-page.html";
let pass = 0, fail = 0;
const ok = (c, m) => { c ? pass++ : fail++; console.log(c ? "PASS" : "FAIL", m); };
const open = async (q = "", w = 1440) => { const p = await b.newPage({ viewport: { width: w, height: 900 } }); p.errs = []; p.on("pageerror", e => p.errs.push(e.message)); await p.goto(U + q); await p.waitForTimeout(3500); return p; };
const form = (p) => p.locator('[data-pane="form"]').innerText();
const doc = (p, rev) => p.evaluate((r) => window.__docText(r), rev);
const flat = (s) => s.replace(/\s+/g, " ");
const shot = async (p, n, f = 0) => { await p.locator('[data-pane="form"]').evaluate((el, f) => el.scrollTop = (el.scrollHeight - el.clientHeight) * f, f); await p.waitForTimeout(500); await p.screenshot({ path: `shots/${n}.png` }); };
const edit = (p) => p.getByRole("button", { name: "Edit" }).click();

console.log("SCOPE: standalone preview verification only — not the production system's revisions, approvals, payments or retained documents.");
console.log("── A · view SO-1363 (masked real case): page and document agree");
let p = await open();
let t = flat(await form(p));
ok(t.includes("Qty: Bedframe 2 · Pillow 2") && t.includes("Services: Delivery fee"), "page Qty by the Register's ladder + service named");
ok(t.includes("Customer (masked)") && (t.match(/\(masked\)/g) || []).length >= 12 && t.includes("Carres Maluri Cheras") && t.includes("BIG MATTRESS BED SDN. BHD."), "personal details masked; sales location and dealer (businesses) kept");
ok(t.includes("Disc (RM)"), "Items keeps the document's Disc column (owner ruling 2026-09-21)");
ok(t.includes("TRION-Q") && t.includes("Front Drawer (2 drawers)") && t.includes("CODY-SK") && t.includes("Fabric BF-10") && t.includes("Mattress gap: Confirm later"), "real lines and configuration");
for (const s of ["Goods amount RM 4,404.00", "Service amount RM 250.00", "Total payable RM 4,654.00", "Paid to date RM 4,654.00", "Balance due RM 0.00"]) ok(t.includes(s), `page ${s}`);
ok(t.includes("261026") && t.includes("RC-170926-9154") && t.includes("Credit card · BSN"), "real card payment, approval code, receipt");
ok(!t.includes("PO-") && !t.includes("already ordered from the supplier"), "no invented PO");
let d = flat(await doc(p, 1));
ok(d.replace(/ /g, "").includes("REVIEWSAMPLE·Rev1·nottheofficialSalesOrderPDF"), "document stamped REVIEW SAMPLE · Rev 1");
ok(!d.includes("TOTAL RECEIVED") && !d.includes("GOODS TOTAL"), "document has no TOTAL RECEIVED / GOODS TOTAL");
ok(/TOTAL PAYABLE 4 /.test(d) && d.includes("Qty: Bedframe 2 · Pillow 2") && d.includes("Services: Delivery fee"), "document: 4 pieces, same Qty and services as the page");
ok(d.includes("Goods amount RM 4,404.00") && d.includes("Service amount RM 250.00") && d.includes("Total payable RM 4,654.00"), "document goods / service / total");
ok(d.includes("Paid to date RM 4,654.00") && /BALANCE DUE RM 0.00/.test(d), "document paid and balance");
ok(d.includes("Rev 1 · Thu, 17 Sep 2026 16:31") && d.includes("Customer (masked)"), "Rev 1 document: signature caption with the masked name, no image");
ok((d.match(/\(masked\)/g) || []).length >= 3, "document prints masked contact details");
ok(!t.includes("Customer signature") && !t.includes("Signed by"), "no signature card on the page");
const revs = async (q) => { await q.getByRole("tab", { name: "Revisions" }).click(); await q.waitForTimeout(300); const x = flat(await q.locator("body").innerText()); await q.getByRole("tab", { name: "Order", exact: true }).click(); await q.waitForTimeout(800); return x; };
await shot(p, "v15-view", 0); await shot(p, "v15-view-bottom", 1);
await p.close();

console.log("── B · safe correction → Save");
p = await open(); await edit(p); await p.locator("#phone").fill("(masked, changed)");
ok(await p.getByRole("button", { name: /^Save/ }).count() === 1, "phone only → Save");
await p.locator("#why").fill("Customer gave a new number.");
await p.getByRole("button", { name: "Save" }).click(); await p.waitForTimeout(1200);
let r = await revs(p);
ok(/Rev 2 · Current .*? Not signed/.test(r) && /Rev 1 .*? Signed Thu, 17 Sep 2026 16:31/.test(r), "Revisions: Rev 2 correction not signed, Rev 1 keeps its signature");
await p.close();

console.log("── C · Remove the pillows, then Restore");
p = await open(); await edit(p);
await p.getByRole("button", { name: "Remove" }).nth(2).click(); await p.waitForTimeout(500);
t = flat(await form(p));
ok(t.includes("Memory Foam Pillow (MEMORY-FOAM-PILLOW-asd) 2 × 220.00 Cancelled"), "pillows shown as Cancelled in Before/After");
ok(t.includes("Qty Bedframe 2 · Pillow 2 Bedframe 2 "), "Qty: Bedframe 2 · Pillow 2 → Bedframe 2");
ok(t.includes("Memory Foam Pillow: No PO yet → Purchasing re-counts what to buy"), "real consequence: no PO, demand re-count");
ok(t.includes("Total payable RM 4,654.00 RM 4,214.00") && t.includes("RM 440.00 paid more than the new total — Payments reviews a refund"), "money: 4,654 → 4,214, refund review 440");
ok(await p.getByRole("button", { name: /Submit amendment request/ }).count() === 1, "cancelling goods needs approval");
await shot(p, "v15-remove", 0);
await p.getByRole("button", { name: "Restore" }).click(); await p.waitForTimeout(400);
t = flat(await form(p));
ok(!t.includes("Cancelled") && (await p.getByRole("button", { name: /Submit amendment request|^Save/ }).count()) === 0, "Restore: no change left, no submit button");
await p.close();

console.log("── E · Rev 2 signed, then Rev 3; every version prints its own document");
p = await open("?as=management");
await edit(p); await p.locator("#qty-l3").fill("1");
await p.locator("#why").fill("Customer keeps one pillow.");
await p.locator("#evidence").fill("WhatsApp from customer, Tue 22 Sep 09:40");
await p.getByRole("button", { name: "Submit amendment request" }).click(); await p.waitForTimeout(600);
t = flat(await form(p));
ok(t.includes("Qty: Bedframe 2 · Pillow 2"), "pending: order unchanged");
await p.getByRole("button", { name: "Approve and apply" }).click(); await p.waitForTimeout(1200);
t = flat(await form(p));
ok(t.includes("Qty: Bedframe 2 · Pillow 1"), "Rev 2 applied");
r = await revs(p); ok(/Rev 2 · Current .*? Not signed/.test(r), "Revisions: Rev 2 not signed yet");
await p.evaluate(() => window.__signCurrent()); await p.waitForTimeout(800);
r = await revs(p); ok(/Rev 2 · Current .*? Signed Tue, 22 Sep 2026/.test(r), "Rev 2 signed (e-sign stand-in)");
await edit(p); await p.getByRole("button", { name: "Configure Trion" }).click();
await p.locator("#l1-size").click(); await p.getByRole("option", { name: "King", exact: true }).click();
await p.locator("#why").fill("Customer changes Trion to King.");
await p.locator("#evidence").fill("Signed change form, Tue 22 Sep 15:10");
await p.getByRole("button", { name: "Submit amendment request" }).click(); await p.waitForTimeout(600);
await p.getByRole("button", { name: "Approve and apply" }).click(); await p.waitForTimeout(1200);
r = await revs(p); ok(/Rev 3 · Current .*? Not signed/.test(r) && /Rev 2 .*? Signed Tue, 22 Sep 2026/.test(r), "Revisions: Rev 3 not signed; Rev 2 keeps its own signature");
const d1 = flat(await doc(p, 1)), d2 = flat(await doc(p, 2)), d3 = flat(await doc(p, 3));
ok(d1.includes("Rev 1 · Thu, 17 Sep 2026 16:31") && /TOTAL PAYABLE 4 /.test(d1) && d1.includes("TRION-Q"), "Rev 1 document: its signature, 4 pieces, Queen");
ok(d1.includes("Paid to date RM 4,654.00"), "Rev 1 retained document: the payment made by its issue date");
ok(d2.includes("Rev 2 · Tue, 22 Sep 2026") && /TOTAL PAYABLE 3 /.test(d2) && d2.includes("TRION-Q"), "Rev 2 document: its own signature, 3 pieces");
ok((d3.replace(/ /g, "").includes("Rev3·notsignedbythecustomer")) && d3.includes("TRION-K") && !d3.includes("Rev 3 · Tue"), "Rev 3 document: King, not signed, no borrowed signature");
await p.getByRole("tab", { name: "Revisions" }).click(); await p.waitForTimeout(300);
await p.getByRole("button", { name: "View version" }).nth(1).click(); await p.waitForTimeout(1500);
t = flat(await form(p));
ok(t.includes("Rev 2") && t.includes("Qty: Bedframe 2 · Pillow 1") && !t.includes("Signed by"), "View version Rev 2: complete order, no signature card");
ok(p.errs.length === 0, "no page errors " + JSON.stringify(p.errs));
await p.close();

console.log("── F · evidence gate");
p = await open("?as=management"); await edit(p); await p.locator("#qty-l3").fill("1"); await p.locator("#why").fill("x");
await p.getByRole("button", { name: "Submit amendment request" }).click(); await p.waitForTimeout(600);
ok(await p.getByRole("button", { name: /needs customer agreement evidence/ }).isDisabled(), "approve blocked without evidence");
await p.close();

console.log("── H · out-of-date request, and a correction that must NOT make it out of date");
p = await open("?as=management");
await edit(p); await p.locator("#qty-l3").fill("1"); await p.locator("#why").fill("Customer keeps one pillow."); await p.locator("#evidence").fill("WhatsApp 22 Sep");
await p.getByRole("button", { name: "Submit amendment request" }).click(); await p.waitForTimeout(600);
await edit(p); await p.locator("#phone").fill("(masked, changed)"); await p.locator("#why").fill("New number.");
ok(await p.getByRole("button", { name: /^Save$/ }).isEnabled(), "a contact correction can still be saved while a request waits");
await p.getByRole("button", { name: /^Save$/ }).click(); await p.waitForTimeout(800);
t = flat(await form(p));
ok(t.includes("Waiting for management") && !t.includes("Out of date"), "the correction does not make the request out of date");
await p.evaluate(() => window.__landOtherChange()); await p.waitForTimeout(800);
t = flat(await form(p));
ok(t.includes("Out of date — propose again") && t.includes("Nothing was applied"), "another approved change makes the request out of date");
ok((await p.getByRole("button", { name: /Approve and apply/ }).count()) === 0, "out-of-date request cannot be approved");
ok(/Rev 1 \(sent on\)/.test(t) && /Rev 3 \(now\)/.test(t) && t.includes("Thu, 1 Oct"), "shows sent-on, now and proposed values");
await shot(p, "v15-stale", 0);
await p.getByRole("button", { name: "Propose this version again" }).click(); await p.waitForTimeout(600);
ok(await p.locator("#qty-l3").inputValue() === "1" && await p.getByRole("button", { name: /Submit amendment request/ }).count() === 1, "propose again reopens edit with the proposal");
await p.close();

console.log("── J · SO-1206 (real): unclassified SKU and per-unit service ×2");
p = await open("?so=1206");
t = flat(await form(p));
ok(t.includes("Qty: Mattress 1 · Bedframe 2 · Pillow 4 · Mattress protector 2 · Items to check 1"), "unclassified M1201F-K keeps its qty under Items to check, not folded into Mattress");
ok(!t.includes("Other goods"), "no Other goods on the page");
ok(t.includes("Services: Dispose old mattress ×2 · Dispose old bed frame · Delivery fee"), "per-unit service shows its quantity");
ok(t.includes("Dispose old mattress Queen ×2") && t.includes("Dispose old bed frame King"), "service rows name the service, detail beneath");
const svc = await p.evaluate(() => { const t = document.querySelector('[data-pane="form"] table[aria-label="Items"]'); const heads = [...t.querySelectorAll("thead th")].map((h) => h.textContent.trim()); const qi = heads.indexOf("Qty"), ci = heads.indexOf("Item Code");
  const rows = [...t.querySelectorAll("tbody tr")].filter((r) => r.children.length === heads.length).map((r) => [r.children[ci].textContent.trim(), Number(r.children[qi].textContent.trim())]);
  const s = rows.filter(([c]) => ["dispose-mattress", "dispose-bedframe", "DELIVERY"].includes(c)); return { lines: s.length, qty: s.reduce((n, [, q]) => n + q, 0) }; });
ok(svc.lines === 3 && svc.qty === 4, `services measured from the Items table: ${svc.lines} lines, qty ${svc.qty} — the Register footer counts qty (4), so lines ≠ qty here`);
for (const s of ["Goods amount RM 6,778.00", "Service amount RM 510.00", "Total payable RM 7,288.00", "Paid to date To check", "Balance due To check"]) ok(t.includes(s), `SO-1206 ${s}`);
ok(t.includes("Payment data to check.") && t.includes("RM 3,644.00 paid, but no payment record exists") && !t.includes("Balance due RM"), "summary/record mismatch: shown as to check, no trusted balance");
d = flat(await doc(p, 6));
ok(d.includes("Items to check 1") && d.includes("Dispose old mattress ×2") && /TOTAL PAYABLE 10 /.test(d), "document: same Qty words, service ×2, 10 pieces");
ok(/Paid to date To check/.test(d) && /BALANCE DUE To check/.test(d), "document: paid and balance wait for Payments");
ok(!/ADD-ON Dispose old mattress ×2/.test(d), "document service row keeps the plain name; ×2 only in the Services summary");
r = await revs(p); ok(r.includes("Rev 6 · Current") && r.includes("Rev 1–5 were recorded on Mon, 10 Aug 2026"), "Revisions: Rev 6 current, earlier revisions named");
await shot(p, "v15-1206", 0.62);
await edit(p); await p.getByRole("button", { name: "Remove Memory Foam Pillow" }).click(); await p.waitForTimeout(400);
t = flat(await form(p));
ok(t.includes("payment data to check first — no refund or balance is worked out") && !t.includes("Payments reviews a refund"), "a money change on SO-1206 works out no refund or balance");
ok(p.errs.length === 0, "no page errors " + JSON.stringify(p.errs));
await p.close();

console.log("── K · SIMULATED edge case (labelled): discount, voided payment, part payment");
p = await open("?case=sim");
t = flat(await p.locator("body").innerText());
ok(t.includes("SIMULATED EDGE CASE — invented data, not a real order"), "simulated case is labelled on screen");
ok(t.includes("200.00") && t.includes("4,798.00"), "Disc 200.00 shown; amount 2 × 2,499 − 200 = 4,798.00");
for (const s of ["Goods amount RM 5,198.00", "Service amount RM 410.00", "Total payable RM 5,608.00", "Paid to date RM 2,000.00", "Balance due RM 3,608.00"]) ok(t.includes(s), `sim ${s}`);
d = flat(await doc(p, 1));
ok(d.includes("4,798.00") && d.includes("200.00") && d.includes("Paid to date RM 2,000.00") && /BALANCE DUE RM 3,608.00/.test(d) && !d.includes("1,200.00"), "document: discount, voided RM1,200 excluded, part payment balance");
await shot(p, "v15-sim", 0.6);
ok(p.errs.length === 0, "no page errors " + JSON.stringify(p.errs));
await p.close();

console.log("── I · kit measurements");
p = await open();
const m = await p.evaluate(() => {
  const h2 = document.querySelector('[data-kit="panel"] h2');
  const label = document.querySelector('[data-kit="panel"] label');
  return { panelTitle: h2 && getComputedStyle(h2).color, label: label && getComputedStyle(label).fontSize };
});
console.log("MEASURE panel title color", m.panelTitle, "· label size", m.label);
ok(m.label === "11px", "labels use kit FieldFrame 11px (locked)");
await edit(p); await p.waitForTimeout(400);
const rows = await p.evaluate(() => [...document.querySelectorAll('[data-pane="form"] table')].slice(0,1).map(t => [...t.querySelectorAll("tbody tr")].map(r => Math.round(r.getBoundingClientRect().height))));
const inp = await p.evaluate(() => { const e = document.querySelector("#qty-l3"); const r = e.getBoundingClientRect(); return { h: Math.round(r.height), w: Math.round(r.width) }; });
console.log("MEASURE items row heights in edit", JSON.stringify(rows), "· qty input", JSON.stringify(inp));
await shot(p, "v12-edit-items", 0.55);
await p.close();

console.log("── G · widths and states");
for (const w of [1440, 1180, 820, 390]) { const q = await open("", w); const ov = await q.evaluate(() => document.documentElement.scrollWidth - innerWidth); ok(ov === 0 && (await q.locator("canvas").count()) > 0, `${w}px: no page sideways scroll, document shown`); if (w === 390) await q.screenshot({ path: "shots/v11-390.png", fullPage: true }); await q.close(); }
for (const s of ["loading", "error"]) { const q = await open(`?state=${s}`); ok(s === "loading" ? (await q.locator('[data-testid="page-loading"] [data-testid="kit-loading-skeleton"]').count()) === 2 : (await q.getByRole("button", { name: "Try again" }).count()) === 1 && (await q.locator("body").innerText()).includes("This sales order could not be opened"), `state ${s} renders`); await q.screenshot({ path: `shots/v11-${s}.png` }); await q.close(); }
console.log(`TOTAL ${pass} passed · ${fail} failed`);
await b.close();
