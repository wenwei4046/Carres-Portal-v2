/**
 * Principal Approvals inbox — stub. Real implementation lands in Phase 3 M4.
 * Wired up here so the sidebar tab switch has something to render without
 * crashing during M3 (shell only).
 */
export default function PrincipalApprovals() {
  return <div className="px-9 py-8">Approvals page coming in M4.</div>;
}
