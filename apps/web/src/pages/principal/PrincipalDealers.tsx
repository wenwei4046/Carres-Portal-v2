/**
 * Principal Dealers admin — stub. Real implementation lands in Phase 3 M5.
 * Wired up here so the sidebar tab switch has something to render without
 * crashing during M3 (shell only).
 */
export default function PrincipalDealers() {
  return <div className="px-9 py-8">Dealers page coming in M5.</div>;
}
