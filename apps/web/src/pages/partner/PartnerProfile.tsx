/**
 * Partner Profile — stub. Phase 4.5 Chunk 1 leaves this for a later sub-phase
 * (4.5b polish). At minimum, sign-out happens via the sidebar's `/me` link.
 */
export default function PartnerProfile() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold text-foreground">Profile</h1>
    </div>
  );
}
