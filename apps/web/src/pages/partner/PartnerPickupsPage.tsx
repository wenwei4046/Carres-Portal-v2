/**
 * Partner Pickups — stub. Phase 4.5 Chunk 1 Task 26 wires the real
 * implementation (leg-type chips + RFD-pending indicator + accept/reject
 * dialogs).
 */
export default function PartnerPickupsPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold text-foreground">Pickups</h1>
    </div>
  );
}
