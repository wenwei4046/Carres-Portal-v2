/**
 * Partner Dashboard — stub. Phase 4.5 Chunk 1 Task 25 wires the real
 * implementation (today's pickups KPI, RFD-pending count, etc).
 */
export default function PartnerDashboard() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold text-foreground">Partner Dashboard</h1>
    </div>
  );
}
