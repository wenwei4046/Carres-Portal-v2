# Open carry-forwards (living doc)

> **What this is**: the full text of every open carry-forward (CF), moved out of CLAUDE.md
> §17.5 on 2026-07-25 to keep CLAUDE.md lean. **This is the living doc**: when a session
> opens a new CF, append the full entry HERE and add a one-line index row in CLAUDE.md
> §17.5; when a CF closes, mark it [CLOSED date + how] here and delete its index row there.
> Content below is the verbatim §17.5 text as of 2026-07-25.

---

### 17.5 Open carry-forwards

**HIGH**:
- ~~`line-category-calls-itself-DISPLAY-ONLY-and-two-callers-filter-stock-with-it`~~ — **CLOSED 2026-08-20 the same day it was opened, by `CARD-2026-08-20-so-loan-category`.** The order-detail endpoint now resolves both the line SKUs and the free-unit SKUs through `skuCategories` — the one shared reader — and attaches `category` to each; `OrderDetailDrawer.tsx`, `LoanPanel.tsx` and `StockPickerGrid.tsx` read it through the new `resolvedCategory(sku, category)` instead of parsing the SKU text. All thirteen production SKUs the keyword list misreads are asserted to resolve correctly. ⚠️ **TWO THINGS ARE DELIBERATELY NOT CLOSED, and neither is an oversight.** ① **The parser is still reachable**: `resolvedCategory` falls back to `lineClass` when the catalog holds no row (`null`), because 975 live units are in that bucket and treating them as “not this category” would empty the loan picker. The exit condition is a NAMED TEST, not a comment — when the catalog is populated, that expectation flips and `lineCategory` dies with it. ② **D9 itself is not closed**: the storage rate is the third answer and still its own, tracked under `d4-is-now-three-storage-arithmetics`. Original finding, kept because the reading lesson is the point: **found 2026-08-20 by grepping the callers instead of trusting the doc comment, one day after `56239a3c` (PR #859) closed Stock's half of D9.** `packages/shared/src/line-category.ts:118-136` heads `lineCategory` with **“⚠️ DISPLAY GROUPING ONLY — never ask this whether goods are safe to send”** and names exactly two consumers (`OperationOrdersControl.tsx:1542`, `OrderDetailDrawer.tsx:3506`), both of which only choose a header to print a row under. **The grep returns more than two, and two of the extras FILTER STOCK.** Measured: `OrderDetailDrawer.tsx:3618` builds `orderCategories` as `[...new Set(goodsLines.map(l => lineCategory(l.sku)).filter(c => c !== "acc"))]` — and its own comment says why: *“acc would let keyword-missed units leak in”*. The code already knows the parser misses models; its mitigation is to DROP the miss bucket. `LoanPanel.tsx:714-715` then runs `const catSet = new Set(orderCategories); const lendable = freeUnits.filter((u) => catSet.has(lineCategory(u.sku)))`. **So a miss on EITHER side deletes the unit from the flow**: an unrecognised order line contributes no category, and an unrecognised free unit resolves to `acc`, which was deliberately excluded — a real sofa sitting in the warehouse cannot be offered as a loaner because its model name is not in a regex. `StockPickerGrid.tsx:140` does the same thing one screen over: `loanMode ? lineCategory(u.sku) === "sofa" : stockMatchKey(u.sku) === matchKey`, so the same unit is absent from the loan picker entirely. This is D9's own signature, stated in `ERP-ARCHITECTURE.md:28` — *“the Sofa facet reads 0 while 10 of 28 live orders carry a sofa”* — a filter quietly returning nothing, not a wrong label. **Why it gets worse on a date:** `lineClass` (`:86`) recognises goods from a hardcoded keyword list (`jager|cody|trion|hilton|fenrir|ricardo|regal|divan`, `hk55|dsl90|…|seater|incliner|eleganz`, `firmcare|softcloud|…`) compiled into shared source. Every model sales key into the catalog under Jess's 2026-08-19 direction is absent from that list and returns `unknown`. **The catalog will hold the right category and the loan flow will not see it.** ⚠️ `chase-supplier-plan.ts:60` is NOT affected — it reads `meta?.category` first and only falls back to the parse, which is the shape the fix should generalise. **Firm fix, and it is precedented rather than novel:** `56239a3c` already shipped the reader — `apps/api/src/lib/sku-categories.ts` resolves `sku → product_skus → product_models.category` and `/inventory` consumes it. Carry `category` on the order-line and free-unit payloads from that same reader, and have `LoanPanel` and `StockPickerGrid` read the field instead of parsing the string. **Keep `null` and ABSENT apart exactly as #859 does** — `null` means the catalog was asked and holds no row; no key at all means this endpoint did not ask, and only the asking endpoints may carry it. **Do NOT fix this by adding the new model names to the regex** — that IS the defect, repeated once per new product, and it moves catalog ownership back into `packages/shared`. **Falsifier:** if `lineCategory` is gone from both files and the loan picker still refuses a unit whose SKU matches no keyword list, this entry is wrong. **Bounded today only because §6 says every row is test data — and that bound expires the moment sales key a real product in.** **The reading lesson, recorded because it is the reason this sat unseen:** the doc comment was accurate when written and is now an under-count, and a chat (this one) repeated “display-only, therefore cosmetic” out of it before running the grep. A comment describes a blast radius on the day it was typed; `grep` describes it today.
- `emergency-restock-has-no-governed-po-continuation` — **found 2026-08-11 by Card 4B, recorded under the card's own §7 instruction not to solve it.** Urgent restock (card K3, migration 0290, `UrgentRestockPanel`) runs `raise → COO approves → … → "I ordered it"`, and the `…` was the `+ New PO` modal: an approved ask creates NO `purchase_demands` row and never reaches Batch Purchase. Card 4B deleted that modal and revoked `operation_create_po` / `operation_create_pos_batch` / direct table INSERT (migration 0339), so **an approved emergency now has no governed way to become a Purchase Order** and `I ordered it` records work done somewhere this system cannot see. **FORBIDDEN as fixes, all four named by the card:** keeping a legacy authority alive for Emergency · bypassing the governed boundary · auto-converting approved asks into `purchase_demands` · minting a new emergency exception. **The open question is a BUSINESS one and belongs to Jess, not to engineering:** does an approved emergency ask become a `purchase_demand`, and if so who supplies the supplier, quantity, cost and destination Batch Purchase will require — given K3's own ruling that an emergency deliberately carries **no** suggested quantity, because the demand is by definition absent from sales history. Full statement in `docs/purchasing/MASTER.md` §2.0.1. **Bounded today only because §6 says every row is test data and the emergency lane has never run for real.**
- ~~`rental-complete-order-has-no-in-flight-guard`~~ — **CLOSED 2026-08-07 by `66846fd0` (YH, "rental cart page UI fixed"). This entry then sat OPEN for thirteen days describing a fixed defect, and was caught on 2026-08-20 only because someone re-measured before quoting it into a briefing for Jess. Both halves are closed, and the code carries the reasoning:** the firm fix below is what shipped — `const [rentalSubmitting, setRentalSubmitting] = useState(false)` (`DealerPos.tsx:532`) sits inside `submitDisabled` at `:538`, and its doc comment states the Law C failure it exists to stop. The SECOND, separately-undiagnosed defect — *the confirm screen does not advance after success* — is closed too: `:1228` branches the whole `<main>` on `rentalDone` and renders the agreement numbers, `No payment has been collected.`, and a Start-another action; `:1405-1413` records why `!rentalDone` is load-bearing in the step-3 gate. **The lesson this entry now exists to carry is about the DOCUMENT, not the code: a carry-forward is a claim with a date, and a claim that is not re-measured before it is repeated becomes a lie told with confidence.** Re-measure every entry at the moment you quote it (Constitution §11). The original diagnosis, kept because the reasoning is still the reference for this class of bug: **found on the real POS 2026-08-06 by clicking the button twice.** Symptom: `Complete order` does not navigate, operations DOES receive the order, and every further click mints another agreement — the `RA-` number increments each time. **Measured cause, not inferred:** `submitDisabled` (`DealerPos.tsx:518`) is `!confirmReady || uploading || createOrder.isPending || !effectiveDealerId`. `handleSubmit` (`:612`) branches at `:615` — `if (cartModeOf(draft.lines) === "rental") { await submitRentalCart(); return; }` — and that `return` happens **before** `setUploading(true)` at `:621`, while `submitRentalCart` never touches `createOrder`. So for a rental cart **neither flag this expression reads is ever set**, and the button stays live for the whole in-flight window. It only goes disabled *after* success, incidentally, because `clearDraft()` (`:589`) drops `confirmReady`. The sale path is guarded; the rental path inherited a guard that cannot see it. **This is ERP-ARCHITECTURE Law C — a door, never a duplicate.** The file already knows the failure mode: `:602-607` warns *"a half-finished run must not look like nothing happened, or the store re-submits and double-signs the customer."* **Firm fix: one `submitting` state owned by `submitRentalCart`, added to `submitDisabled`** — do NOT solve it by disabling on `rentalDone`, which is set only after the whole loop finishes and leaves the entire in-flight window open. **Second, separate defect in the same report and NOT yet diagnosed: the confirm screen does not advance after success** — `setRentalDone(done)` fires at `:590` but the operator sees no state change, which is *why* they click again. Read the `rentalDone` render path before fixing; the guard alone hides this rather than closing it. Bounded today only because §6 says every row is test data.
- `phase-10-rotate-alpha-test-passwords` — 9 alpha users at password='111' (sales / operation / hookka / nicefuture / nets / finance / bd / mattress / sales-mk). PrincipalAccounts UI built 2026-05-18 (commit `941c665`) gives Loo Reset password per row — rotate via UI before sharing portal externally.
- `phase-9-rotate-principal-password` — principal@carres.com still at password='111' (Phase 9 known-risk; rotate Week 2). If brute-force detected: `update auth.users set encrypted_password = crypt('<new>', gen_salt('bf')) where email='principal@carres.com'`. PDPA fine up to RM 300k if principal-level breach.
- ~~`pencil-on-a-persisted-rental-line-replaces-a-signed-agreement`~~ — **CLOSED 2026-08-11, and NOT by the firm fix below — by the Law C refactor it was sequenced behind.** `orderLineEditKind` is now a wrapper over the shared `configureSurfaceFor` core (see `two-line-edit-gates-answer-one-question-and-have-drifted`), and the core routes a rental row to `"rental"`, which the persisted wrapper refuses: only `"bed_mattress"` reopens in place. So the pencil never renders on a rental line and `startEditLine` returns before `setEditing`, which is the same door the inline `isRentalLine` guard would have shut — one guard fewer, and it cannot go stale, because the cart and the drawer now read the SAME routing. Covered both ways: `order-line-edit.test.ts` asserts the core says `"rental"` while the wrapper says `null`, and `PosOrderDetail.test.tsx` asserts no `pos-od-edit-line` renders on a rental row. The rental attrs in both tests are the shape `0275:234-242` really stamps (`agreementId` · `agreementNo` · `planId` · `termMonths` · `monthlyFee` · `contractTotal`) — verified against the migration, because `isRentalLine` requires `planId` + `termMonths` and a `{ rental: true }` fixture would have passed for a reason production never produces. **Unchanged and still true: this is not a rental-amendment route.** `replace_order_lines` still has no rental guard; the client simply no longer offers it a rental line. A real amendment path is Rental's to build (Law A), and the one line in `orderLineEditKind` that refuses `"rental"` is where it opens. Original finding kept below — the write-path evidence is what makes that refusal non-negotiable: **found 2026-08-07 by a read-only review sweep, traced through the write path, not observed on the POS.** `orderLineEditKind` (`order-line-edit.ts:32`) has no rental branch, so a persisted rental line falls through to `"bed_mattress"` and `PosOrderDetail` opens the OUTRIGHT-SALE configurator over it. **Reachability is proven, not assumed:** migration `0275_rental_makes_a_sales_order.sql` (re-issued unchanged in `0279:309-332`) makes `create_rental_agreement` insert an `order_lines` row — `unit_price 0`, `attrs.rental` carrying `agreementId`/`termMonths`/`monthlyFee` — into the same table `GET /api/orders/:id` reads, called live from `rental.ts:1726`. `BLOCKED_MARKERS` (`free_gift · free_item · pwp · bundle_group · combo_key`) does not include `rental`, and `laneOf` (`order-edit-scope.ts:26-32`) does not divert `source_system='rental'`, so the order sits in the operator's own "Order placed" lane with a live pencil. **The consequence is not "the wrong form opens".** `handleReplace`'s up-sell precheck is `newTotal < target.oldTotal` and `oldTotal` is **0**, so every configuration passes; `replace_order_lines` (`0256:100-105`) rejects the four promo markers and has **no rental guard**. The rental's fulfilment line is deleted and replaced by an outright mattress at retail: `rental_agreements.order_id` still points at the order but the line carrying `agreementId` is gone, `rental_billings` and the Stripe subscription are untouched, and an order 0275 deliberately priced at zero now shows retail outstanding — **the exact lie 0275's own header says zero exists to prevent** (*"finance's AR would show RM2,499 outstanding for a customer who owes nothing today"*). The window is `status='place'`, i.e. **while the rental awaits finance credit approval**, which is when an operator is most likely to reopen it. **Firm fix: `if (isRentalLine(line)) return null;` before the sofa test**, importing the existing `isRentalLine` from `./rental-cart` (verified exported at `rental-cart.ts:66`, requires `planId` + `termMonths`, both stamped by 0275) rather than testing `attrs?.rental` inline — one reader for one marker. Update the file header's claim that it *"Mirrors the wizard-cart rule"* in the same edit or the comment becomes the drift. **Do NOT "fix" this by routing to `RentalConfigurePage` the way `cart.ts` does.** The two surfaces are not the same question: `cart.ts` is pre-submit and emits a fresh draft line, and that routing is correct and stays. `RentalConfigurePage.tsx:123` emits `unitPrice: plan.monthlyFee` — a MONTHLY figure — which pushed through `handleReplace` writes RM69 into `order_lines.unit_price` on a signed order and still never touches `rental_agreements`, `rental_billings` or Stripe. Amending a signed rental is Rental's record (Law A); a second form for it here is Law C's duplicate, failure mode D2. A real rental-amendment route is a separate build. **Clears the §11 Orders-V1 freeze bar in my reading** (it corrupts a signed agreement's fulfilment line and fabricates AR) — but that call is Loo's.
- ~~`import-skus-cannot-complete-more-than-about-45-rows`~~ — **CLOSED 2026-08-20 by the one acceptance test it was held open for.** YH imported `loadtest-60-skus.csv` through Catalog → SKU Master → Import SKUs against production: **60 SKUs upserted, 12 new products created, 0 failed.** Sixty rows and twelve new models in one call — on the old route that is `3 + 12 + 60 = 75` subrequests against a cap of 50, so this exact file could not have completed before `0358`, and the pass is therefore evidence and not a coincidence. **This is the proof the SQL editor could never give**, because the defect lived in the Worker's subrequest budget and the editor never enters the Worker. ⚠️ The endpoint is now on Jess's 2026-08-19 critical path (a paper Hookka quotation becomes a CSV loaded into the products page), so treat a future regression here as blocking catalog data entry, not as an import nicety. The full record of the fix follows. That is the whole remaining acceptance test, and calling the RPC from the Supabase SQL editor does not satisfy it — the defect lived in the Worker's subrequest budget, and the SQL editor never enters the Worker. Test file ready at `loadtest-60-skus.csv` (60 rows, 12 models); PASS is `upserted 60, createdModels 12, failed 0` in the dialog. ⚠️ **This is no longer only an engineering loose end** — Jess's 2026-08-19 first priority (*a paper Hookka quotation becomes a CSV loaded into the products page*) runs through this exact endpoint, so the import is now on the critical path for catalog data entry. **The header of this entry read `NOT YET APPLIED` for two days after it was applied**; that was caught on 2026-08-20 by re-measuring, and it is the same failure as the rental entry above. The full record of the fix follows. Migration `0358_the_sku_import_is_one_call.sql` adds `catalog_import_skus(p_payload jsonb)` on the 0143 pattern: find-or-create every model, then upsert every row IN INPUT ORDER inside one transaction, each row in its own `BEGIN/EXCEPTION` subtransaction, returning a per-row result array. The route (`catalog.ts`) keeps its three batched reads and collapses every write into that one call — a **constant 4 subrequests** for a 10-row file and a 500-row file alike. `SECURITY INVOKER`, deliberately: RLS (0002) and the principal-only price trigger (0175) stay exactly where they are, and the migration's sanity block fails if either changes. Guarded by `catalog.test.ts` — 120 rows across 30 models must produce exactly one `.rpc` and three `.from` calls. **VERIFIED 2026-08-18 against the live database**, as the real users, inside transactions that were rolled back — `create function` is transactional, so the whole migration plus its checks ran in one `begin; … rollback;` and nothing was applied. (A) IN-ORDER LAST-WINS AND ABSENT-KEY-MEANS-PRESERVE, as principal: `price 100, desc X` then `price 200, no desc` returned `created_models 1, rows [inserted, updated]` and left **`200.00 | X | size`** — not `200 | null`, which is the result that would have meant every re-import silently wipes the columns an operator left blank. (B) THE GUARD FIRES, as a dealer: `ERROR 42501 forbidden: catalog import is internal only`, raised at the function's line 17 — not a clean empty report. (C) THE 0175 PRICE LOCK STILL BITES THROUGH THE RPC, as operation: the unpriced create succeeded, the `price 999` attempt came back a per-row error, and the sku read **`0.00`**. That last read proves BOTH halves at once — the price was refused, AND the refusal was per-row rather than fatal, because a thrown call would have aborted the transaction and the read would never have run. **APPLIED 2026-08-18** — function present, `prosecdef = false`, EXECUTE granted to `authenticated`, all confirmed by query after apply; the `schema_migrations` tracker row was written BY HAND, because the SQL editor writes none. Merged as PR #844 and deployed on `df46a9d5` (both Pages projects and the Worker reported that SHA). **What is STILL not proved, and the ONLY thing keeping this entry open: no real file over 45 rows has been imported end to end.** One such import closes it. For the record when you do: the tracker query caught TWO collisions here in one hour — written as 0356, renumbered when `0356_a_delivery_order_is_a_document_with_its_own_register` (20260818031718) turned up applied twenty minutes after a fetch reading the tail as 0355, then renumbered again when `0357_a_cancelled_order_voids_its_delivery_orders` (20260818033500) turned up applied eighteen minutes later — that one from `origin/feat/do-auto-complete`, an UNMERGED branch, so watching `main` alone would never have shown it. **Prove the SQL first with the dry run at the foot of the migration (it creates the function inside a transaction and rolls it back, so it needs no number), then check the tail and apply in the same minute.**, run the verification block at the foot of the migration in a rolled-back transaction INCLUDING its negative control, then import one real file over 45 rows and confirm it completes. The absent-key-means-preserve and in-order last-wins rules moved from TypeScript into SQL, so that verification is now their only proof — vitest cannot reach them. Original entry, for the record: **found 2026-08-07; the endpoint advertises 500 rows and delivers about 45.** The binding constraint is **Cloudflare's per-invocation subrequest cap**, not wall-clock or CPU — Workers have no request-duration cap while the client stays connected, and awaiting I/O accrues no CPU, so the obvious diagnosis is the wrong one. Every supabase-js call is one `fetch` = one subrequest. `POST /catalog/import-skus` spends **`3 + M + W`** of them (3 batched reads, one INSERT per new model in the loop at `catalog.ts:1047-1071`, one UPDATE/INSERT per row at `1175`/`1183`) against **50 on the Free plan**. A 40-row file introducing 8 new models is `3 + 8 + 40 = 51` — over. The 500-row cap is real and enforced (`sku-import.ts:215`), and `ImportSkusDialog.tsx:76` sends the whole file in one `mutateAsync` with no client-side chunking and no client-side cap. **The failure is silent:** postgrest-js resolves `{data, error}` rather than throwing (`shouldThrowOnError` defaults false), so the per-row `if (error)` swallows *"Too many subrequests by single Worker invocation"* and the route returns **HTTP 200** with `upserted: ~45, failed: ~455`, reasons truncated to 50 and all identical — indistinguishable from a bad file. **This repo already paid for this lesson and did not carry it forward:** migration `0143` (2026-05-21) records Loo seeing the same signature (*"48 updated, 67 failed"*), names the 50-subrequest cap in its header, and fixes it with an `import_autocount_orders` batch RPC that holds the Worker at a constant 2 subrequests, guarded by a live regression test asserting `sb._calls` has length 1 (`orders-import.test.ts:264-308`). **`import-skus` shipped 2026-06-25 — five weeks later, without it.** **Firm fix: a `catalog_import_skus(p_rows jsonb)` RPC on the 0143 pattern** — loop the rows IN ARRAY ORDER inside one transaction, each row in its own `BEGIN/EXCEPTION` subtransaction, returning a per-row result array; the route collapses to one `sb.rpc` call and a constant 1 subrequest. Number it from the MAX of tracker/repo/branch tails, never `ls` (red line 7); repo tail was `0325` on 2026-08-07. **Do NOT fix this with a bounded `Promise.all` pool** — it cuts wall-clock but not subrequest count, so it fixes nothing that is actually broken, and it is the one change that breaks the last-wins merge at `1207-1209`. In-order replay inside the RPC preserves that merge for free, because iteration N sees N-1's insert; note also that a naive "keep the last duplicate" shortcut is ALSO wrong, since row B's UPDATE patch carries only its present fields (`price 100, desc X` then `price 200, no desc` must yield `price 200, desc X`, not `desc NULL`). **Ship-today mitigation while the migration waits for review: chunk at 20 in `ImportSkusDialog.confirm()`**, sequentially, re-basing each chunk's failure row numbers onto the whole file — worst case `3 + 20 + 20 = 43`, and last-wins survives a chunk boundary because the server re-reads existing SKUs per chunk. **Unverified and worth one check before quoting the numbers to Loo:** the live Cloudflare plan. Everything in the repo says Free (`0143`'s header, `CARRES_PORTAL_V2_PLAN.md:994`, `docs/archive/phase-4-reflection.md:67` — *"never triggered"*), but billing is not in the repo. On Paid the cap is 1000 and the practical ceiling rises to ~995 writes, which still breaches the documented 500-row worst case when most rows create models.
- `d4-is-now-three-storage-arithmetics-and-they-already-disagree` — **NOT a new defect: this updates the record on D4, which `ERP-ARCHITECTURE.md` §7 and `CLAUDE.md` §11 both hold OUT of V1 deliberately.** Do not open a fix card off this entry. What changed is the shape of the thing being deferred. D4 was catalogued as *"Two storage arithmetics for one order"*. There are now **three**, and they **already produce different answers about money** — the doc's own Law D warning (*"Not two implementations that currently agree — that is the arrangement under which a third, wrong one grows unnoticed"*) has already come true, which is worth knowing when §6.1's ownership ruling is finally built. **A** = `storageCharge` (`OrderDetailDrawer.tsx:1790-1794`) → `invoiceTotal`, `balanceDue`, MoneyCard. **B** = an inline ternary at the `StorageCollectWaiver` call site (`:4004-4008`) → the released/written-off wording. **C** = `effTotal` inside `StorageCard` (`:6412-6418`) → `waived`, `statusName`, `feeDue`, the input tint. StorageCard is passed ten props and **not** the computed charge. B is the tell: someone wrapped that call site precisely because `storageCharge` alone was the wrong number there. **Where they part:** A gates on `storageIncurred` (`:1758-1762`), which requires `effectiveStorageStart`; B and C do not. So on an order that has been **delivered** while `storage_from` was never set manually (`:1749-1755` nulls it on `deliveredForStorage`), with a non-zero `storage_fee_override` and no Master-imported fee, **A = 0 while B = C = the override** — the Storage tab reads *"Released by the manager · RM 300 storage fee still to collect"* while the Balance tab's invoice charges RM 0 and `balanceDue` omits it. Exempt, imported-fee and auto branches were walked and agree. **When D4 is picked up, the merge needs a semantic that is written down nowhere:** does a manual override bill on an order whose clock never started? B and C say yes (2 of 3), A says no. §6.1 rules WHO owns the fee (Money In) and that the clock is the order's; it does not answer this. Recommendation for whoever asks Jess, not a derivation from the repo: **the override wins** — it is a human decision and it is already symmetric with `override = 0` meaning "No storage". **One SQL query decides whether this is a live money defect or a loaded gun** and should be run before anyone argues it clears the freeze bar: any row with `storage_fee_override` set, `storage_from` blank and `storage_waiver_status='approved'`. Per §6 a zero count does not close it.
 **➕ RE-MEASURED 2026-08-28, and it is worse than this entry says: the storage rate is TWO wrong answers, not one.** The drawer's Storage tab asks `lineCategory(sku)` (`OrderDetailDrawer.tsx:1501-1505`) while the shared rule and all four server gates ask `storageCategoryForSku` (`ops-order-control.ts:641`) — different functions, different rules, nothing forcing them to agree, and only the first has a catalog-aware wrapper. Measured the prefix parser against eight real production SKU shapes (`B1201S-K` · `B1201S` · `MODEL-C` · `ESS-PILLOW` · `B2003-Q` · `SF-3STR` · `NF-MATT-K` · `OH-BF-Q`): **all eight return `other`**, so no rate applies and the fee computes RM 0 against a live RM150/month + RM200 table. `B1201S-K` is the same SKU `orders/MASTER.md` records as a production-verified mattress — it reads correctly everywhere the catalog is asked and wrongly here. **Now scoped as `docs/cards/CARD-2026-08-28-storage-rate-asks-the-catalog.md` (QUEUED).** It implements the FROZEN §6.1 ruling, so it carries no owner question, and it is 🟢 READY rather than an engine gap: `skuCategories` and `resolvedCategory` both already exist from D9's other two thirds.
- `register-row-double-click-does-not-open-the-workspace` — **found 2026-08-10 on PRODUCTION during the Sales Order cutover smoke test, and it is NOT a cutover regression: it reproduces identically on the pre-cutover deployment.** Symptom, measured on `erp.carresofficial.com` and again on `71a1b554.carres-portal.pages.dev` (the predecessor, same account, same row SO-1308, both 1440 and 1130): a real mouse double-click on a register row leaves the URL at `/operation/orders`. It never becomes `/operation/orders/so/:id`. A right-click on the same row surfaced no context menu either. **The wiring is correct in source** — `SalesOrdersRegister.tsx:312` passes `onRowDoubleClick`, `DataGrid.tsx:1221` puts `onDoubleClick` on the `<tr>`, and `openWorkspace` (`:307`) navigates — and **the destination is healthy**: navigating to `/operation/orders/so/f14e4c0e-…` directly opens the workspace with title `SO-1315 — Carres`, `[Edit] [Print PDF] [Back to register]`, a working `Print PDF`, and an `Edit`+`Save` that mints a proper before/after revision pair. **So the defect is in getting there from the row, not in the page.** **HYPOTHESIS, not a finding — do not write it up as a cause without checking it:** the register passes `selectable` and no `onRowClick`, so click 1 toggles selection (`DataGrid.tsx:1216-1219`) and re-renders; the grid is virtualized, so the `<tr>` node under the cursor may be replaced between the two clicks, and a browser only emits `dblclick` when both clicks land on the same element. **First thing to check is cheaper than the fix: whether a HUMAN mouse reproduces it.** Every observation here is Playwright-driven, and the whole class of bug this hypothesis names is exactly the class that automation can manufacture on its own. If a human cannot reproduce it, this entry closes as an automation artefact and the only cost is a test that must open rows by URL. If a human CAN, the row is the only door an operator has to the workspace on the page that is now the OFFICIAL Sales Orders entry, and it is a blocker. **Out of scope for the cutover card by its own MUST-NOT list** (`✗ modify Stage 1–3 business design in any way`), which is why it is recorded rather than fixed.

- `two-refund-tables-both-live-and-neither-knows-the-other` — **Law C, `a door, never a duplicate`. Found 2026-08-12, re-measured on `main` 2026-08-14 — still true.** `refunds` (`0001_init.sql:422`) and `order_refunds` (`0345_a_refund_is_a_debt_until_it_is_paid.sql:36`) both exist and both have live doors. `refunds` has the **only UI** (`apps/web/src/pages/finance/FinanceRefunds.tsx`, routed `FinanceApp.tsx:40`), its API is mounted (`apps/api/src/index.ts`, 2 hits for `finance/refunds`), and it carries the **RM 1,000 approval gate** (`apps/api/src/routes/finance/refunds.ts:101`). `order_refunds` has four newer routes (26 `refund` references in `apps/api/src/routes/operation/order-payments.ts`) and no UI. **0345 never mentions the older table** — `grep -n "refunds" 0345_*.sql | grep -v order_refunds` returns one unrelated error string. **What breaks:** Card 8's derived completion reads `order_refunds` only (`orders.ts` → `sales-order-completion.ts`), so a refund raised through Finance — the only door with a screen — leaves MONEY OUT reading clear while Carres still owes the customer. And a refund raised through the new operation door skips the RM 1,000 gate entirely. **This is not an engineering decision.** Choosing which record survives means dropping a table or deleting live routes: red lines 1 and 5, so it needs Jess's confirmation in the conversation where it happens. **Until then do NOT add a second UI to either table** — a screen is what turns a duplicate record into a duplicate habit.
- ~~`the-payment-ledger-double-counts-a-mirror-row`~~ — **WITHDRAWN 2026-08-17. I wrote this entry, and measuring it properly shows the defect it names does not exist.** The claim was that `summarizePayments` adds a `counted_in_paid = false` MIRROR row — money the raw-create door already put into `orders.paid` — and so counts real money twice. **A double-count needs one reader to sum the ledger AND `orders.paid`. Measured: no reader does.** The drawer's Balance panel computes against `ops_order_control.balance`, the imported outstanding, under Jess's 2026-07-02 ruling recorded at `OrderControlPanel.tsx:560-563` (*"balance IS the amount owed; payments in the ledger reduce it"*) — `orders.paid` never enters it. The collections desk takes goods money from `orderMoney`/`orders.paid` and reads the ledger for STORAGE only, which is a separate debt with its own clock and its own gate. And `counted_in_paid` **is** consumed where it belongs: `0343:204`, the void path, which reverses `orders.paid` only for a row that bumped it. **What the measurement did find, and it is now fixed rather than carried:** `PaymentSummary.paid` had **zero readers** across `apps/` and `packages/`, and it summed every kind together — goods money plus a storage collection. Not dead weight but a loaded field: the first person reaching for an innocent "total paid" would have got the two added, which is how the double-count I imagined would actually have arrived. The field is removed; a caller must now name `byKind.payment + byKind.deposit` or `storageCollected`. **The lesson worth keeping is about the entry, not the code:** it was written from 0343's comment describing the hazard, not from tracing who reads the number. That is the same failure this file records against me twice before.
- `mail-cannot-leave-the-building-and-the-recovery-path-is-dead` — **found 2026-08-12 mapping email infrastructure before the Resend build; three separate faults, one theme.** ① ~~**Password reset goes nowhere.**~~ **CLOSED 2026-08-17 by `70398581` (YH, "recovery gets its step 02, and the reset link finally lands")** — `UpdatePassword.tsx` exists, `App.tsx:98` routes `/update-password` to it, and `UpdatePassword.test.tsx:57` is a live regression test asserting the path resolves to that page and NOT to the catch-all. ⚠️ **② and ③ below are still OPEN**, so this entry does not close. *(Caught stale on 2026-08-20 — this fault had been fixed for three days while the entry still described it as live.)* The original diagnosis, for the record: `Login.tsx:8` sends the recovery link to `${origin}/update-password`. That string appeared **once in the entire source tree — in that line.** No route in `App.tsx`, no component, and zero `PASSWORD_RECOVERY` listeners. Because `detectSessionInUrl: true` (`lib/supabase.ts:24`), clicking the emailed link **silently signs the user in** and the catch-all route drops them on the home page with no password field. It is the ONLY email this portal sends, and two HIGH entries above depend on it for rotating the `111` passwords. ② **`carres.com.my` is not a registered domain** — `nslookup` returns `Non-existent domain`. The login page's *"Contact admin → admin@carres.com.my"* writes into the void, which is the other path a locked-out user takes. ③ **`carresofficial.com` has Google Workspace (MX `smtp.google.com`) and DKIM published, but NO SPF and NO DMARC record.** So company mail already sends unauthenticated. **This matters before the Resend work, not after:** a domain may hold exactly ONE SPF record, so the value must cover Google and the new sender together — two records make both invalid and break company email. Verifying a subdomain (`send.carresofficial.com`) is the option that cannot break Workspace. **Fix ① first and separately** — it is a live user-facing defect, it needs no DNS, and the mail system will want that route anyway.

**MEDIUM**:
- `copy-sales-order-clones-a-promo-price-and-drops-its-reason` â **found 2026-08-24 by the read-only PWP/promo walk (25 agents, every finding adversarially refuted before it survived); PARKED THE SAME DAY by YH pending owner question Q2 â do not fix without that ruling.** `apps/web/src/pages/operation/sales-order-copy.ts:80-84` maps each source line to exactly `{ sku, qty, unit_price: Number(line.unit_price) }` â **the RM 0.00 PRICE of a promo/gift line survives the copy and its MARKER (`attrs.pwp` / `attrs.free_item` / `attrs.free_gift`) does not**. The new order therefore carries free goods no rule granted, no trigger backs and no voucher paid for, and every downstream promo guard is blind to them because the only thing those guards read is the marker that was dropped. **Two in-repo comments claim the opposite behaviour and are BOTH FALSE** â `SalesOrderWorkspace.tsx:149-151` and `:1336-1340` say a copy re-derives or strips promo pricing; measured, it does neither. Verified twice: once by the walk's finder, once by an independent refuter told to kill the claim, and once more by hand in this session before it was written anywhere. **Why it is PARKED and not fixed: the three defensible fixes give three different businesses.** (a) refuse to copy promo/gift lines â a copy is then always honest but sometimes incomplete; (b) copy them at CATALOG price â the goods survive but the customer's deal does not; (c) copy the MARKER and re-validate against today's rules â the deal survives when the rule still stands and dies loudly when it doesn't. Engineering can build any of the three in an afternoon; **only the owner can pick, because each answer changes what a copied order MEANS** (owner question Q2, asked 2026-08-24 and deferred by YH: âleave them be, tackle when such edge cases hitâ). **Firm fix once ruled: implement in `buildCopyDraft` â one function, already pure, already tested** â and correct the two false comments in the same commit. **Do NOT** âfixâ it meanwhile by stripping the RM 0.00 silently: a copy that re-prices lines without saying so is the same silent-money-edit disease in the other direction. **Falsifier:** if `sales-order-copy.ts` maps line `attrs` (or refuses marked lines) and both comments are corrected, this entry closes. Bounded today only because Â§6 says every row is test data â and by the odds of an operator copying an SO that carried a promo line, which rise as promos start being authored for real.
- `red-is-doing-more-than-one-job-on-the-operation-pages` — **found 2026-08-21 by sweeping `apps/web/src/pages/operation/` for red token classes (report-only slice; nothing was changed).** Blueprint §0 locks *“Red has one job: late / act now. Blue marks the current thing or primary action.”* Measured: **156 red usages across the operation pages**, of which **134 are legitimate** (late, overdue, owed, refused, cancel, delete) and **22 are red doing a different job** — mostly a red NUMBER used for emphasis or a red panel used to mean “exception” rather than “act now”. Worst concentration: `OperationPurchaseOrders.tsx` (6 of the 22). ⚠️ **A separate and sharper finding rides along: three files paint red with RAW TAILWIND rather than the kit tokens** — `AnnotationTimeline.tsx:60,137` (`text-red-700`, `bg-red-50/60`), `EscalationInboxCard.tsx:16,20` (`text-red-600`) and `GlobalActivity.tsx:152` (`bg-red-50/60`). Those bypass `kit-red-*` entirely, so a palette change would miss them and they are invisible to any audit that greps the token names. **Firm fix, in two independent passes:** ① move the three raw-Tailwind files onto `kit-red-*` (mechanical, no judgement, no behaviour change); ② review the 22 semantic uses one at a time with the owner’s rule in hand — an *exception* that nobody must act on today is not `late / act now`, and the honest alternative is the amber attention token already used at `SalesOrderWorkspace.tsx:1862`. **Do NOT sweep by string match**: the `Recovery` lesson in COPY-STANDARD applies — a word (or a colour) is banned where it names the banned concept, and a chat renaming every hit will “comply” its way into a worse screen. **Falsifier:** if the owner rules that an exception marker IS act-now, the 22 collapse to zero and only pass ① remains. **Bounded today because none of this is wrong data — it is a screen teaching the operator that red means ‘look here’, which is exactly how red stops meaning ‘act now’.**
- `migration-numbers-are-hand-picked-integers-under-three-parallel-lanes` — **found 2026-08-18 after ONE file had to be renumbered TWICE IN ONE HOUR.** `0358_the_sku_import_is_one_call.sql` was written as 0356 when the repository tail, every one of 40+ branches and the tracker all read 0355. Twenty minutes later `0356_a_delivery_order_is_a_document_with_its_own_register` (tracker version `20260818031718`) was already applied; eighteen minutes after THAT, `0357_a_cancelled_order_voids_its_delivery_orders` (`20260818033500`) was applied too. **The second one came from `origin/feat/do-auto-complete`, a branch that has not merged** — so a chat watching `main`, or diffing `origin/main...HEAD`, would never have seen it. Only the tracker query did. **The mechanism, stated plainly:** the scheme is a hand-picked SEQUENTIAL INTEGER, the number is chosen when the file is WRITTEN, and it is not actually claimed until the file is APPLIED or merged. Every minute in that gap is a window for another lane, and this repository runs three lanes shipping several times a day. Postgres does not read the number at all — it is a convention for humans and the CLI — so the collision costs nothing at apply time and everything afterwards: red line 6 forbids altering a committed migration, so the loser renumbers and **the already-applied tracker row keeps its old name forever**. That is not hypothetical. `ENGINEERING.md:267` records two lanes taking 0346 in the same minute on 2026-08-13; **the repository now holds one 0346 and one 0347 while the tracker holds two rows named 0346**, permanently, and a third tracker row carries no numeric prefix at all (`an_appointment_names_the_carrier_it_was_made_with`). **Root cause: Supabase's own `version` column is a TIMESTAMP precisely because timestamps under parallel work do not collide; this repository layered a hand-counted integer on top of it and inherited exactly the failure the timestamp exists to prevent.** **Mitigation that works TODAY and needs no decision** — stop letting the number matter until the last minute: `create function` is transactional in Postgres, so the whole migration plus its checks can be pasted into one `begin; … rollback;`, which proves the SQL, applies nothing and claims no number. Do that FIRST; then check the tail and apply IN THE SAME MINUTE. A renumber before the dry run costs thirty seconds; after the apply it is permanent. 0358 carries this instruction in its own ORDERING header. **The real fix is an owner/architecture decision, not a build fix**, because red line 6 means no existing file can ever be renamed: options are (a) leave it and rely on the discipline above, (b) number new migrations by timestamp so the file name matches the tracker `version` and collisions become arithmetically impossible, or (c) require every lane to apply only AFTER merge, which removes the unmerged-branch surprise but delays every deploy. **Recommendation: (b) for new files only**, since it costs one convention change, needs no rename, and ends the class of bug rather than asking three lanes to be careful. **Falsifier:** if the tracker query shows no further collisions over the next month of three-lane shipping, the discipline in (a) is sufficient and this entry closes without a scheme change. **For whoever picks this up:** the measurement is one query — `select name, version from supabase_migrations.schema_migrations order by version desc` — and it is also the only thing that catches an unmerged branch's applied migration, which is why it belongs in the pre-apply routine regardless of which option wins.
- `e2e-login-helpers-look-for-a-button-that-no-longer-exists` — **found 2026-08-10 while writing the cutover's production smoke test.** `e2e/tab-routing.spec.ts:9` and `e2e/login.spec.ts` drive the login form with `getByRole("button", { name: /sign in/i })`. The login page has since been redesigned: the submit control reads **`Enter portal`** (`<button type="submit" class="cta">`), and the password field's `getByLabel(/password/i)` now resolves to the **`Show password` eye button**, not the input. Both fail with a 30s timeout, so **every e2e spec that logs in is dead**, not flaky — and the suite is not in CI, so nothing reported it. **Firm fix: `page.locator('input[type="password"]')` and `page.locator('form button[type="submit"]')`**, in a single shared `e2e/support/login.ts` rather than re-copied per spec — the copy is why one redesign broke every file. Verified working against production in the same session.
- ~~`escape-closes-every-layer-of-the-pos-order-drawer`~~ — **CLOSED 2026-08-11 by the firm fix below, unchanged.** `PosOrderDetail.tsx` now computes `const nestedSurfaceOpen = editing !== null || stripeOpen;` and its `document` listener reads `if (e.key === "Escape" && !nestedSurfaceOpen) onClose();`, with `nestedSurfaceOpen` in the effect deps. Gated ONLY those two, per the entry — `addOpen`, `addonEditing` and `viewChangeOpen` still register no listener of their own and adding them would have traded a wrong exit for no exit. **The non-deterministic double-close goes with it:** the parent and `StripeCollectModal` were the two `document` listeners racing in registration order, and while `stripeOpen` is true the parent no longer fires at all, so the modal's is the only one left. Three tests in `PosOrderDetail.test.tsx` (`describe("Escape gating")`) pin all three states — closes when bare, does not close while the configure surface is up, and RE-ARMS once the surface closes, which is the assertion that would catch someone "simplifying" the deps array back to `[onClose]`. Original finding: **found 2026-08-07 by review, mechanism settled from the DOM spec, not observed in a browser.** `PosOrderDetail.tsx:665-676` registers an **unconditional** `document` keydown that calls the drawer's `onClose`. The nested configure surfaces register their own on `window` (`PosConfigurePage.tsx:417`, `SofaConfigurePage.tsx:522`), and `StripeCollectModal.tsx:110` registers a second one on `document`. **`window` is not a sibling of `document` — it is the LAST node in the propagation path**, so the bubble order is `target → … → document → window` and the drawer's listener fires FIRST. One Escape mid-edit closes the whole order and takes the in-progress configuration with it. Two `document` listeners also means the parent/Stripe pair fires in registration order, which flips on any board re-render because `onClose` is an inline arrow from `OrderStatusPage.tsx:512` / `BdOrdersBoard.tsx:383` — **non-deterministic double-close**. **Do NOT fix this with `e.stopPropagation()` in the child.** It is unreachable: the parent has already run, and React 18 may flush the resulting unmount at the microtask checkpoint between listener callbacks, so the child's effect cleanup can remove its own listener before it ever fires. **Do NOT fix it with capture-phase on `window` either** — it works, and it makes the configurator a global keyboard interceptor for the whole app, invisible at the parent. **Firm fix: gate the PARENT.** `const nestedSurfaceOpen = editing !== null || stripeOpen;` and `if (e.key === "Escape" && !nestedSurfaceOpen) onClose();`, adding `nestedSurfaceOpen` to the deps. The house already has this rule one directory over — `CatalogStep.tsx:922-924`, *"one Escape = one layer; no stacking fight"* — implemented there by unmounting the outer surface, which `PosOrderDetail` cannot do because it hosts the configurator, so it stands down instead. **Gate ONLY those two.** `addOpen`, `addonEditing` and `viewChangeOpen` register no Escape listener of their own, so adding them would make Escape do nothing at all while they are up — a wrong exit traded for no exit. Giving those three their own handlers *and* adding them to the gate is a second, larger change and must happen in one edit if at all. Costs in-progress work but corrupts no record, so whether it clears the §11 Orders-V1 freeze bar is Loo's call, not engineering's.
- ~~`two-line-edit-gates-answer-one-question-and-have-drifted`~~ — **CLOSED 2026-08-11, and it closed the rental bug with it, which the entry predicted it would not.** `configureSurfaceFor({ sku, attrs }, catalog)` in `order-line-edit.ts` is now the one core, returning `"sofa_build" | "bed_mattress" | "rental" | null`; `lineEditTarget` (cart) and `orderLineEditKind` (persisted) are wrappers holding only what genuinely differs, exactly the two things the entry named — the cart refuses `bundle_group` alone, the persisted row refuses the full `BLOCKED_MARKERS` set for server `line_not_editable` parity; and `attrs.sofa_build` reopens the canvas in the cart but is refused on a persisted row, where the geometry belongs to the GROUP (`draftFromSofaGroup`) and a raw un-exploded build cannot round-trip at all. **Direction chosen deliberately:** the core lives in `order-line-edit.ts` and `cart.ts` imports it, not the reverse — that file's whole job already IS this question, `rental-cart.ts` (which the core needs for `isRentalLine`) imports nothing back, and it adds no file (Law 5). `LineEditTarget` is now an alias of `ConfigureSurface`, so all six call sites are untouched. **Where the entry was wrong: it said the refactor "does not fix the bug", only stops the third copy.** It does both — the persisted wrapper has to decide what to do with a core that now returns `"rental"`, and refusing it is the same guard the standalone fix would have added, in the one place both surfaces read. Original entry: **Law C, `a door, never a duplicate`. Opened 2026-08-07; the drift already produced `pencil-on-a-persisted-rental-line-replaces-a-signed-agreement` above.** `lineEditTarget(DraftLine, catalog)` (`cart.ts:20`) and `orderLineEditKind(OrderLine, catalog)` (`order-line-edit.ts:32`) both answer *"which configure surface re-opens this line?"* over two row shapes that both carry `sku` + `attrs`. They now disagree about rental, and they already disagreed about which markers block editing. **Firm fix: one core taking `{ sku, attrs }` and returning `"sofa_build" | "bed_mattress" | "rental" | null`, with two thin wrappers for what genuinely differs** — cart rejects `bundle_group`, persisted rejects the full `BLOCKED_MARKERS` set for server `line_not_editable` parity; and `attrs.sofa_build` means *"open SofaConfigurePage"* in the cart but *"no pencil, cannot round-trip the engine"* on a persisted row, so the core needs a `persisted: boolean` or the wrappers must own that branch. **Sequence this AFTER the rental guard, not instead of it** — a shared core returning `"rental"` still has to be rejected by the persisted wrapper until a rental-amendment route exists, so the refactor does not fix the bug. It stops the third copy.
- `sales-order-maintenance-left-a-dead-component-and-two-live-endpoints` — **found 2026-08-07. The feature was WITHDRAWN from the web, not renamed or rebuilt.** `apps/web/src/components/data-grid/DataGrid.tsx` (449 lines) plus its test have **zero importers** anywhere in the repo — the only external mention is a prose comment in `ReserveStockDialog.tsx:19`, and `check-design-standard.mjs:86`'s `<DataGrid` regex only scans `pages/**` so it never sees the file. The proof of withdrawal is `OperationApp.tsx:43`: the comment `// 0174 — Sales Order Maintenance (AutoCount-style configurable SO grid).` survives with **no import beneath it**. No `SalesOrderMaintenancePage.tsx` exists under `pages/**`. Meanwhile the API router is still mounted (`apps/api/src/index.ts:215`) serving `GET /grid`, `GET /config`, `PUT /config` — and `queries.ts` calls only `/entry-config`, the 0219 Order Entry field config, a **different** feature sharing the prefix. So: an orphaned component, an orphaned comment, and two live endpoints with no client. **Two separate decisions, deliberately not bundled.** (1) The web-side delete is safe and inventoriable — `formatCell` is unused outside its own test, and the shared `SoGrid*` types live in `packages/shared` and are consumed by the API, so they survive; it drops the web suite by 6 tests and `docs/ENGINEERING.md`'s baseline needs the new number. **Deleting files is Jess's call under red line 5 — do not take it as implied by this entry.** (2) The endpoints are a different question: an HTTP endpoint's caller set is not inventoriable from this repo's source. **Do NOT touch `check-design-standard.mjs`** — removing `<DataGrid` from `LIST_MARKER_RE` would weaken a `pages/**` guard to tidy a regex that is a pattern, not a reference.
- `create-po-numbering-has-no-lock` — **noticed 2026-08-07 while verifying the PO eta-update defect; deliberately kept separate so that fix stays reviewable.** `_operation_create_po_inner` (`0079_create_po_assigns_procurement_partner.sql:99-105`) computes `select coalesce(max((substring(id from 'PO-(\d+)$'))::int), 2030) into v_max_seq` and then inserts `PO-<v_max_seq + 1>`, with no advisory lock and no unique-per-sequence guard beyond the primary key. Two concurrent issues can read the same `max` and race; today only the PK saves it, and the loser gets an opaque duplicate-key error rather than a fresh number. Not observed in production and narrow while Purchasing is one desk. **Firm fix: an advisory lock on the sequence read, or a real sequence/identity column** — not a retry loop around the insert, which turns a data-integrity question into a latency question. Belongs to the Purchase Orders workstream.
- `pos-wordmark-is-a-no-op-at-step-1` — **found on the real POS 2026-08-06.** Clicking `CARRES` on `/principal?tab=pos` does nothing. **Measured cause:** the wordmark IS wired (`DealerPos.tsx:1014-1025`), but its handler is `if (submitted) startAnotherOrder(); else if (step !== 1) setStep(1);` — so when the operator is **not** submitted **and already on step 1**, no branch runs. The button is live, focusable and silent. Note the wizard's own wordmark (`ConfigureTopbarBrand.tsx:37-46`) is a *different* button that calls `onBack` and works, which is why the behaviour looks inconsistent between screens. **This is not purely a bug fix — it needs one IA decision first, and the decision is engineering's per §4, not Loo's:** does the wordmark mean *"top of this wizard"* (today's comment at `:1009-1013` says so) or *"leave POS, back to the portal"*? The operator expectation reported is the second. **Recommendation: keep the existing meaning inside the wizard and add the only missing branch — at step 1, unsubmitted, navigate out to `/principal`** — because a control that does nothing teaches the operator the logo is dead, and they then stop trying it on the screens where it does work. Settle it against `docs/ui/MASTER.md` and `docs/03-page-patterns.md` before writing the branch. **Do NOT make the logo always navigate out**: `:1011-1013` records a real reason for the reset-on-submitted path — *"jumping back with the old cart still loaded would invite an accidental duplicate order"* — which is the same disease as `rental-complete-order-has-no-in-flight-guard` above.
- `receiving-queue-model-architecture-review` — **OPEN, and it belongs to the RECEIVING workstream, not to Purchase Orders. Jess, 2026-08-03:** *"Keep the Receiving rail unchanged. Do not modify the Receiving module in this Purchase Orders workstream … We will review the Receiving queue model in the Receiving chat after Purchase Orders is complete."* **What was found** (measured on `origin/main` `963fc9a3`, which already carries PR #566 Slice B): `OperationReceiving.tsx` renders a rail of FOUR STATUS buckets — `Receiving issue` · `Partially received` · `In transit` · `Fully received` — computed by `poReceivingProgress`. **Jess's stated model is a QUEUE, not a status**: `To Receive` / `Received`, where `To Receive` lists POs and `Received` lists Receiving Sessions, and the workspace re-titles itself per queue. Her reason is extensibility — Draft · Submitted · Posted · Returned · Voided all fit under a queue model without ever adding a sixth tab. **Also ruled in the same message and already true of the merged code: there is NO `GRN` tab and none may be added** — GRN is a DOCUMENT of Receiving, the way the PO register is not a second module beside Purchase Orders. Verified: `PurchasingTabs.tsx` carries exactly five tabs (To Order · Purchase Orders · Receiving · Claims · Settings, the last manager-only) and the word `GRN` survives only in two comments in `OperationApp.tsx`. **ADDED 2026-08-03 by Jess, from the AutoCount study**: `Received 2 / 5` is a QUANTITY and cannot answer the question the operator actually has — is this running normally · did the supplier short-ship · is it coming in two lorries · is the rest simply not sent yet? A receiving STATE answers it and a fraction never can. Her candidate vocabulary, **for the review to consider, not to build now**: `To Receive` · `Receiving` · `Partially Received` · `Completed`. (AutoCount carries `Posted` / `Partial` on its own batch screen; that is a reference case, not a specification.) **Rule for whoever picks it up**: this is a rail/queue change on a page another chat shipped hours earlier — take it in the Receiving chat, on its own worktree, never as a side edit from a Purchasing sibling. **Do not treat the four status values as waste**: they stay, demoted from rail buckets to a listing filter or a workspace fact.
- ~~`payhold-blind-to-the-payment-ledger`~~ — **CLOSED 2026-07-27 by card C5 (PR #447).** `packages/shared/order-money.ts` is now the ONE outstanding rule and FOUR readers ask it: the ladder's 🔒, `bookingConfirmGate`, the Payments collections desk, and the order DRAWER — which the card did not list and which was the worst offender, telling an operator `RM 7,248 outstanding · HOLD DELIVERY` on an order `orders.paid` said was settled. `orders.paid` is the money truth; `ops_order_control.balance` is the fallback for imported rows and is read AS the outstanding (0165: "RM the customer still owes"), never as a total to net `paid` against — the booking gate had been doing exactly that. **Three premises corrected by measuring prod**: (1) `order_payments` is not writer-less, it is EMPTY — the drawer's Record-payment form and the raw-create door both write it, and the raw-create door writes the same deposit into `orders.paid` too, which is precisely why the two stores may never be summed; (2) `balance` means outstanding, not total; (3) no order leaves the Delivery board, because all 55 control rows are `booking_stage='none'` so the ladder never reaches the money rung. SO-1209's money gate passes; its booking is still refused for GOODS (0 units reserved), which is the goods half working. Original entry kept below.
  - _(was)_ `payhold-blind-to-the-payment-ledger` — **CORRECTED + RAISED TO HIGH 2026-07-27 after live verification; scheduled as card C5** (`orders/MASTER.md`). J3's finding was real but its diagnosis named the wrong table, and the wrong name would break whoever fixed it. Verified live: `ops_order_control.balance` is **NULL on all 55 control rows** (so the ladder's 🔒 has never fired — that half was right), but the money is **not** in a payment ledger: `order_payments` and `payments` both hold **0 rows**, and no payment RPC writes them — `top_up_order` and `record_stripe_checkout_payment` both write **`orders.paid`**, which is the only populated money figure. Real outstanding from that column: **18 orders, RM 56,859** (RM 52,209 on order lines alone; J3's RM 50,659 was the same arithmetic netted across orders instead of clamped per order). **The severity is higher than "a lock that never fires"**: `bookingConfirmGate` reads `collected` from the empty `order_payments`, so outstanding = full order value for every priced order — **SO-1209 (RM 7,248, `orders.paid` RM 7,248, paid in full) is refused a booking confirm today**. Firm fix (C5): one shared outstanding helper reading `orders.paid`, with `ops_order_control.balance` as the fallback for imported rows; three consumers (booking gate · ladder rung · Payments queue). **Do not point any reader at `order_payments`** — a table with no writer cannot become a source of truth by being read; starting to write it is a separate Payments card.
- ~~`disabled-user-service-role-routes`~~ — **CLOSED 2026-07-26 by migration 0267**: `custom_access_token_hook` now selects `... and status = 'active'`, so a disabled account is handed NO role claim and the middleware's existing `isRole()` check 401s it. No new claim, no middleware edit, no frontend change — the hook already had a "no row → return the event untouched" path and the middleware already rejected roleless tokens; one predicate joined them up. Measured before: samantha minted `{"role":"operation"}`. After: `{}`, while all 14 active accounts mint exactly what they minted before (row-by-row verdict OK) with every entity id intact. Residual (inherent to JWTs, accepted): an access token issued BEFORE the disable stays valid until it expires ~1h; the three layers that bound it are session revocation at disable time (now logged, not swallowed), RLS denial (0266), and no new role-bearing token (0267). Original entry: 0266 follow-up (2026-07-26): 0266 makes `disabled` real at the DATABASE layer (RLS + every `hr_*` DEFINER gate now deny). What it does NOT cover: routes that use `adminClient` (service_role) bypass RLS entirely, and their Hono role gates (`requireHr` / `requirePrincipal` / `requireOperation`) read `auth.role` **from the JWT**, not from `app_users`. So a disabled user still holding an unexpired access token could reach those routes — including account creation — until it expires (~1h). Narrow: it needs their session to have survived the disable, and 0266 + the now-logged `signOut` failure make that far less likely. Firm fix: add `status` to the JWT via `custom_access_token_hook` and reject non-active in `middleware/auth.ts` — zero per-request DB cost, stale by at most one token lifetime, and it matches the §8 Fix-1 "claims in the JWT, not per-row lookups" doctrine. Do NOT solve it with a per-request `app_users` read; that taxes every call to close a one-hour window. The hook is the highest-blast-radius function in the system, so that change wants its own pass and its own verification.
- ~~`hr-rpc-null-role-gate-shape`~~ — **CLOSED 2026-07-26 by migration 0266**: all 8 `hr_*` gates rewritten (programmatically, from `pg_get_functiondef`, so each body is otherwise byte-identical) to `coalesce((select public.app_role())::text,'') not in (…)`. Verified live: a disabled user calling `hr_team_source()` / `hr_commission_source()` now gets 42501 forbidden where they previously got data. Original entry: HR-O1 (2026-07-26, noticed while verifying 0265): every `hr_*` DEFINER function opens with `if (select public.app_role()) not in ('hr','principal') then raise exception 'forbidden'`. When `app_role()` returns **NULL** — an authenticated Supabase user with no `app_users` row, e.g. an orphan left behind if `auth.admin.createUser` succeeded but the `app_users` insert then failed — `NULL not in (...)` evaluates to NULL, the `if` is NOT taken, and the function proceeds. The gate is open-on-unknown where it should be closed. Not known to be exploitable today (accounts are minted through routes that roll back on failure, and every caller also passes Hono's `requireHr`), which is why this is MEDIUM rather than HIGH — but it is the wrong default for a permission check and it is copy-pasted across 0244/0245/0250/0252/0254/0259/0260/0265. Firm fix: one sweep replacing the predicate with `coalesce((select public.app_role())::text,'') not in ('hr','principal')`, applied to every `hr_*` / `org_*` DEFINER function in one migration, plus a test that a role-less JWT gets 403. Do it as its own pass — it touches too many functions to ride along with a feature.
- ~~`rental-billing-writes-need-rpc`~~ — **CLOSED 2026-07-26 by 0281.** `rental_record_payment` is now the ONLY writer of `rental_billings`: SECURITY DEFINER, computes the supplier/commission split server-side from the agreement's own snapshot rates (a caller never sends money figures), idempotent by `stripe_invoice_id`, stamps `recorded_by`, and writes a `rental_billing_events` history row on every call. Gated to finance/principal or the webhook's service_role JWT — deliberately narrower than `is_internal()`, which admits bd. The CASCADE half is decided too: an FK cannot cascade only the unpaid rows, so a BEFORE DELETE trigger makes a COLLECTED month undeletable by any path including cascade from the agreement, while unpaid rows stay deletable (re-anchoring needs that). Proven in a rolled-back prod transaction before apply. Original note kept below for history.
  - _(was)_ `rental-billing-writes-need-rpc` — Rental base (2026-07-25, PR #268 review): the living-side tables (`rental_agreements`/`rental_billings`) are blanket `is_internal()` FOR ALL — any internal JWT could direct-PostgREST money columns with no audit trail. Fine while DORMANT (zero write paths exist), but the BILLING ENGINE phase MUST put collection recording behind a SECURITY DEFINER RPC that computes the split server-side (`rentalMonthlySplit`), stamps `recorded_by` and writes a history row (guardrail #4 no-silent-money-edits). Also decide then: `rental_billings` ON DELETE CASCADE from agreements → RESTRICT or soft-cancel so collected-month records are undeletable.
- `order-payments-ledger-has-no-reader` — **C5 (2026-07-27), the seam C5's own premise did not account for.** C5 correctly made `orders.paid` the money truth and took every gate off the `order_payments` ledger. But the card assumed that ledger has no writer, and it has TWO: the drawer's Record-payment form (`POST /api/operation/orders/:id/payments`, with its receipt numbering and its principal-only void) and the raw-create door (`orders.ts`, which posts the at-creation deposit into the ledger AND into `orders.paid` — the double-write that makes summing the two stores unsafe). Consequence: **pressing Record payment now moves nothing.** The receipt prints, the row lands, and the outstanding, the 🔒 and the booking gate all stay exactly where they were. Bounded today — the ledger holds 0 rows, so that button has never once been used in production, and the live collection path is `top_up_order` → `orders.paid` — but it is a money button that lies, and it lies silently. **Firm fix (the Payments card): make the record-payment route the writer of `orders.paid` too** (increment inside a SECURITY DEFINER RPC, one transaction, audited — never two PostgREST writes), then the ledger becomes the history of a number that moves. Until then do NOT "fix" this by summing the ledger into the outstanding: the raw-create double-write would report a half-paid order as settled.
- ~~`row-dots-of-is-dead-code`~~ — **CLOSED 2026-07-27 by C10 (PR #471): revived, not deleted.**
  `rowDotsOf` now has a renderer — three 14px Lucide icons (goods `package` · delivery `truck`
  · money `wallet`) beside the stage pill in the Status column, each with its own tooltip.
  The closure is proved in BOTH directions on the downloaded bundles: the dot titles and the
  `row-dot-` testid grep **0** in `index-BYggEOBr.js` (the previous live bundle — they were
  tree-shaken) and **1** in `index-CToiHJof.js`. Two corrections to the entry below: the
  function was **never unit-tested** (C10 wrote the first cover — 11 tests, negative control
  included), and its tuple order was money-first per the retired §14 note, now goods ·
  delivery · money per Law 6. The original entry, for the record:
- `row-dots-of-is-dead-code` — **C5 (2026-07-27), found by grepping the shipped bundle.** `rowDotsOf` in `OperationOrdersControl.tsx` computes the three-dot row signal (money · goods · delivery) and is exported and unit-tested, but **nothing renders it**: the `dots` column shows a stage-word pill instead (`ORDER_COL_DEFS`, "not the old anonymous dots", Jess 2026-07-19). Proof: none of its title strings — `Money — settled`, `Stock — all in`, `Delivery — delivered` — appear anywhere in the live bundle. C5 updated its money branch for consistency and it changes nothing on screen. **Not deleted**: removing an exported function plus its tests is Jess's call (red line #5), and ACTION-FLOW-STANDARD Law 6 wants a three-dot column WITH per-dot icons, so this may be the skeleton C1 revives rather than dead weight. **Decide at C1** (which re-specifies that column): revive it with the icons, or delete it and its tests in one pass. Until then it is a function whose tests pass and whose output nobody sees.
- `order-money-keyed-balance-unreachable` — **C5 (2026-07-27).** The `keyed` branch of `orderMoney` (imported rows with no line prices falling back to `ops_order_control.balance`) has **zero live rows to exercise it**: `balance` is NULL on all 55 control rows, and all 19 priced orders take the `lines` branch. It is unit-tested and its semantics are argued from 0165's own wording, but it has never run against real data. Two judgement calls inside it that a first real row should be checked against: `paid` is NOT netted out of the keyed figure (nothing re-writes `balance` after a payment, so the human who keyed it maintains it — the Payments page's editable field is that door), and the order's Total is reported as `paid + balance`. If either reads wrong on the first imported order that carries a keyed balance, fix it in the ONE function.
- `supplier-portal-still-shows-lead-time` — **P1 (2026-07-28), found in the LIVE bundle after
  the deploy, not in the source.** The card counted five doors holding a "how long does this
  factory take" number and P1 shut all five, but `Lead time` still greps **1**: the survivor is
  the SUPPLIER PORTAL's own dashboard (`apps/web/src/pages/supplier/SupplierDashboard.tsx`),
  which shows a factory the free-text `suppliers.lead_time` we hold about it. It is read-only
  there, it feeds no engine, and no internal screen reads it any more — so it is not the
  "manager edits a number and nothing happens" trap. It is a different question: **do we show
  a factory our own private note about its lead time at all**, now that the number we actually
  plan on lives elsewhere and may not match it? That is Jess's call, not a build fix. Also
  worth knowing when it is answered: `Lead time` is a COPY-STANDARD do-not-use word, so
  whatever survives should not be spelt that way.
- ~~`purchasing-carres-work-week-still-hard-coded`~~ — **CLOSED the same day it was opened
  (Loo, 2026-07-28), and the answer was the opposite of the suspicion.** P1 reported the
  route's `offDays: [0, 6]` as a hard-coded literal contradicting "Mon–Sat everywhere" and
  left it alone rather than tidying it, because changing it moves every order-by date on the
  board. Loo ruled: **it is the OFFICE calendar and it has been correct all along** —
  arranging a delivery is office work, and there are THREE calendars, not one (Law 2A in
  `docs/ACTION-FLOW-STANDARD.md`: Office Mon–Fri · Warehouse Mon–Sat · Delivery Mon–Fri plus
  a reduced Saturday). The supplier's own week is a fourth and is meant to be, which is
  exactly why P1 moved it per-supplier. **What remains open is a different, larger thing and
  it has its own record in the index**: `working-days.ts` takes no calendar argument, so every
  caller counts on whichever week it was handed. Naming the calendar at every call site is a
  card, and nobody may "just switch the default".
- `purchasing-settings-changes-table-never-pruned` — **P1 (2026-07-28).**
  `purchasing_setting_changes` grows one row per edit forever and the loader reads the most
  recent 400 to find the last change per row. At a few edits a year that is a century of
  headroom, so nothing is built — recorded only so that whoever one day sees a settings
  screen missing a "was" line knows the cause is the 400-row window, not a lost row.
- `rental-dunning-has-no-send-channel` — segment 2b's ladder (Day 3 SMS, Day 7 WhatsApp, Day 21 warning) is unbuildable: there is NO message-sending integration anywhere in apps/api. `delivery_partners.whatsapp_group_url` is a stored link, not a sender. Loo (2026-07-26): skip for now, maybe a base standby later. Nothing should be designed around auto-messaging until a provider is picked.
- ~~`rental-payment-failed-not-recorded`~~ — **CLOSED 2026-07-27 by 0295** (PR #439). `invoice.payment_failed` has a webhook branch, `rental_billing_events.kind` gained `payment_failed`, and `rental_record_payment_failure` is its only writer (same gate as the paid RPC: `rental_can_approve() OR service_role`, narrower than `is_internal()` because a BD sells these). Three design points worth keeping: it does **not** touch `rental_billings` (0281's single-writer law, and `overdue` would be a second source of truth for lateness when the read already derives it); idempotency keys on Stripe's **event** id, not the invoice, because Smart Retries fire a genuinely new event per attempt and three refusals must be three rows; and a refusal with no matching instalment is still recorded against the agreement with a null billing rather than dropped. Surfaced on the instalment row (`Card declined` + date + the bank's own sentence), in the panel header, and — the part that makes it findable — as a badge on the agreements LIST, fed by the `security_invoker` view `rental_agreement_card_trouble`. Paying the month is what clears it; there is no flag to reset.
- `rental-payment-failed-event-not-subscribed` — **the rental collection engine has almost certainly never fired, and this is why.** The live endpoint (`https://api.carresofficial.com/stripe/webhook`, LIVE, Active) reports **“Listening to 3 events”** while the code handles **5**. The three are almost certainly the `checkout.session` trio from 0223; `invoice.paid` arrived with 0281 on 2026-07-26 and nothing in that worklog entry mentions returning to the Stripe dashboard. That reading matches the data exactly: `rental_billing_events` holds one row and it was entered by hand. **Both `invoice.paid` and `invoice.payment_failed` need ticking** — Stripe dashboard → the endpoint → Edit destination → the event list. No code. (The MCP key here is refused on `GetWebhookEndpoints`, so this cannot be checked or fixed from a session.) **Expect old-system traffic once `invoice.paid` is on**: the CARRESS Stripe account is shared with the old carressglobal system and those subscriptions are still collecting; our handler correctly answers `ignored: unknown_subscription`.
- `api-typecheck-hr-assign-salesperson-dead-import` — the api `tsc --noEmit` baseline moved **4 → 6** on 2026-07-27: PR #440 retired `hr_assign_salesperson` but left `apps/api/src/routes/hr.ts` importing `hrAssignSalespersonInput`, which `@carres/shared` no longer exports (TS2305 + TS6133). Verified on clean `origin/main`. **Harmless at runtime** — the import is unused so esbuild drops it, and `wrangler deploy --dry-run` bundles clean — but it hides the next real type error behind noise. Fix is deleting one line, and it belongs to that line, not to whoever trips over it next.
- `rental-two-calendar-implementations` — the due-date rule exists twice: `rentalDueDates()` in packages/shared and `rental_due_dates()` in SQL. The DB cannot import TypeScript and the POS cannot call SQL before an agreement exists, so one had to be mirrored. Both assert the SAME worked example (30 Aug 2026 -> 7 Sep -> 7 Oct -> ... -> 7 Jul 2033) in their own suites; change the rule and change both, or the tests fail.
- `rental-interest-daily-vs-whole-month` — **now load-bearing, since 0300 gave the arithmetic a caller.** 8%/month accrues PRO-RATA BY DAY. "8% per month" in Malaysian contracts is often read as "per month or part thereof", which is harsher. The gentler reading was chosen because it cannot over-charge; switching is `Math.ceil(days / 30)` inside `rentalLateInterest` **and the same change in the SQL mirror `rental_late_interest()`** — both assert the same worked examples, so changing one alone fails a test. Worth a word from Loo now that a button exists to charge it.
- `rental-interest-not-pushed-to-stripe` — 0300 RECORDS the penalty and does not bill it. The locked design (proposal §3) is a one-off Stripe invoice item on the next cycle, but **no live agreement has a Stripe subscription** (measured 0 of 1), so that path cannot be verified and shipping an unverifiable money path is worse than shipping the ledger half — 0281's own reasoning. Finance can see and chase the charge today. Fix when a real subscription exists: `stripe.invoiceItems.create` on the agreement's customer, keyed to the `interest_charged` event so a retry cannot double-bill.
- `rental-settlement-discount-undecided` — a settlement must equal the full remaining amount or it is refused (with the real figure in the message). Real early settlements are often discounted, and nobody has ruled how a discount spreads across N months and two payees — a guess would quietly short the supplier's 49%. Decide the rule with Loo before the first customer asks; the refusal is deliberate, not an oversight.
- `rental-buyout-pending-status-unused` — 0249 minted a `buyout_pending` status and 0300 does not use it: Loo's rule is that the customer signs the document FIRST, so a settlement is one act with a precondition, not a dwell state somebody has to remember to finish. The value stays in the CHECK; if a two-step (elect → pay) flow is ever wanted, it is there.
- `rental-ra1003-old-calendar` — RA-1003's 84 instalments still sit on the 26th (the pre-0281 anchor) and it has no Stripe subscription. Loo confirmed it is testimony data, not a real contract, so it was deliberately NOT re-anchored. New agreements get the correct calendar.
- ~~`rental-pos-config-projection`~~ — **CLOSED 2026-07-25 (sell lane, 0255)**: `rental_plans_pos` definer VIEW exposes id/sku/term/fee/package basics + `stripe_ready` for ACTIVE plans only; the pct columns are absent from the column list; anon revoked, authenticated SELECT. A guard test asserts the API response carries no split field.
- `rental-combo-agreement-sku-null` — Rental offers (2026-07-26, 0264): a `combo` rent line carries `rental_plans.sku = NULL` (a combo is a shape, not a sellable code), but `rental_agreements.sku` is still `text NOT NULL` and `create_rental_agreement` copies the plan's sku straight across. Signing a COMBO plan at the POS would therefore raise a NOT NULL violation. Harmless today (the sell lane only lists what the principal authored, and no combo plan exists yet), but the sell-lane segment MUST either snapshot a label (`{MODEL}#{combo label}`) or make the column nullable before a sofa combo goes on sale. Same review pass: the RPC also ignores `selected_options` / `one_off_total` / `gifts` (0264 snapshot columns) — the picked options and the money due once at signing are not yet frozen onto the agreement.
- `rental-offer-option-recompute` — Rental offers (2026-07-26, 0264): `quoteRental()` is the ONE pricing implementation (options + surcharges, one-time vs monthly) and the config previews use it, but nothing recomputes it server-side yet because the POS rental lane does not send picks. When the lane ships, the signup RPC/route must re-run `quoteRental` from FRESH config and reject `invalidPicks` (the sofa-P4 trust-gate doctrine) rather than trusting a client total.
- `rental-service-sku-price-drift` — Rental offers (2026-07-26, 0264): creating a service package mints its `SVC-…` product_sku at the package's standalone price, but a later package PATCH does not re-price that SKU (nor re-mint the code — a minted code is permanent by design). Until the sell lane reads the package price directly, a principal who edits a package price should also edit the SKU in SKU Master.
- ~~`rental-first-month-vs-billing-row`~~ — **CLOSED 2026-07-26 by 0281.** The signup month is now recorded when the rental checkout completes, keyed `cs:<session_id>` because that money arrives as a one-time line item and never has an invoice of its own. A re-delivered session is a no-op through the RPC's own idempotency. If the record fails the handler acknowledges with `warning: first_month_not_recorded` rather than 500ing — the subscription link already succeeded, and the instalment is recoverable from the finance screen. Original note kept below for history.
  - _(was)_ `rental-first-month-vs-billing-row` — Rental sell lane (2026-07-25, segment ①): the Stripe subscription checkout COLLECTS month 1 at completion, but `rental_billings` seq 1 stays `due` until segment ②'s `invoice.paid` engine lands (money marks are deliberately NOT written outside the future DEFINER RPC — guardrail #4). On the pilot, finance reads the truth in the Stripe dashboard; ②'s engine must idempotently catch up historical invoices (incl. `billing_reason='subscription_create'`) by `stripe_invoice_id`.
- `rental-billing-anchor-drift` — Rental sell lane (2026-07-25): `rental_billings.due_date` anchors on the agreement's `start_date`; Stripe anchors billing on CHECKOUT COMPLETION. A link paid days later (WhatsApp'd QR) drifts the two calendars by that many days. Segment ② reconciles by `stripe_invoice_id` (not by due-date matching); if the drift matters commercially, re-anchor the schedule at link time inside ②'s RPC.
- `rental-agreement-store-read` — Rental sell lane (2026-07-25): store JWTs still cannot READ rental_agreements (internal-only RLS by design); the sell lane holds the signup response in memory and the checkout routes load via service client + explicit Hono ownership checks (the 0223 sessions pattern). A "My rentals" store list needs a deliberate dealer-scoped RLS read (dealer_id = jwt dealer) in a later segment.
- ~~`rental-approve-without-signature`~~ — **CLOSED 2026-07-26 by 0279.** Signing is built: `create_rental_agreement` now takes the signature and stamps `signed_at`/`signed_name`/`signature_path`/`template_id`/`template_version` in the statement that creates the row, so an agreement is BORN signed or is not born. The four-line guard that sat commented in 0268 is live in `rental_approve_agreement` (`signed_at IS NULL` → 422 `not_signed`), it stranded nobody (0 pending applications when applied, asserted in the migration's own sanity block), and because 0275's `proceed_order` rental branch requires `active`, that one line also means an unsigned contract can never reach operations. api test added: an unsigned signup never reaches the RPC; a rolled-back prod assertion proved a refused approve writes zero billings. Original note kept below for history.
  - _(was)_ `rental-approve-without-signature` — Approver gate (2026-07-26, 0268): Loo's flow is signature FIRST, credit approval second, and `rental_approve_agreement` deliberately does NOT enforce it. Reason: **signing is not built**. 0267 shipped the wording, the version machine, the `rental-agreements` bucket and the five landing columns (`signed_at` / `signed_name` / `signed_nric` / `signature_path` / `signed_doc_path`), and NOTHING in apps/api, apps/web or any later migration writes one of them — so a `signed_at IS NULL` guard would have jammed the approval queue permanently shut on the day it shipped. Instead the approver SEES the state: `rental_pending_approvals()` returns the signature fields and the page shows a "Not signed yet" pill on every application. **Firm fix when the signing view lands**: uncomment the four-line guard already written into 0268's `rental_approve_agreement` body (it is there verbatim, with this slug next to it) and add an api test that a signature-less approve returns 422 `not_signed`. Until then the T&C's "no signature, no order" rule is enforced by the humans, not the system.

- ~~`rental-agreement-wording-unpublished`~~ — **CLOSED 2026-07-27, measured live**: `rental_agreement_templates` now holds version 1 (`Rental Agreement — Terms and Conditions (v5)`, `active`, effective 2026-07-26) and `rental_current_agreement_template()` returns `1`, so 0279 no longer refuses with `no_agreement_template`. Somebody pressed Save in Admin → Rental → Agreements, which was always the entire fix. Rental signup is not blocked. Original entry below for history.
  - _(was)_ `rental-agreement-wording-unpublished` — **this blocks every rental signup right now.** `rental_agreement_templates` holds ZERO rows on live prod, so `rental_current_agreement_template()` returns NULL and 0279 refuses the signup with `no_agreement_template`. That is correct behaviour, not a bug: a rent-to-own contract cannot be signed against a document that does not exist, and the alternative (signing against nothing) is what the whole migration exists to prevent. The POS shows a named blocker card above the signature pad and the Complete button is disabled, so the operator is told rather than left guessing. **Fix: press Save version 1 in Admin → Rental → Agreements.** PR #329 shipped that screen with Loo's own T&C (v5_260706) preloaded behind a button; nobody has pressed it since. No code change needed.
- `rental-signer-name-not-editable` — `createRentalAgreementInputSchema` and the RPC both accept a `signedName` different from the customer (a spouse or guardian signs), and the contract records whichever arrives — but `DealerPos` always sends `draft.customer.name`, because adding a field the operator must think about on every sale is friction for a rare case. Firm fix when a real case appears: one input on the rental confirm step, defaulting to the customer name.
- `rental-signed-doc-pdf-not-archived` — 0279 captures the SIGNATURE and the VERSION signed against, which is what makes it evidence. `signed_doc_path` (0267's column for the filled, rendered agreement PDF) still has no writer, so the private bucket holds the drawing and the contract can be re-rendered from `template_version` — but no single archived file is the artifact. Firm fix: render the filled template server-side on approval and write it to the same bucket.
- `rental-signed-nric-no-reveal-audit` — `signed_nric` is optional but PDPA-sensitive, and `rental_pending_approvals()` returns it in clear to every finance/principal caller. HR-P4 built reveal-with-audit for exactly this class of field. Firm fix: mask it in the queue payload and add a reveal route that writes an audit row, mirroring the People screen. Low today (zero real NRICs on file).
- ~~`rental-retired-page-still-on-disk`~~ — **CLOSED 2026-07-27 (PR #439): Loo said delete, so both files are gone.** `RentToOwnPage.tsx` (419 lines) and its test (136) removed, the `DealerPos` comment that pointed at them rewritten rather than left dangling, and the shipped bundle greps **0** for the page's own `pos-rental-page` testid. Side-lesson in the worklog: a `git stash -u`/`pop` round-trip **un-stages a deletion**, so `git ls-files` kept listing the file and `check-design-standard.mjs` crashed opening it — `git add -A`, not a checker bug. Original entry below for history.
  - _(was)_ `rental-retired-page-still-on-disk` — `apps/web/src/pages/dealer/pos/RentToOwnPage.tsx` has been unimported since rental became a POS category, and 0279 shut its submit from the inside (it now refuses with a message naming the live lane, and its tests pin that shut) rather than leaving an old door open behind a note — the care-plan lesson. But the file, its ~600 lines of duplicate signup UI and its test still exist and are compiled. Firm fix: delete both. Not done here because red line #5 forbids deleting files that were not asked for; it needs one word from Loo.
- ~~`rental-reject-does-not-cancel-the-order`~~ — **CLOSED by 0275 (2026-07-26); the index simply never caught up.** That migration made a rental mint a real Sales Order and taught reject to cancel it — the 07-26 entry says "closes CF" in its own text. Verified live 2026-07-27 from `pg_get_functiondef`: `rental_reject_agreement` both mentions cancel and touches `orders`. Nothing to do. Original entry below for history.
  - _(was)_ `rental-reject-does-not-cancel-the-order` — Approver gate (2026-07-26, 0268): the spec says "Reject → the order fails", but `rental_reject_agreement` only fails the AGREEMENT — it does not touch `orders`. That is currently a no-op distinction because the rental lane never mints an order at all (`rental_agreements.order_id` has no writer anywhere; a rental produces an `RA-` and never an `SO-`). Deliberately not papered over: cancelling an order from a rental RPC would reach into order state with live cascades (threads are ON DELETE CASCADE, POs, stock). **Firm fix when the lane starts creating orders**: have the reject RPC either call the existing cancel path in the same transaction, or refuse to reject while a linked order has already proceeded — and add the proceed-side guard (an order whose agreement is not `active` cannot reach operations) as a blocker inside `proceed_order`, matching the six existing `P0001` + detail-code blockers there.
- `rental-plan-reprice-policy` — Rental sell lane (2026-07-25): re-pricing a plan archives the old Stripe Price (new signups get the new fee) and existing SUBSCRIPTIONS keep charging their original price; an agreement signed at the old fee but not yet linked is BLOCKED from checkout (422 `plan_repriced` — re-sign on the current plan). Loo should know: mid-term re-pricing of live agreements is a segment-②+ policy decision, not supported today.
- `guarantee-claim-no-stock-movement` — Guarantee packages (2026-07-26, 0261-0263): `guarantee_claim` records the swap (status → claimed, `replacement_sku`, an `order_history` line, optional Service Case link) but does NOT move stock, reserve a unit, or raise a replacement order line. Ops issues the replacement mattress by hand today. Firm fix when volume justifies it: have the claim RPC optionally mint an `ops_stock_items` OUT movement (or a zero-priced replacement line on the original order) inside the same transaction, so the swap's COGS is captured rather than reconstructed from notes.
- `hr-comp-bd-revenue-unattributed` — HR-P7 (2026-07-26, 0278): the Business Development group's cost is shown but its revenue reads `not enrolled` rather than a number, because BD earns through the dealers it owns and **nobody is enrolled**: measured live, 0 dealers carry a `bd_owner_user_id`, `bd_profiles` is empty and `bd_commission_rates` is empty. Claiming RM 0 of revenue would read as failure rather than as "not started" — the O1 lesson about empty states that cannot be explained. Firm fix when Loo enrols a BD: BD group revenue = the attributed revenue of dealer-channel stores whose `bd_owner_user_id` is that person, which needs the dealer-channel slice that `hr_commission_source` deliberately excludes today (staff commission is showroom-only by the 0244 dealer-exclusion law) — so it wants its own read, not a widening of that RPC.
- `hr-comp-trend-single-month` — HR-P7 (2026-07-26): the ratified spec asked for a 6-month cost/revenue trend. Carres has **one month of orders** (2026-07-21..26, six days), so five of six bars would be empty and the line would read as a collapse. The strip renders what exists and fills in by itself, one month at a time — nothing needs building later. Related and more important: the cost/revenue RATIO is withheld entirely while a month is in progress (`monthInProgress`), because a full month of salary over a partial month of sales is directionally wrong, not merely imprecise. Both self-resolve; neither needs a switch.
- `hr-comp-no-prorate-by-join-date` — HR-P7 (2026-07-26, 0278): a person's full monthly salary counts toward a month even if they joined or left mid-month, because `hr_employees.join_date` is **0 of 9 filled** (P4 shipped the field; nobody has entered one). Pro-rating against a null date would silently under-count. Firm fix once join/exit dates are populated: pro-rate the loaded figure by employed days in the month, inside `computePeopleCost` where the rule stays testable — and note the exit side needs `exit_date` too, so the honest gate is "both dates present or count the full month".
- `hr-role-nobody-holds-it` — HR-P6 (2026-07-26, found while building the prod verification for 0276): **no `app_users` row has `role = 'hr'`.** Live roles are principal 1 · operation 6 (1 disabled) · finance 1 · bd 2 · dealer 1 · showroom 1 · partner 1 · supplier 2. Every `hr_*` gate admits `('hr','principal')`, so the entire HR portal — P1 Team, P2 permissions, O1 Overview, P4 People, P5 runs, P6 Performance — is reachable today by **principal@carres.com only**, which is also the account still on password `111` (see the two HIGH CFs). Not a bug in any migration: the gates are correct and the role exists in the enum since 0244. It does mean (a) nothing has ever exercised the `hr`-role branch of those gates against a real session, and (b) "give HR to a person who is not the Chairman" is an unstarted step, not a built one. Firm fix: when Loo appoints an HR person, mint them through the Team tab (THE account door) with role `hr`, then re-run one gated read as that session to confirm the non-principal branch. Until then treat every "HR can…" statement in the docs as "principal can…".
- `kpi-actuals-not-frozen` — HR-P6 (2026-07-26, 0276): the scoreboard's sold figure is computed from the month's attributed `order_lines` for **open and closed months alike**, never read from `commission_run_lines`. That is deliberate and the alternative is worse: `commission.ts` accumulates `basis` ONLY in its percentage branch, so a per-model store freezes `basis = 0` and reading it would print RM 0 sold — the same trap O1 hit with `report.totalBasis`. Consequence to know: the month lock (0272) stops re-attribution but NOT line edits, so editing an old order's lines changes what P6 says that month sold. Bounded on purpose — **P6 displays no commission at all**, so it can never contradict a frozen statement, and the figure it does show is the same arithmetic O1's SOLD tile uses. Firm fix if a frozen sales figure is ever wanted: add a `sold` (and `units`) column to `commission_run_lines` and snapshot it at close, then read it here when the run is approved. Do NOT reach for `basis`.
- `kpi-bonus-tiers-not-built` — HR-P6 (2026-07-26, 0276): the ratified spec's fourth table, `kpi_bonus_tiers` (attainment % → bonus RM, highest reached pays), is NOT built. Three reasons: the spec itself marked the money wiring "optional, off by default"; live `staff_commission_rates` still has **zero rows**, so this would be a second money path built before the first one has a single rate; and the semantics already exist in `model_commission_tiers`. Firm fix when Loo wants attainment to pay: build it as an ADJUSTMENT source feeding P5's existing `commission_adjustments` slot rather than a parallel engine — the run is the only thing that should ever produce a payable figure.
- `kpi-manual-metric-none-authored` — HR-P6 (2026-07-26, 0276): `kpi_manual_actuals` + `hr_set_manual_actual` + `PUT /api/hr/kpi/manual` all ship, but every metric in `KPI_METRICS` is `source: 'computed'`, so **nothing in the UI reaches the manual path** and no live row can exist. It is the declared escape hatch the spec's risk #4 protects (non-sales KPIs must not need a migration), and the shared engine's manual branch is unit-tested with a fixture. Firm fix to use it: add one entry to `KPI_METRICS` with `source: 'manual'`, widen the `kpi_key` CHECK in a new migration (the shared test `kpiKeysForMigration` fails until you do), and the target row grows an "actual" input generically.
- `kpi-target-history-not-frozen` — HR-P6 (2026-07-26, 0276): effective dating protects history against EDITS (a new `effective_from` never rewrites an earlier month), but `hr_delete_kpi_target` can remove a past row, which retroactively changes what an earlier month's attainment reads. Deliberate: without a delete, a target set on the wrong person is permanent and makes them look behind forever. It is audited (`KPI target REMOVED - …`) and targets are not money. Firm fix if attainment ever becomes a pay input: refuse to delete or re-date a target whose month has an approved `commission_run`, mirroring 0272's back-dated-rate trigger.
- `hr-hq-staff-no-birthday` — HR-P4 (2026-07-26, 0269): `hr_employees` deliberately does NOT store `dob`/`gender`. The ratified spec listed them, but `salespersons` already stores `birthday`+`gender` and has **four live write sites** (`lib/create-account.ts`, `routes/hr-team.ts`, `routes/staff.ts` create + patch), so copying them onto the employee row would re-create exactly the two-sources-of-truth drift 0269 exists to prevent, and repointing all four would have dragged the live POS staff profile into this phase. Consequence: floor staff read their DOB through the join like every other identity field; **HQ staff have nowhere to store one at all** (nobody has asked for it — no HQ birthday field has ever existed). Firm fix if Loo wants HQ birthdays: add `birthday`/`gender` to `app_users` so the concept has ONE shape per identity kind and People keeps reading through — NOT a copy on `hr_employees`. A later phase could unify both into `hr_employees`, but only WITH the four write-site repoint done properly.
- `guarantee-terms-no-admin-ui` — Guarantee packages (2026-07-26): `guarantee_terms` (covered category · coverage years · remedy · terms text) is authored by MIGRATION only. The PRICE is editable in SKU Master like any SKU, so a mismatch is possible: the principal can drop the RM150 without touching the 15-year promise. Firm fix: a small Guarantee card in the P&M Rental/Promo tab family (principal-only, mirroring how rental plans are authored) — the RLS write policy and the zod input/patch schemas already exist.
- `guarantee-attach-no-ui` — Guarantee packages (2026-07-26, downgraded same day): BOTH POS doors (sell in the cart, and add-to-existing-order via AddProductOverlay) now force the covered-item pick, and Loo confirmed the POS is the ONLY order entry going forward — so an UNATTACHED entitlement can realistically only come from hand-written SQL or a future non-POS door. The `guarantee_attach` RPC + `POST /api/guarantees/:id/attach` exist as the repair path and validate the category, but no button calls them yet; the desk shows "Not attached to an item" and the invoice falls back to "the mattress on this order". Firm fix if it ever fires: an "Attach to item" action on the desk row listing that order's eligible lines.
- `bd-network-board-page-cap` — BD POS (2026-07-19, PR #225): `BdOrdersBoard` fetches ONE `/api/orders` page (PostgREST page cap, guardrail #3) and computes the month summary cards client-side from it, so once the network outgrows a page the monthly numbers under-count (the ALL-TIME line is safe — it reads the uncapped `dealers_with_stats` RPC). Fine at today's ~190 orders; add a month-aware SQL aggregate endpoint (GROUP BY dealer) when volume grows.
- `orders-channel-filter-outlet-id-proxy` — Showrooms split (2026-07-19): `apps/api/src/routes/operation/orders.ts` still implements its public `channel=dealers|showrooms` filter as `orders.outlet_id IS NULL` / `IS NOT NULL`, a proxy from before dealers got auto-created default outlets (2026-05-22). It is now **inverted in practice** — 184 of 187 prod orders have a NULL `outlet_id`, so `channel=dealers` returns the showroom archive's orders and `channel=showrooms` returns almost nothing. **No frontend passes the param today** (verified — it is a dead query arg), which is why this was left alone rather than folded into PR #226's diff. Fix when it is ever wired to UI: filter on `orders.channel` (which the create RPC + AutoCount import both set) or join `dealers.channel`, and retire the `outlet_id` proxy. This was the THIRD independent “is it a showroom?” source; `dealers.channel` is now the authority.
- `principal-dealers-join-unbounded` — Showrooms split (2026-07-19): the `channel` + outlet roll-up on GET `/api/principal/dealers` reads `dealers(id, channel)` and `outlets(dealer_id)` with no `.limit()`, so PostgREST's server-side `max-rows` (1000 on hosted Supabase) would silently truncate and under-report `outletCount` once total outlets across all stores crosses it. Unreachable at today's volume (3 stores / 2 outlets) and the page already assumes <100 stores, but it is silent-wrong rather than an error. Firm fix: widen `dealers_with_stats_list`'s `RETURNS TABLE` to carry `channel` + a count (needs a migration), or use a `head:true, count:'exact'` aggregate per store.
- `bundle-pwp-gwp-stacking` — Bundle pricing (2026-07-19): bundle component lines still act as PWP-rule TRIGGERS and GWP gift triggers — with the live Cloud·Lumi→Kayu PWP rule, adding the King bundle mints a voucher for a SECOND discounted Kayu, and Cloud's 2-pillow GWP auto-appends; both stack ON TOP of the bundle discount. Intended mechanically (a trigger is a trigger), but a POLICY decision for Loo: deactivate the PWP rule if the bundle replaces it, or accept the stack as designed. (Bundle lines themselves can't RECEIVE a PWP/free-item claim — the cart hides both affordances on them.)
- `staff-order-mutation-scope` — Staff PIN login (2026-07-18): the order MUTATION routes (`/:id/lines`, `/proceed`, `/cancel`, `PATCH /:id`, `/top-up`, `/address`, `/date`) are NOT staff-tier-narrowed — only GET list / GET detail / POST create are. A salesperson-tier session that somehow knows another salesperson's order id (it can't SEE it — detail 404s) could still mutate it within the dealer. RLS still bounds everything to the dealer; consistent with Loo's "PIN = workflow, not security boundary" call. Tighten by applying the same `resolveStaffScope` gate to those routes if wanted.
- `staff-wizard-claim-preprovisioned` — Staff PIN login (2026-07-18): the setup wizard's identity step always CREATES a new staff row; a principal-PRE-provisioned PIN-less manager would end up duplicated (dedupe = deactivate the spare, or add a claim-existing flow to the wizard).
- `staff-salesperson-user-id-provisioning` — Staff PIN login (2026-07-18): principal account creation never writes `salespersons.user_id`, so a FUTURE salesperson-role email login starts unlinked → the orders routes treat it as dormant (works, but unscoped — sees the whole dealer like today) until the link is set by hand. The 2 existing links (sales@carres.com / sales-mk@carres.com) were seeded and work.
- `combo-cost-pos-bundle-exposure` — Phase 5 (2026-06-26): the new `combos.cost` + `sofa_combo_pricing.cost_by_height` are mapped unconditionally by `comboFromRow`/`sofaComboFromRow` into the GET `/api/catalog` bundle, which the dealer/showroom POS also fetches — so the principal-only cost benchmark + per-seat-height cost land in the POS network payload (devtools-readable), even though no POS UI renders them. **This exactly mirrors the pre-existing `product_skus.cost` read-exposure (accepted since 0175), not a new pattern**, and the feature is dormant (0 combos/sofa-combos in prod). No contract impact (cost never reaches an order_line/invoice/PO; the pure pricing engine ignores it). Defense-in-depth (one consistent fix): strip `cost` from each sku + `cost`/`cost_by_height` from each combo/sofaCombo in the bundle for non-internal JWT roles (dealer/showroom/partner/supplier) before `catalogResponseSchema.parse`, and make those schema fields `.optional()`.
- `delivery-followup-integrity` — Phase 6 (2026-06-26): the cross-ORDER delivery follow-up single-use backstop is a soft read of `order_addons.attrs->>cross_category_source_so` (NOT a DB unique constraint) — so it is (a) non-atomic (TOCTOU between the check and create_order), and (b) RLS-scoped, so the same source SO could be reused across different dealers. Also, same-customer identity is enforced only when BOTH orders carry a phone; a phone-less source (e.g. AutoCount-imported header) bypasses the identity check. All DORMANT (rates 0 → no discount applies until configured) + same-dealer-bounded by RLS. Firm fix when it goes live: a partial unique index over the link (a real `orders` column or `unique(order_addons.attrs->>cross_category_source_so) where addon_key='DELIVERY'`) + a name-based identity fallback (or reject an unverifiable phone-less source). The category-basis requirement (source+new genuinely span sofa×bed) WAS added in the review fix.
- `delivery-fee-semantics-notes` — Phase 6 (2026-06-26): three intentional semantics to remember once delivery rates go live: (1) **special-fee = max-wins** — a `special_delivery_fee_rules` standalone_fee can only RAISE the trip fee above the config base, never offer a rate BELOW it (2990s replaces-when->0, allowing lower; Carres deliberately never goes below base — a heavier special model shouldn't deliver cheaper). (2) `tripsCrossCategory` runs on the **charged-filtered** category set, so removing a category from `charged_categories` also disables the (already-inert in-order) cross trigger for it. (3) variant-scope special rules match on `product_skus.variant` == the rule's `sizeCodes` (sourced from `allowed_options.sizes`) case-insensitively — if the size-pool text drifts from the SKU variant text, a variant-scope rule silently never matches. Document for the principal; none is a live bug (dormant).
- `free-item-sofa-build-disallowed` — Phase 7 (2026-06-26): a "Make free" (free-item) campaign is **disallowed on a sofa-BUILD line** (one carrying `attrs.sofa_build`) — the server rejects such a claim (409 `free_item_not_eligible`) and the POS hides the affordance. Reason: free-item forces unitPrice 0, but the sofa recompute re-runs `computeSofaPrice` with an ABSOLUTE drift gate → forcing 0 would 422 `sofa_price_drift`. So a sofa can receive a Default Free GIFT (an appended RM0 accessory) but its own build line cannot be "made free". If a free sofa is ever wanted, move the free-item zeroing to AFTER the sofa explode + zero the exploded per-compartment lines (excluding them from the drift compare). Dormant (no campaigns authored).
- `free-gift-route-configured-test` — Phase 7 (2026-06-26): the POST `/api/orders` route tests cover only the anti-tamper strip + the ineligible-claim 409 (the shared orders mock can't return a configured campaign/gift). The configured happy paths (a valid free-item claim forces unitPrice 0 in the create_order payload; a resolved default gift line is APPENDED) are covered by the isolated `free-gift-resolve.test.ts` unit tests. Same mock-limitation precedent as the sofa/special-addons/delivery recompute route tests (§17.7). Add a configured-path POST integration case if the orders mock gains per-table support.
- `delivery-route-configured-test` — Phase 6 (2026-06-26): the POST `/api/orders` integration test only covers the DORMANT 0-addon delivery path (the shared orders test mock can't return a configured `delivery_fee_config`); the configured path (non-zero DELIVERY addon, cross-cat, follow-up) is covered by the isolated `delivery-fee-recompute.test.ts` unit tests. Same mock-limitation precedent as the sofa/special-addons recompute route tests (§17.7). Add a configured-path POST integration case if/when the orders mock gains config-returning support.
- `combo-component-price-availability` — Phase 4 (2026-06-20): the POS `skuPrice` lookup is built from the **pos_active** catalog bundle, so a combo component SKU that's `pos_active=false`/discontinued isn't in the bundle → it's priced 0 and the other components absorb the full combo price. The `explodeCombo` guard prevents NaN, and the combo **TOTAL still equals `comboPrice`** — only the per-line split skews. Defence: include combo-component SKU prices in the catalog GET, or have the Combos tab require pos_active components.
- `fabric-tier-db-lock-sofa-fabrics-tier` **[CLOSED 2026-06-21** — migration 0179 added the 0175-style `enforce_sofa_fabric_tier_principal_only` trigger on `sofa_fabrics`; `tier` changes are now DB-locked to the principal (NULL-role service/migration bypass). The original gap below is resolved.]** — Phase 3 (2026-06-20): a sofa fabric's `tier` (its price band) is gated to principal in the UI + (no API field gate) only; `sofa_fabrics` RLS stays `fabrics_write_internal`, so an internal non-principal hitting `PATCH /api/catalog/sofa-fabrics/:id` directly could re-tier a fabric. The tier-DELTA config + per-model overrides ARE 3-layer principal-locked (0176 RLS `is_principal()` + API 403 + UI); only the tier LABEL on the fabric row isn't DB-locked. Signed-off v1 (0 fabrics in prod today). Defense-in-depth: add a 0175-style trigger on `sofa_fabrics` rejecting non-principal `tier` changes.
- `catalog-server-null-supplier-po-guard` — the null-supplier (service/accessory) Create-PO guard is FE-only (explicit `SUPPLIERLESS_CATEGORIES` short-circuit in `findSupplierForSku` + orphan band, 2026-06-15). Defense-in-depth: also reject any PO line whose `product_skus.supplier_id IS NULL` server-side in `operation_create_po` / `apps/api/src/routes/operation/pos.ts`, so a bypassed UI can't slip one through.
- `phase-10-supplier-pos-list-urgency-blank` — supplier PO LIST embed silently null because orders has no supplier read policy post-0111. Refactor list enrichment to SECURITY DEFINER RPC, OR add non-recursive supplier orders policy. Drawer works (uses RPC).
- `phase-10-abandon-cascade-to-po` — `operation_abandon_order` doesn't cascade to linked POs. PO may waste supplier production capacity.
- `phase-10-cogs-real-source` — `finance_monthly_pl` 55% revenue placeholder. Real: `sum(purchase_order_lines.cost * received_qty)` post-Phase-6.
- `phase-10-opex-real-source` — `finance_monthly_pl` constant RM 42k/month placeholder. Need opex schema (rent/payroll/ops).
- `phase-10-csv-bulk-import` — bank_statements manual-only. Maybank2u 5-col CSV format locked (Q3=B), deferred.
- `phase-10-partner-pickup-rpc-regression-guard` — 2 prior SOP-CASE regressions (0117 + 0118) in 2 days. Add regression test asserting CASE is in pg_get_functiondef.
- `phase-10-operation-to-thread-cascade-audit` — Mostly closed in 0129; spot-check remaining candidates (`operation_cancel_po`, `operation_warehouse_pick` — closed; `operation_revert_*` — verified safe).
- `phase-10-incoming-server-side` — `SupplierIncoming` V1 uses open-PO demand. Proto semantics: pending sales orders filtered by `supplier.cat_covered`. Dedicated `/api/supplier/incoming` RPC needed if precision matters.
- `phase-10-customer-pod-upload` — Separate POD bucket + flow distinct from supplier DO uploads.
- `phase-10-test-lp-confirm-flow` — Master data only has Carres Klang (own WH). To exercise `ready_confirm_sent` path (Nets sees "Awaiting Accept"), need LP-owned WH. Either add new WH or change Carres Klang's `owning_partner_id` to Nets.
- `phase-9-pdf-storage-persistence` — Phase 5 invoice PDF renders on-demand; 7-year tax retention needs Storage bucket + signed URLs.
- `phase-10-historical-doc-vocab-mismatch` — 600+ "logistics" refs in plan/spec docs describe "Phase 4 = the Logistics phase" while live role is "Operations". Glossary footnote could help onboarding.
- `phase-10-bundle-size-regression` — Web build jumped 1064 → 2620 KiB raw (266 → 781 gzipped) between 0121 and 0123. Investigate tree-shaking impact.
- `phase-10-issuance-groups-test-coverage` — `CreatePOModal.test.tsx` lacks end-to-end per-(sourceSo, sku, attrs) assertion. Add bundle-prefill test with `issuanceGroups.length === N`.
- `phase-10-shortage-by-so-test-coverage` — Phase 3 server endpoint has 1 bySo test. Add 3-SO disjoint-SKUs fan-out + stockpile vs bundle scope.
- `phase-10-multi-leg-pod-upload` — frontend Storage upload + bucket RLS for `delivery-orders/orders/<id>/legs/<n>/<ts>.<ext>` paths so partners can attach a POD photo per handoff. Each `delivery_stops` leg already carries `pod_url` / `pod_signed_by` / `status` cols (0156); needs RLS verification + UI wiring before frontend uploads via supabase-js. (From 2026-06-05 multi-leg ship — POD intentionally deferred to V2.)
- `phase-10-smart-partner-suggest` — multi-leg "+ Add leg" form + Inbox partner dropdown should auto-recommend a partner from the customer's delivery state (Klang→NETS, Johor→TEOW/TT, Singapore→EU/SSY). Operation currently picks manually.
- `add-lines-pwp-one-promo-policy` — Add-product P2 (2026-07-18): only ONE promo application per order via add (a second → 409 `pwp_add_conflict`; existing PWP markers on the order are stripped to context lines, never re-validated as fresh claims); voucher-CODED claims are wizard-only (add → 409 `pwp_voucher_add_not_supported`). A since-deactivated rule on existing claimed lines no longer blocks adds (markers stripped), BUT a rule deactivation between a P3 submit and its approve still 409s the approve (approval re-prices everything fresh).
- `add-lines-delivery-rederive` — Add-product P2 (2026-07-18): an add re-derives the delivery fee from the CURRENT `delivery_fee_config` (DORMANT today, rates 0), recovering the operator inputs from the persisted DELIVERY*/DELIVERY_ADD rows. Consequence once rates go live: a config change between create and the first add changes the order's fee, and a raw (fee-less) order can GAIN a delivery fee on its first add. `excludeOrderId` spares the order's own cross-link from the single-use follow-up backstop.
- `change-request-midproduction-approve` — Add-product P3 (2026-07-18): APPROVE of a proceed-lane change request is allowed while `operation_stage` is in_production/dispatched — the appended lines enter the ops flows as NEW shortages. By design (operation IS the approver), but worth remembering when a mid-production add lands.
- `topup-method-ledger-check-mismatch` — POS entry fixes (2026-07-18): the `order_payments` (0193) ledger CHECK still whitelists only cash/bank/card/cheque/online/other, while manual-payment panels now render arbitrary active `order_entry_config` keys. Today `top_up_order` writes `orders.paid` + history only (no ledger row), so a config key never hits that CHECK — but ALIGN the CHECK (or map config→canonical) BEFORE any config-keyed method is ever routed into the 0193 ledger.

- `hold-release-does-not-close-the-po` — Receiving R4 (2026-07-27, 0299): `ops_stock_resolve_hold(..., 'back_to_stock')` frees the quarantined unit into sellable stock and writes its `stock_movements` row, but it does NOT raise the PO line's `received_qty`. So a line ordered 10 that received 8 and kept 2 repaired-on-our-floor units stays at `received 8 / 2 pending delivery`, and the PO stays `open` forever unless the supplier actually delivers a replacement. **Deliberate, and the reasoning is load-bearing**: `received_qty` means "good units this supplier delivered against this line", a unit we quarantined and then decided to keep was not delivered good, `damaged_qty` records that (R1's accumulate law), and whether the supplier is still on the hook is settled by the CLAIM's ask/answer pair — not by a stock move. It is also **pre-existing R1 shape, not new**: only `operation_receive_po_with_do` has ever closed a PO, and duplicating its close + its thread-advance + its reserve loop inside a stock RPC would put the two copies one edit apart from disagreeing. Consequence for **R5**: "on-time %" and "partial-delivery rate" must be computed from the claim's `closed_at` / `supplier_response`, never from `purchase_orders.status`, or a made-good PO reads as permanently late. Firm fix if it ever matters: give the receive RPC a `p_from_hold_release` entry point so there stays exactly ONE function that closes a PO.
- `claims-facet-counts-a-truncated-page` — found by P2-Claims (2026-07-28, PR #494). The Claims facet rail counts the rows it was HANDED, and the API hands it `DEFAULT_LIMIT = 200`; the `Open / Closed / All` chips above it come from a separate unfiltered head-count. **Past 200 claims the two disagree on the same screen** — a facet saying 180 beside a chip saying 340, with no way for the reader to know which is lying. Invisible today (**0 claims exist**) and it does not degrade gracefully: it gets quietly wrong rather than visibly empty. Fix = server-side facet counts, in the same call that already produces the head-count. **Not "raise the limit"** — that moves the number at which it starts lying and costs a bigger page to do it. Same shape as `bd-network-board-page-cap` and `principal-dealers-join-unbounded`; if a third one appears, the pattern is worth a card of its own rather than three fixes.
- `supplier-claim-comment-says-the-verb-table-has-four` — `packages/shared/src/schemas/supplier-claim.ts` comments that `Close` is "the only verb outside the four-verb dictionary … FLAGGED for Jess". The table has SIX verbs and `Close` is one of them; the flag was answered long ago. **The code is correct and the comment is what will mislead** — a chat reading it either re-raises a settled question or treats a legal verb as an exception. Fix in place whenever anything opens that file. Recorded beside `code-comments-point-at-deleted-docs` because it is the same disease: a comment that outlived the thing it described.
- `code-comments-point-at-deleted-docs` — found by D0.4 (2026-07-28) while killing the old master spec's inbound pointers, and left because they are CODE comments in two lanes that were both busy. Four sites, two dead targets: `apps/web/src/lib/wa-templates.ts:3` and `OrderDetailDrawer.tsx:1871` both cite **`docs/whatsapp-chase-templates.md`**, one of the seven purchasing documents deleted 2026-07-27; `packages/shared/src/schemas/ops-order-control.ts:356` and its test cite **"UI-KIT §7.5"**, a section that stopped existing in the 2026-07-27 kit rewrite. Harmless to run and expensive to read: a chat that follows either pointer finds nothing and concludes the rule behind it was never written — which is exactly how the storage rate got mis-stated in two files for a month. Fix in place whenever anything else opens those files; repoint the WhatsApp pair at `orders/MASTER.md` §3 "What we send" (where D0.4 put the rule) and the storage pair at `orders/MASTER.md` §3's outstanding formula. **Do not open a card just for this** — it is a comment, and a card that only edits comments will keep losing to real work.
- `ready-stock-plan-review-has-no-ruled-words` — Ready Stock K2, surfaced 2026-07-28 while the receiving line was retiring `Send back`. `OperationStockPlan.tsx:681` renders **`Send back`** on the plan review, and `Send` is pinned by COPY-STANDARD to raising a purchase order to a factory — so the word is wrong there too. **It is deliberately NOT fixed by R8** (Loo, 2026-07-28): one business, one dictionary. It is the same SHAPE as the warehouse count's return and a different OBJECT — a proposal about stock, not a count of goods — and Ready Stock has no working-flow file and no dictionary rows of its own, so any rename now would be a receiving chat choosing words on behalf of a business line that has not spoken. Line ⑤ is marked COMPLETE, which is exactly why this is written down: there is no open card that would trip over it. **The sequence when it is picked up:** Ready Stock writes `docs/READY-STOCK-WORKING-FLOW.md` → the flow names this action → the action gets its five strings in COPY-STANDARD → only then does the screen change. Until then the word stays as it is, on purpose, and a chat that "tidies" it has broken the rule rather than followed it. Note also that `Return` is available as the verb (it was added to the table on the same day) — but the OBJECT and the party are Ready Stock's to name, and that is the part nobody may guess.
- `warehouse-receipt-orphaned-by-a-po-relocation` — Receiving R6 (2026-07-28, PR #490), reported by the build chat rather than found later. A warehouse receipt SNAPSHOTS the warehouse it was filed at, which is right — the count happened somewhere. But the warehouse's Incoming list is built from the PO's CURRENT warehouse, so **relocating a PO to a different warehouse after a count has been filed and before Carres reviews it leaves an open receipt attached to a PO that has vanished from that warehouse's screen.** The staff member who counted the goods can no longer see the thing they counted, and the ops review panel still shows it waiting. Bounded today to the point of being unreachable: **one warehouse exists** (`Carres Klang`) and no warehouse account has ever logged in, so there is nowhere to relocate a PO to. It stops being unreachable the day a second warehouse is real — which R6's own card anticipates ("partner-warehouse SSY/EU logins after Klang proves R6"). Firm fix: the Incoming list should union the PO's current warehouse with any warehouse holding an OPEN receipt for that PO, so a filed count keeps its own screen until it is reviewed; alternatively refuse a relocation while an unreviewed receipt exists, which is the stricter and probably more honest rule — a PO whose goods somebody has physically counted is not free to move. Do NOT fix it by dropping the snapshot: the warehouse a count happened at is a fact, not a pointer.
- ✅ **CLOSED BY RULING 2026-08-05 (Loo, card R9) — not by building the fix below, and the difference matters.** He ruled option A: **a claim holds ONE outcome, and two outcomes are two claims** (AutoCount is already A — `Cancel Purchase Order` / `Goods Return` / `Purchase Return` are three documents, and *"换 2 退 1"* is two of them). So *"supplier took 2 back, we scrapped 1"* is **two claims**, reached by a **split button** on the claim screen, and `ops_stock_resolve_hold` moving every unit of ONE claim to ONE outcome is **correct behaviour rather than a limitation**. **The `p_item_ids` narrowing proposed below is therefore NOT the fix and must not be built** — it would put a second outcome back inside one claim, which is the model Loo rejected. Kept rather than deleted so the next chat meeting this shape reads the ruling instead of re-proposing the patch. Original entry, for the reasoning it still carries:
- `hold-resolution-is-per-claim-not-per-unit` — Receiving R4 (2026-07-27, 0299): `ops_stock_resolve_hold` moves EVERY unit still held under the claim to one outcome. A claim covering 3 damaged units where the supplier takes 2 back and lets us scrap 1 cannot be recorded as it happened — the operator must pick one word for all three. Deliberate: the units of one claim are one problem, one supplier answer and one physical decision, and the per-unit picker UI would be built for a case nobody has met (0 claims exist). Firm fix when a real split appears: the RPC already resolves by predicate, so add an optional `p_item_ids uuid[]` narrowing the same `where`, and give the panel checkboxes — no schema change, and the audit/po_history lines already name a count rather than assuming "all".

- `action-step-cannot-open-its-form` — Portal Core C6 (2026-07-28, PR #486): a checklist step
  NAMES the button that records it (`Confirm booking`, `Record ready date`) and cannot press
  it — the operator reads the word and then has to find that button themselves in the drawer.
  Deliberate at ship time and ruled "not now" by Jess the same day: the card's "where a form
  already collects the inputs, the form IS the checklist" says *do not duplicate the form as
  a row of ticks*, it does not ask for navigation, and C2's action list is control-free by
  design. The PATTERN already exists — `CallsRow.onOpen` opens "the tab where this is
  worked" — so the firm fix is one `onOpen` per step key mapped to the panel that owns it
  (supplier → Items · logistics → Delivery · money → Balance · photo → Documents), passed
  down from `OrderDetailDrawer` exactly as `CallsPanel` receives its own. It stays a
  disclosure, never a writer. Left for whoever next opens `OrderDetailDrawer.tsx` (7,000
  lines) for its own reason — a navigation pass through that file is not a side effect worth
  taking on its own.

- `a-promise-is-never-moved-silently-has-no-door` — **found 2026-08-12, re-measured 2026-08-14.** `0326_a_promise_is_never_moved_silently.sql` defines `operation_request_order_change` and grants it. **It has zero callers:** `grep -rl "operation_request_order_change" apps packages` returns **0** files — the only three hits repo-wide are the definition, the revoke and the grant inside the migration itself. Meanwhile `delivery_date` is still in the plain-save whitelist (`0340_a_change_names_who_asked_for_it.sql:100`), **so a promised delivery date can still be moved without asking anyone — the exact act the migration is named after.** The engine is written, reviewed and applied; only the route and the operator surface are missing. Cheapest open architecture item measured this week: no new SQL, no new decision, and the migration header already states the intended behaviour. **Do NOT close this by deleting the RPC** — the ruling behind it stands; what is missing is its door.
- `orders-master-says-card-10-is-both-built-and-not-built` — **found 2026-08-12 by reading the MASTER against the code; Law 3 violation inside one file.** `docs/orders/MASTER.md` contains **7** occurrences of "not built"/"NOT BUILT". Two of them are about Card 10: a `## Status — TARGET is not BUILT` heading and a `## Card 10 · My Work / Team Work — approved target, not built` heading. The same file also carries `COMPLETE`, `**ALL TEN CARDS ARE COMPLETE**` and the full shipped record — and the code exists (`apps/web/src/pages/operation/OperationWork.tsx`, mounted `OperationApp.tsx`). `git blame` puts the two "not built" lines in the blueprint commit that predates the build; they were simply never overwritten when Card 10 landed. **The MASTER OVERWRITE LAW forbids exactly this shape** — obsolete text left beside current truth. **The wider pattern is the part worth fixing:** the ENGINE half of Sales Order V2 (Cards 1–10) matches its documentation, while the register/toolbar/footer half has documentation that ran AHEAD of code — a docs-only commit on 2026-08-11 wrote approved-but-unbuilt UI (`Scan`, an `…` overflow, a selection row that replaces the toolbar, a status footer carrying counts) into blocks marked ✅ shipped. So the MASTER currently cannot tell a reader which of the two halves they are in. **Whoever holds the Orders lane owns this**; it is a documentation repair, not a build.

**LOW**:
- ~~`an-old-revision-opens-with-typable-fields`~~ — **CLOSED as a carry-forward 2026-08-27, the same day it was opened: it became `docs/cards/CARD-2026-08-27-sales-order-old-revision-fields-lock.md`.** A carry-forward is a risk nobody has scoped; a QUEUED Card is scoped work awaiting owner review. Keeping both would give one finding two homes and two states, which is exactly the duplication Law 3 exists to stop. The Card carries the measurement, the firm fix and the falsifier; this line survives only so a reader who knew the old key can find the new one.
- `receiving-progress-rail-word-outlives-the-goods-receipts-rename` — **found 2026-08-20 during the CARD-2026-08-20-purchasing-sidebar-groups owner walk, recorded rather than fixed because that Card may not touch a page body.** The rail and the Destination Header now say `Goods Receipts` (`portal-nav.ts`, `PurchasingTabs.tsx` `PAGE_WORD`), but the page body still prints `Receiving Progress` as a facet heading in its own left rail — measured live at `/operation?tab=receiving`, text node under `NAV[receiving-rail]`. **`Receiving Summary` and `Start Receiving` are NOT part of this defect**: they are approved workspace/action words in `docs/COPY-STANDARD.md` and stay. Only the facet heading is the stray: a queue rail labelled with the OLD destination word tells the operator they are somewhere other than where the header says they are. Scope when someone next opens the Receiving page body: rename that one heading to the governed word and check no sibling facet inherited it. Bounded and cosmetic — no route, key, API or business rule is involved, and nothing is mis-filed because of it.
- `hold-entry-only-from-incoming` — Receiving R4 (2026-07-27, 0299): the guard trigger allows `incoming → on_hold` and no other entry, so a unit already in the FREE pool that is later discovered damaged cannot be quarantined — it goes through `needs_repair` + condition (the Defective view) instead. Deliberate: two quarantine concepts on one table would give the warehouse two ways to say one thing, and worse, the refurbish path (`/refurbish-complete`, a direct PostgREST update gated only on `needs_repair`) would hand a "held" unit straight back to the pool with no claim ever answered. Consequence to remember: **a supplier claim can only ever be raised at receiving**, which is exactly R2's law, but it also means a latent factory fault found a week later has no supplier-claim route at all — that is a service-case (S-line) shape today. If Jess ever wants a post-receipt supplier claim, the entry rule and the refurbish door must be settled in the SAME change.
- `pool-usage-release-does-not-reverse` — Ready Stock K4 (2026-07-27, 0292): releasing a reserved unit back to free does NOT remove or negate its `ops_stock_pool_usage` row, so a month's split can exceed what net-left the warehouse. Deliberate, and the table's own comment says so: the ledger answers "what did we draw on ready stock FOR", and a release does not unmake the decision that was made. Bounded — `ops_stock_release` has never been called on prod (0 reserved rows at ship time). If the split ever needs to be a net figure, add a `released_at` stamp written by `ops_stock_release` and let `summarisePoolUsage` take a mode, rather than deleting rows: a deleted draw is a decision nobody can audit.
- `pool-usage-bulk-record-is-all-or-nothing` — Ready Stock K4 (2026-07-27): the register flips a WHOLE record, so reserving the 555-unit pillow row for an order that needs one pillow takes all 555 out of free stock, and the ledger honestly records 555. That is a pre-existing property of the per-unit engine (0218 bulk rows), not something K4 introduced, and the card is explicitly told not to rebuild reservation. Consequence: a bulk draw dominates the month's split. Firm fix belongs to the stock engine — split a bulk record on partial reserve (decrement `qty`, mint a reserved sibling) — and every consumer of `ops_stock_items.qty` must be re-read when it happens.
- `stock-movements-qty-hardcoded-1` — noticed while reading `ops_stock_takeout` for K4 (2026-07-27): the `stock_movements` row it writes is `qty 1` regardless of the record's real `qty`, so taking out the 555-unit pillow record logs one unit out. Pre-dates K4 (0137 wrote it before the 0218 bulk column existed) and K4 deliberately did not change it — the movements ledger feeds `ops_rollup_stock_balances`, which is itself `count(*)`-based (K1's note), so fixing one half alone would make the two disagree differently. Fix both together, or neither.
- `stock-reserve-reason-column-orphaned` — Ready Stock K4 (2026-07-27, 0292): `ops_stock_items.reserve_reason` (0213, values `urgent`/`exchange`) now has NO writer — the reason lives in `ops_stock_pool_usage`, which is dated, attributed and survives the unit. The column holds 0 rows, is still returned by the API's `shape()` and is read by nothing. Left in place on purpose (dropping a column is Loo's call, red line #5); when it goes, drop the column, its CHECK, the `reserveReason` field in `apps/api/src/routes/ops/stock.ts` and the reference in `scripts/cleanup-autocount-orders.sql`.
- `case-evidence-abandoned-draft-orphans` — Service Case S2 (2026-07-27, 0289): the wizard uploads evidence against a client-minted `draftId` BEFORE the case exists (it has to — the case cannot be created without the files). If the operator then closes the wizard, the objects stay in the private `service-case-evidence` bucket under `draft/{id}/` with nothing referencing them, and no `service_cases` row can ever name them. Bounded and cheap: the bucket is private, 25 MB per object, and the volume is one abandoned complaint at a time — but it grows monotonically. Firm fix when it matters: a cron sweep deleting `draft/` prefixes older than 7 days (the only paths that can be orphaned; a filed case's paths live under `draft/` too, so the sweep must exclude any path present in some case's `evidence` — cheaper alternative is to have the create MOVE the objects to `case/{id}/`, which then makes the prefix itself the liveness test). Deliberately not built now: a mover adds a failure mode between "bytes uploaded" and "case filed", which is the one place S2 must not become flaky.
- `case-evidence-prose-path-ungated` — Service Case S2 (2026-07-27): the evidence gate binds to `issueType`, not to the endpoint — a `POST /api/ops/service-cases` with no issue type files a case with no evidence demanded. That is deliberate: the edit modal still writes prose-only cases and S1 explicitly kept that path alive, and the one real case on file (SC2607-01) predates the wizard entirely. The database backs the same shape (0289's `sc_evidence_required_with_issue` only fires where `issue_type is not null`). Consequence to remember: the wizard is the only door that demands photos, so a future non-wizard create door inherits no gate unless it names an issue type. Second, smaller edge: PATCHing an issue type ONTO a legacy evidence-less case now violates that CHECK and would surface as a 500. No live path does this (`ServiceCaseModal` never sends intake fields — it shows them read-only), which is why it is LOW; if an edit path ever needs to set an issue type, it must demand the checklist in the same call.
- `hr-source-builds-a-dead-unattributed-array` — attribution retired (2026-07-27, PR #435 + 0296/0297): `hr_commission_source` still builds an `unattributed` jsonb array — one object per salesperson-less showroom order, each carrying a correlated `sum(qty * unit_price)` over `order_lines`. **Nothing reads it.** PR #435 deleted the worklist that consumed it, 0297 stopped `GET /api/hr/report` forwarding it, and `HrUnattributedOrder` is gone from the web types, so the key dies at the Worker. It is also **empty by construction**: 0296's `orders_salesperson_required` refuses to write a native order without a salesperson, and the array's own 0265 filter already excludes the imported archive — so it evaluates to `'[]'` on every call. Left in place on purpose, and this is the whole trade: the same function's `legacyUnattributed` sibling IS still read (the Overview archive footnote, the only line reconciling HR against the orders list — 56 orders on file, 19 counted), so the concept cannot leave the RPC wholesale; and the function is **6,647 characters and feeds every HR screen** (Overview, Commission, Performance, People cost all read one call), so a full CREATE OR REPLACE to delete a key that computes nothing is a worse risk than the dead weight. **Firm fix**: next time that function is opened for a real reason, drop the `'unattributed'` key from its `jsonb_build_object` and the subquery feeding it, keep `legacyUnattributed`, and assert on the QUOTED key `'unattributed'` — a bare-word assertion matches the migration's own comment about the removal (0296 lesson 9). **Watch item**: the array is only empty while the constraint holds; loosen `orders_salesperson_required` and it silently starts returning rows again that no screen will show.
- `guarantee-pos-one-per-add` — Guarantee packages (2026-07-26): the POS covered-item picker adds ONE unit per tap (ruling #2 is 1:1), so a customer buying two mattresses taps the guarantee card twice. Deliberate — merging into a qty-2 line would silently cover the same mattress twice. If the flow ever feels slow, add a per-candidate quantity to the picker rather than letting the cart merge.
- `guarantee-expiry-derived-not-stored` — Guarantee packages (2026-07-26): an entitlement past `expires_on` keeps `status='active'` in the table; "expired" exists only in `effectiveGuaranteeStatus()`. Every app surface reads the derived value, but ANY raw-SQL report / export must apply `expires_on < current_date` itself or it will over-count live cover.
- `rental-sku-delete-23503-500` — Rental base (2026-07-25): `rental_plans.sku` → product_skus is ON DELETE RESTRICT; once a plan is authored, the EXISTING catalog SKU hard-delete funnels the 23503 through `mapPgError` (no 23503 case) → raw 500 instead of a friendly 409. Same latent class as 0177 combo_components. Add a 23503→409 branch to mapPgError (or pre-check) when it first bites.
- `bd-inquiries-ui-retired` — BD POS (2026-07-19, PR #225): the Phase-8 BD Inquiries PAGE died with the ERP-style BD portal; `/api/bd/inquiries` + the `bd_inquiries` table stay live but UI-orphaned. Build a POS-style inquiries panel if Loo ever wants the pipeline back; delete the API + table if he confirms it's dead.
- `sofa-regroup-invoice-pdf-itemized` — Sofa customer regroup (2026-07-19, PR #223): the finance/ops INVOICE PDF (internal/tax doc; dealer-denied route) still itemizes a built sofa's per-compartment lines — the customer receipt / SO PDF / POS detail now regroup to ONE model line. Regroup the invoice only if Loo asks. Also: a voucher whose TRIGGER sku is a compartment sku now prints in the SO doc's defensive orphan block instead of under its line; and pre-2026-07-19 sofa orders can't re-print the seat height ("24″") — `sofa_height` wasn't persisted on exploded lines before this ship.
- `pos-only-account-card-gone` — Dealer POS-only (2026-07-19, PR #210): the deleted back-office Settings page's read-only account card (dealer name / region / status / deposit balance) has NO replacement surface — display-only loss, flagged to Loo in the ship summary. Add a small info block to the POS StaffManagePage overlay if he asks.
- `bundle-quote-stale-price` — Bundle pricing (2026-07-19): Save-Quote snapshots bundle component lines WITH their split prices + attrs; loading the quote after the principal re-prices/deactivates the bundle resurrects the OLD split (review finding #2). Consistent with quote semantics for normal lines (quotes freeze catalog prices too; create is client-priced), so accepted as a snapshot — strip/re-validate bundle lines on quote load if it ever bites.
- `bundle-order-detail-itemized` — Bundle pricing (2026-07-19): downstream of the cart (order detail, SO grid, invoice, ops) bundle lines render as N itemized lines at their split prices (Σ = bundle price exactly); only the POS CART badges them "Bundle · name". Add an order-detail regroup/badge off `attrs.bundle_key` (the sofa-build regroup pattern) if the itemized view confuses customers.
- `add-lines-rpc-30-line-cap` — Add-product P2 (2026-07-18): a non-UI client sending many sofa builds in one add can exceed the RPC's 30-line post-explosion cap → an unfriendly 422 `invalid_lines`. The UI sends 1 line per add, so this only bites bypassed callers; fails closed.
- `change-request-survives-unproceed` — Add-product P3 (2026-07-18): a pending `order_change_request` survives a proceed→place unproceed; approving it then equals a direct add (harmless). The dealer can't re-submit until the pending one is decided or cancelled (one-pending partial unique).
- `structured-address-flat-writer-clears` — POS entry fixes (2026-07-18): EditOrderModal + the ops `CustomerIdentityCard` still write the flat `customer_address` string only, so they CLEAR the structured parts (stale-guard by design — the composed string stays canonical). Convert those two surfaces to MYAddressFields if the round-trip drift ever annoys.
- `sofa-compartment-armconfig-add-form` — Sofa engine Phase 1 (2026-06-21): the compartment-pool "Add" form in the Maintenance UI doesn't author `arm_config` (rows can carry it via a direct insert / later edit, but the create form omits the field). Wire `arm_config` into the Add form when the field starts being authored from the UI (e.g. once Phase 3's drag-builder needs arm-cap validation per compartment).
- `sofa-engine-roadmap-remaining-phases` (watch) — **Phases 1-5 ALL SHIPPED + LIVE (P5 = the explode cutover, PR #36, 2026-06-23).** The ~5-phase **2990s sofa custom-cell engine** roadmap is code-complete; the engine is **DORMANT in prod (0 compartments authored)**. **Next = the principal authors real compartment data via Maintenance → the engine wakes → live-verify the explode + decide flat-sofa (628 SKUs) coexistence at cutover.** Deferred follow-ons: **recliner** per-seat toggle+pricing (needs a migration `product_models.recliner_upgrade_price`) · **group-drag/rotate** (P3 polish; per-cell drag+snap reconnects fine) · **regroup polish** for SO-grid / SO-PDF / returns (the exploded lines render correctly itemized today; customer-facing `DealerOrderDetail` already regroups) · promo (PWP/GWP) + bedframe.
- `sofa-p4-fabric-tier-trusted` (LOW, open) — P4/P5 trust `attrs.fabric_tier` rather than re-deriving it from `attrs.fabric_id` against `sofa_fabrics.tier`. Exposure bounded to the tier DELTA (à-la-carte + combo base fully reverified). Re-derive server-side from `fabric_id` if it ever bites. Dormant (0 tiered fabrics).
- `sofa-p4-asof-not-pinned` (LOW, open) — `recomputeAndExplodeSofaBuildLines` runs without an `asOf`, so a combo's `effective_from` anchors to server-today; a combo flipping effective between client preview + submit can cause a rare false `sofa_price_drift` reject (self-healing on rebuild; never under-prices). Pin `asOf` to the preview timestamp if needed.
- `sofa-p5-resync-clobber` (LOW, narrowed 2026-07-24 PR #253) — re-offering a compartment re-asserts the synced sku's `description`/`supplier_id` from the model+pool (canonical by design); a principal hand-edit to THOSE is lost on the next re-sync. `price`/`cost`/`pos_active` all survive a live re-offer (price/cost omitted from the upsert; pos_active preserved on a live row — first insert + re-offer-after-un-offer assert ON, offer = on sale). Intended; documented.
- `change-request-accessory-qty-edit` (LOW, 2026-07-25 PR #287) — accessory/service PRODUCT rows (pillows etc.) stay pencil-less in BOTH lanes: `orderLineEditKind` only admits configurator-backed rows (mattress/bedframe/sofa group), so a plain qty change on a flat accessory has no edit path (place or proceed). Matches the wizard-cart rule; if stores ask for qty edits, extend `orderLineEditKind` with a qty-only mini-editor riding the SAME replace pipeline (up-sell law included).
- `change-request-replace-edit-in-place` (LOW, 2026-07-25 PR #287) — a PENDING replace_lines request has View + Cancel but no in-place Edit (the AddProductOverlay edit mode can't seed a replace target); the store cancels + re-pencils instead. The 0234 one-pending unique makes the cancel→resubmit gap a brief race window (another same-dealer submission could slip in) — accepted for v1; wire `update_order_change_request` (already kind-aware server-side) to a seeded configure surface if it ever matters.
- `combo-crud-non-atomic` — Phase 4 (2026-06-20): combo + components insert/replace isn't a DB transaction (no RPC): POST uses a compensating-delete on a failed components insert; PATCH does delete-then-insert with a small no-components window. v1-acceptable (principal-only, low frequency). Defence: a `create_combo`/`update_combo` SECURITY DEFINER RPC if combo churn grows.
- `combo-skupicker-clear-x-a11y` — Phase 4 (2026-06-20): the combo editor's searchable SKU picker clear (X) is a `<span role=button>` without keyboard reachability (cosmetic; the collapsed trigger itself is a real button).
- `combo-submit-payload-test` — Phase 4 (2026-06-20): no test proves `combo_key` reaches the create_order payload through `DealerPos.handleSubmit` (verified by code inspection only); fold a combo-payload assertion into the existing `pos-web-shell-tests` carry-forward.
- `catalog-web-component-tests` — the new Product & Maintenance pages (`apps/web/src/pages/catalog/**`) have no web component/E2E tests yet (API side is covered in `catalog.test.ts`). Add a SkuMaster render + price-readback test, a Modular sizes-cascade test, and an E2E smoke (load page → filter Service → toggle a SKU off → persists after reload).
- `catalog-addon-reenable-by-key` — disabling an add-on removes it from the active-only GET bundle; restore is via re-adding the same key (the form PATCHes `active=true` on the duplicate-key 500). Fine for v1; a dedicated "show disabled add-ons" read would be cleaner if add-on churn grows.
- `pos-live-smoke` — the dealer POS (PR #22) was NOT live-smoked by Claude (POS sits behind a real dealer login on staging; build/typecheck/tests green instead). **Loo to manual-smoke**: login dealer → lands on POS → pick Mattress card → configure → cart → Proceed → customer + date + floor → confirm payment + signature → submit → ThankYou → "View orders" → "New order" returns to POS with draft cleared. Also verify sofa↔mattress mutex + disposal-size gate + ASAP auto-proceed.
- `pos-topbar-outlet-picker` — outlet/salesperson selection stays in the CUSTOMER step (`Step1Customer`); the POS top bar only mirrors the chosen outlet name read-only once set. Reference shows the outlet locked in the header — a top-bar picker (esp. for multi-outlet dealers) is deferred.
- `pos-web-shell-tests` — `CatalogStep` + the pure helpers (`cart`, `catalog-index`, configurators+`ConfigureDrawer`) are unit-covered, but `DealerPos` (3-step state machine + ported `handleSubmit`) and the `DealerApp` layout-route routing have no integration/E2E test. Logic is reused-verbatim, but add a `DealerPos` submit/state-machine test + a routing test (`/dealer`=POS no sidebar, `/dealer/orders`=sidebar) + a Playwright happy-path.
- `pos-folder-rename` — reused building blocks (`draft.ts`, `Step1Customer`, `Step3Delivery`, `Step3SignaturePayment`, `SignaturePad`, `PaymentSlipPicker`, `ThankYou`, `configurators.tsx`) still live under `dealer/new-order/`; new POS components under `dealer/pos/`. `new-order/` is now a misnomer — rename to `order/` once the external imports (`storage.ts`, `PODUploadDialog`, `DOAttachModal` import `draft.ts`/`SignaturePad`) are repointed. Tidy-up only.
- `pos-product-photos` — most `product_models` have no `photo_url`, so POS cards show a branded `▦` placeholder. Wire real photo display when catalog photos are uploaded (the `product-model-photos` bucket + `photoUrl` field already exist from 0171/0173).
- `pos-family-three-spellings` — POS catalog bands (2026-08-01, PR #548): one product family is spelled three ways on three surfaces — the card badge says `Bedframe` (`CATEGORY_LABEL`, `pages/catalog/components/atoms`), the left rail says `Bed frames` (`railEntries` in `CatalogStep`), and COPY-STANDARD's own table writes it `bed frame`. The new band header deliberately follows the RAIL (it is the operator's index, and one array now feeds both so the wall and the rail cannot drift), which leaves the badge as the odd one out on the same screen. **Not fixed here on purpose**: `CATEGORY_LABEL` is read by 5+ surfaces (POS card, POS card series line, add-product overlay, catalog admin, SKU master), so changing it is a copy ruling with a blast radius, not a POS tweak. **Firm fix**: Jess picks ONE spelling, COPY-STANDARD records it, and `CATEGORY_LABEL` + `railEntries` both take it from a single shared constant.
- `pos-all-series-select-is-dead` — POS catalog toolbar (noticed 2026-08-01, PR #548, pre-existing since the re-skin): the `All series` `<select>` beside the search box has exactly ONE hardcoded `<option>` and `onChange={() => {}}`. It is a control that cannot control anything, sitting in the toolbar's prime right-hand space, and its own comment admits it (*"One brand today — the series filter activates when series data exists"*). Carres has no series/branding column, which is also why the band grouping had to key on category. **Firm fix**: delete it until a series concept actually exists — a dead control teaches operators that controls here may not work. Kept for now because removing a visible element from the POS toolbar is a layout ruling, not a bug fix.
- `phase-11-deploy-verify-branch-has-latest` — **LESSON (2026-06-14)**: deploying `phase/11.2-state-machine` straight to prod briefly reverted the 6/12 ops overhaul, because the branch had forked from main on 6/05 and never carried it. Fixed by merging main back in + reconciling the enum (PR #15). **Before deploying ANY branch to prod, confirm it contains main's latest *deployed* work** (`git log --oneline HEAD..origin/main` should be empty, or you're shipping a regression). Cheap pre-deploy check; would have caught this instantly.
- `phase-11-migration-0165-0166-dual-files` — 0165 + 0166 each have two distinct files (an `ops_*` migration + an 11.1 migration sharing the number). Cosmetic only — migrations apply manually via MCP (tracker keys on timestamp). Renumbering was deliberately NOT done: the tracker records the exact names `0165_add_proceed_date` / `0166_set_order_date_2arg_compat_shim`, so renaming the files would desync file↔tracker. Leave unless a fresh `supabase db push` pipeline is ever introduced.
- `phase-10-rename-script-blind-spot-doc` — 0121 + 0123 both missed alias patterns. Enumerate ALL alias patterns upfront next rename, OR use comprehensive regex `(?<![a-z_])<col>(?![a-z_])`. Worth a doc in `docs/superpowers/`.
- `phase-10-pg-regex-word-boundary` — PG ARE `\b` is backspace, NOT word boundary. Use `\y` (PG-specific) or lookbehind. Tripped 0126 first apply.
- `phase-10-historical-dl-literal-strings` — `audit_log` + `order_history` `'DL-'` / `Auto-promoted DL-%s` literals intentionally kept as historical record.
- `phase-10-frozen-migration-vocab-drift` — 700+ `dl` / `logistics` refs in frozen migrations 0001-0122 + historical docs. Onboarding glossary footnote.
- `phase-10-thread-reserved-at-null-but-stock-reserved` — thread #534e59eb of #1003 had `reserved_at IS NULL` while stock_balances showed `reserved=3`. Reservation via path that doesn't set `thread.reserved_at`. Cosmetic.
- `phase-10-operation-attach-do-defensive-fallback` — Could fall back to first thread's warehouse_id if `orders.warehouse_id IS NULL`. Currently relies on 0127 upstream fix.
- `phase-10-rollup-trigger-audit-after-partial-revert` — Defensive direct UPDATE in 0127 backfill may be unnecessary. Investigate trigger UPDATE behaviour.
- `phase-10-drop-other-pre-thread-legacy-functions` — Survey pg_proc for other `_v3`-superseded predecessors.
- `phase-10-purchase-orders-do-number-fallback` — `purchase_orders.do_number` not auto-updated by partial-pickup RPCs; legacy reads NULL. Most reads `?? null` already.
- `phase-10-stockpile-po-readiness` — Stockpile POs (no thread links) still use existing PO-level "Mark Ready". New thread checklist appropriately hides for stockpile.
- `phase-10-partner-side-reprint` — `PartnerFactoryPickupsPage` doesn't mount `PickupHistoryList`. Add `GET /api/partner/pos/:poId/pickup-events` for parity.
- `phase-10-e2e-fixtures-partial-pickup` — `seed-test-users.ts` needs `supplier-nicefuture@x.com` + `partner-nets@x.com`. `seed-e2e-fixtures.sql` needs `PO-FIX-NF-PARTIAL`. Then un-fixme `e2e/phase-10-partial-pickup-happy.spec.ts`.
- `phase-10-orders-status-rollup-from-threads` — DL-1006..1015 stayed at `proceed_order` after threads dispatched. 0106 trigger only fires on `orders.logistics_stage='delivered'`. Pickup ≠ customer-delivery. Reminder.
- `phase-10-operation-procurement-page-ready-pill` — Operation procurement page has `ready_confirm_sent` branch at `ProcurementTabContent.tsx:515`; verify `ready_for_pickup` equivalent exists.
- `phase-9-custom-domain` — Bind real domain to Pages once alpha stable. Q2 deferred Week 2+.
- `phase-9-secret-rotation-cadence` — Quarterly rotation policy for Workers secrets.
- `phase-10-orders-partner-filter-chip` — Orders kanban needs `[All] [NETS] [TEOW] …` filter chips so operation can see "all single-partner-X orders" the way the old spreadsheet had per-partner tabs. The 0156 GIN index on `delivery_stops` already supports the `@>` containment query.
- `phase-10-stock-indicator-on-order-card` — Order kanban cards should show a 🟢/🟡/🔴 stock indicator inline so operation needn't open every drawer to gauge availability.
- `phase-10-multi-leg-promotion-decision` (watch ~3 months from 2026-06-05) — γ jsonb design assumes <30% of orders are multi-leg. Track the ratio in production; if it crosses 30% OR per-partner reliability dashboards become a real ask, promote to first-class relational `order_delivery_legs` table (β). jsonb shape mirrors the relational shape so backfill = one `INSERT … SELECT`. (Currently 0/158 orders multi-leg.)

**Pre-Phase-10 still open (low, deferred)** — full list in git history:
- `phase-4.5-chunk-2-alerts-tab-routing` — StockAlertsTile useNavigate URL/tab desync **[CLOSED 2026-06-05** — PR #12: StockAlertsTile + OperationWarehouse now use the tab-state callback instead of a bare URL navigate.]
- `phase-4.5-chunk-2-stock-alerts-high-threshold-expose` — `logistics_stock_alerts()` returns `low_threshold` only
- `phase-4.5-chunk-1-lp-whitelist-tighten` — Trigger covers 20 of 29 PO columns; switch to allow-list
- `phase-4.5-cleanup-at-own-wh-waiting-rename` — Irreversible enum recreate to drop deprecated `at_own_wh_waiting`
- `phase-4.5-do-storage-retention-policy` — TTL or archive for delivery-orders bucket
- `phase-4.5-0036-rollup-amend-cleanup` — Consolidate 0036+0047 `orders_rollup_stage` into single source
- `phase-4.5-storage-signed-url-ttl-config` — Explicit TTL for signed-upload URLs
- Earlier pre-Chunk-1 CFs (pagination, type tightening, schema audits) preserved in git history

- `guarantee-service-two-registries` — Guarantee & Service merge (2026-07-26, 0274): a sellable care plan is now a `guarantee_terms` row with `kind='recurring'`, but the RENTAL offer's *included* (free-with-a-rental) package still comes from the separate `service_packages` table, and `rental_plans.included_package_id` still points there. So "a care plan" has TWO registries and a principal authoring one must know which door to use — precisely the confusion this merge set out to remove. Deliberately not folded in this pass: re-pointing the rental offer's package picker + `create_rental_agreement`'s entitlement minting at `guarantee_terms` touches a just-shipped, credit-gated money path. **Firm fix**: make `guarantee_terms` the ONE cover registry — add a nullable `guarantee_sku` to `rental_plans` beside `included_package_id`, have the rental approve path mint a `guarantee_entitlements` row (which already counts visits since 0274) instead of `service_entitlements`, then retire `service_packages` / `service_entitlements` / `service_visits` once nothing reads them. Until then, treat `service_packages` as rental-internal and author everything sellable in SKU Master.
- `guarantee-recurring-no-visit-schedule` — Guarantee & Service merge (2026-07-26, 0274): a recurring plan tracks HOW MANY visits remain (`visits_used` / `visits_total`) but not WHEN they are due. `service_visits` has per-visit due dates spaced `365/visits_per_year` days apart; the guarantee ledger has none, so nothing can tell ops "this customer is owed a clean this month" — a visit is only recorded when someone remembers to claim it. Acceptable for authoring + selling (this pass) and for a pilot, but the Operation → Guarantees desk should grow a due-date column before care plans are sold at volume. **Firm fix**: either add `next_visit_due` to the entitlement (cheap, one column, derived from `starts_on` + used count) or mint per-visit rows the way the rental engine does. The second is the honest one if visits ever need individual scheduling, photo proof or a partner assignment.
- `rental-hq-direct-no-longer-allowed` — Rental SO (2026-07-26, 0275): `create_rental_agreement` now REFUSES a dealer-less agreement (`dealer_required`), because it mints an `orders` row and `orders.dealer_id` is NOT NULL. The RPC previously allowed an "HQ-direct" rental with no store, and RA-1003 (Loo's test record) was created that way. This is a deliberate narrowing — the POS always sends a store, and a rental with no store has nobody to deliver through either — but it IS a capability that existed and no longer does. **If HQ-direct selling is ever wanted back**, the fix is a real "Carres HQ" dealer row to attribute those orders to, not making `orders.dealer_id` nullable (which would ripple through every dealer-scoped RLS policy and report).
- `rental-order-total-is-zero` — Rental SO (2026-07-26, 0275): a rental order's line is priced 0 and its total is 0 by design (the money lives in `rental_billings`, and any other figure either overstates AR or double-counts the schedule). Consequences to know before reading a report: a rental SO contributes NOTHING to sales totals, AR, margin or commission-by-order-value, and the SO PDF / invoice will print RM0 unless they special-case `attrs.rental` (the terms are on the line for exactly that). **Firm fix when the billing engine lands**: report rental revenue from `rental_billings` (collected) rather than from `orders`, and teach the invoice template to print the rental block instead of a price.
- ~~`case-sla-no-closed-at`~~ — **CLOSED 2026-07-27 by S5 (PR #474), WITHOUT the column this entry asked for.** The premise held for the STATUS FLIP and not for the case: S3 (0293) made the transition into a closed status impossible without a `customer_confirmed` entry in `service_cases.progress`, and that entry already carries `on` — the BUSINESS date the customer said the problem was solved, stamped server-side. So the day a case ended is on file for every case closed since S3, and it is the better of the two dates by the module's own law ("a case is finished when the customer is"), where `closed_at` would have recorded the afternoon somebody changed a dropdown. S5 derives it (`caseFinishedOn`) and ships no migration. **The backfill this entry proposed is exactly what was refused**: filling `closed_at` from `updated_at` would put a row-touch timestamp into a business figure and flag it "approximate" — the one live pre-0293 case is instead counted in `finish.unmeasured`, named on screen, never averaged and never assumed on time. Original text kept below for the reasoning; do not re-open it as written. Service Case S4 (2026-07-27, 0298): `service_cases` records WHEN a case was opened (`opened_at`) but never when it was CLOSED. The status carries `is_closed` and nothing timestamps the transition, so the deadline clock can only be switched off for a closed case — it cannot say whether that case finished inside its 14 working days. **This blocks two of S5's own asks**: "avg days to close" and "SLA hit rate" are not computable for any case already closed, including every case closed before the column exists. **Firm fix, and it belongs in S5's migration**: add `closed_at timestamptz` set by a trigger on the transition INTO a closed status (the 0293 trigger already computes exactly that transition and can carry it), plus a one-time backfill from `updated_at` for the rows on file, flagged as approximate. Doing it in S5 rather than S4 is deliberate — S4 needs no such column and inventing one it does not read would be a field nobody maintains.
- `case-sla-deadline-only-for-open-cases` — Service Case S4 (2026-07-27): the deadline is derived from `opened_at`, which the edit modal lets an operator change freely. Moving Opened moves the deadline, silently, with no record — unlike the one extension, which is refused after the first use and always carries a reason. Bounded today (one case on file, and the field is the honest place to correct a mis-typed report date), but it is a second, unaudited way to move a deadline. **Firm fix if it is ever used that way**: make `opened_at` read-only after the first day, or write an `sla_events` entry when it changes.
- `eta-model-stock-eta-vs-line-etas-undecided` — Portal Core C8b (2026-07-28, PR #497, 0305), **deferred by Loo the same day and the deferral is the point**. The portal holds a supplier ready date in TWO places on `ops_order_control` — the single `stock_eta` column and the per-line `line_etas` map — and **the ladder reads only `line_etas`**: `stockEtaOf` (apps/web) computes the overshoot from the latest ETA among lines still WAITING and never looks at `stock_eta`, while `POST /:id/delay-decision` accepts EITHER as a date a decision may be made about (C8's validation reads `stock_eta, line_etas`). So an operator can record a delay decision against a date that can never open `Delay planning`, and C8b's 0305 trigger deliberately mirrors the LADDER rather than the route — a stamp about a date the engine can never compute is a stamp the engine can never match, and the pair would simply stop matching and go silent. **Unreachable today and measured, not assumed**: `stock_eta` NULL ×55 and `line_etas` empty ×55, so no live order can reach either path. **Loo, 2026-07-28: do NOT rule on whether `stock_eta` is a factory ETA now.** The ETA model is not fully built — P3 (Supplier Calls) has not shipped and no real PO has been run — and defining a model that half-exists is how a definition gets written to fit the half that happens to be there. **When it is picked up:** after P3 lands and P5 starts running real POs, settle the whole ETA model in one pass — which store is the factory's promise, which is the per-line answer, who writes each, and what a decision is allowed to point at — then make the ladder, the decision route and 0305's trigger read that one answer. Until then the seam stays exactly where C8 left it, on purpose, and a chat that "unifies" the two stores has chosen a data model on behalf of a line that has not spoken.

- `purchasing-balance-date-hits-every-customer-on-a-merged-po` — Purchasing P3 (2026-07-29, PR #506, 0306). **ACCEPTED BY LOO, NOT A DEFECT TO BE QUIETLY FIXED.** He ruled that a new BALANCE delivery date enters Delay planning exactly as a delayed tomorrow's-delivery answer does, because *"the portal must surface every known risk that may affect the Requested Delivery Window"*. The date therefore reaches `ops_order_control.line_etas` for **every customer order the PO covers** — and a PO line MERGES several customers' quantities (purchasing/MASTER.md) while **nothing in the data says whose units are the short ones**. So on a merged PO, customers whose own goods are not short are still flagged for a Delay planning decision. **His reasoning, recorded so nobody re-litigates it:** *"Today, without allocation, this may over-report on merged POs. That limitation belongs to P5 (allocation), not P3."* The trade is not close: over-reporting costs one operator answer per order (`We can still make the promised date`, and Delay planning is a DECISION, never a customer call), while under-reporting costs a missed customer promise. **It is also consistent with what the engine already believes** — 0299's receive RPC advances a customer thread only on `bool_and(received_qty >= qty)`, so a short PO line already leaves every linked customer waiting. **Unreachable today**: 0 purchase orders, 0 lines, 0 promises. **Firm fix, and it is P5's**: purchasing needs an allocation — which units of a merged PO line belong to which customer order line — and the push then narrows to the orders genuinely short. **Nobody may narrow the push before that allocation exists.** A rule that guesses which customer is affected is worse than one that over-says and is known to; P3 chose the loud one deliberately, and a chat that "tidies" it has overturned a ruling rather than fixed a bug.

- `po-stock-functions-executable-by-anon` — measured by P4's pre-flight (2026-07-29) and **deliberately NOT fixed in P4** (Loo: *"Do not fix the other six broad `anon` grants in P4 … Do not create or schedule another card now."*). Six SECURITY DEFINER functions that write purchase orders and/or the stock register carry `EXECUTE` for **both `anon` and `authenticated`**: `operation_receive_po_with_do` · `operation_create_po` · `operation_create_pos_batch` · `_operation_create_po_inner` · `operation_issue_pos_for_order` · `operation_cancel_po`. (`operation_receive_po_line` and `po_receive` were on this list and are revoked by P4/0307 — they had zero call sites and could post inventory outside the truthful destination path.) **What presently limits them is the guard INSIDE each body, not the grant**: every one resolves `public.app_role()` first and raises `42501` for a role that is not operation/principal, and `app_role()` returns NULL for an unauthenticated caller and for a disabled account (0266/0267). So an `anon` call fails — but it fails on a check written in plpgsql, one edit away from being weakened, rather than on a privilege the database would refuse to hand out. `_operation_create_po_inner` is the sharpest of the six: it is an INTERNAL helper (the `_` prefix says so), it is called only by the two create doors, and it has no reason to be reachable over PostgREST at all. **Recommended future review** (its own card, when Loo schedules one — not now): revoke `EXECUTE` from `anon` on all six, keep `authenticated` only where a browser genuinely calls the RPC, and add a sanity block asserting both directions the way 0303 and 0306 do — a `revoke … from public` alone does not drop `anon` on Supabase, which is why this has to be asserted rather than assumed. **Nothing here is exploitable today**; it is defence-in-depth that the portal already applies everywhere else.

### `po-history-two-legacy-send-claims` — CLOSED AS "KEEP", 2026-08-03

**Decision: the two rows stay. Do not rewrite them, do not delete them.** This
is recorded so the next chat that finds them does not "fix" them.

`po_history` holds two rows on **PO-2032**, written 2026-08-02 59 seconds apart
by one operator clicking WhatsApp twice:

```
sent to Ohana via whatsapp (Revision 1)
```

`audit_log` holds the matching pair. Both predate 0317, which stopped the
writer claiming a send. They are supplier-visible: `po_history_read` (0002)
admits a supplier to their own PO's rows, and Ohana has one active login.

**Why keeping them is the right engineering answer, not the lazy one:**

1. **Both tables are append-only BY DESIGN.** Measured: `po_history` and
   `audit_log` each have ZERO update and ZERO delete policies — only INSERT and
   SELECT. Correcting a row means reaching past RLS with `service_role` to
   defeat a constraint somebody deliberately built. The precedent — *history
   may be rewritten when it embarrasses us* — costs more than two rows.
2. **`audit_log`'s entire job is to record what happened, including the bug.**
   Editing it destroys the evidence that the defect existed and was fixed. It
   is internal-read only, so nothing outside Carres reads it anyway.
3. **The set is CLOSED and provably so.** Zero rows written since 0317, and the
   live `purchasing_record_send` cannot produce another (verified: its body
   contains `opened · Snapshot` and no `sent`). A guard in
   `purchasing-words.test.ts` now asserts the WRITER, not just the screen, so a
   future migration cannot quietly reintroduce the sentence.
4. **They are test data.** CLAUDE.md's standing rule: every row today is test
   data and the database starts CLEAN at go-live. PO-2032 and the Ohana login
   are seeded rows; no real supplier will ever read them.

**What was rejected and why:** an in-place UPDATE (defeats the append-only
design and erases the record) · a DELETE (irreversible production-data loss for
a cosmetic gain) · a correcting APPEND (leaves the false sentence visible AND
adds a contradicting one, which is worse for a low-English reader) · a
display-layer rewrite (a reader that edits stored text is a lie in the other
direction, and it would mask a genuine future row).

**Re-open this only if** the database does NOT start clean at go-live, or a
real supplier account is pointed at PO-2032 before then. In either case the
answer is still not an UPDATE — it is to void the PO's test history wholesale
with the rest of the seed data.

### `po-workspace-rail-inline-copy` — LOW, blocked on PR 507

`OperationPurchaseOrders.tsx` still draws its own 200px navigation rail inline.
The shared recipe already exists at
`apps/web/src/pages/operation/components/workspace-rail.tsx` and
`OperationReceiving.tsx` renders through it.

**After PR 507 lands** (the 4,047-conversion typography codemod, which owns that
file today), `OperationPurchaseOrders.tsx` adopts `RailGroup` / `RailItem` and
deletes its inline pair. It is an import, not a decision — the home is built.

**This entry is NOT permission to edit the file while the codemod PR owns it.**
Hand-resolving conflicts inside a 4,047-conversion codemod is how a codemod
gets corrupted. Wait for 507.

Worth carrying with it: extracting the recipe is what let the design linter SEE
that the rail hovered BLUE, against `01-design-tokens` §2.3 (a row / nav / chip
hovers grey; blue is the primary action and the selected row only). The shared
copy is grey and guarded. **The inline copy in `OperationPurchaseOrders.tsx` is
still blue**, hidden inside that file's baseline — adopting the shared
primitive fixes it in the same move.


- `add-line-spelt-two-ways-across-two-modules` — **A CHOSEN COST, recorded so nobody "fixes"
  it by picking the wrong side.** P19 (2026-08-05) shipped `Add line` / `Remove` in
  Purchasing's Create Purchase dialog, ruled by Loo; the Sales Portal's New Sales Order spells
  the same two gestures **`Add line item` / `Remove line`**
  (`PrincipalNewOrder.tsx:1028` · `:999`). One act, two spellings, two modules.

  **Why it was chosen, stated at the time rather than reconstructed.** The alternative was to
  take the Sales Portal's pair verbatim — zero new words — but that pair contains
  `Remove line`, and Purchasing's own PO preview already renders `Remove`
  (`TO_ORDER_WORDS.itemsRemove`). So option 1 cost **zero new words and one inconsistency
  INSIDE Purchasing**, while what shipped cost **one new word and zero inconsistency inside
  Purchasing**. A new word is a one-off — it needs a COPY-STANDARD row, which P19 wrote. An
  inconsistency inside one module is a permanent reading cost, and the two screens sit in one
  operator flow (create purchase → issue PO → preview). **Within-module consistency was ruled
  to outrank cross-module consistency, and this is the bill for that ruling.**

  **The rule for whoever picks it up:** if it is ever aligned, **the Sales Portal moves to
  Purchasing's pair, not the reverse** — Purchasing's is the ruled one and has the dictionary
  row. It is that lane's card, and it is not urgent: the two forms are different documents
  reached by different people doing different jobs. **What is NOT allowed is a chat "tidying"
  Purchasing back to `Add line item`** — that would re-introduce the within-module split the
  ruling paid a new word to avoid.


- `orders-drawer-items-keyed-by-sku` — **A React duplicate-key bug, found by S2.5 while
  building the LIST's expansion, and left alone deliberately.** `OrderDetailDrawer.tsx:3327`
  renders its Items panel with `<Fragment key={l.sku}>`, and **a sku is not unique within an
  order** — one live order carries `Essential Memory Pillow(L)` on two separate lines, so two
  real lines collapse into one React child.

  **Why S2.5 recorded it instead of fixing it.** The S2 card names the drawer DO-NOT-TOUCH for
  every capability in the queue, and a one-word repair inside a 7,717-line file that another
  lane may be holding is not worth breaking that. **The fix is `key={i}`**, which is what the
  list's own `OrderItemsPanel` does and for exactly this reason. Whoever next owns that file
  takes it in passing.

- `api-suite-red-on-main-four-stale-route-mocks` — **`apps/api` has been red for a long time
  and no card has owned it.** Measured 2026-08-08 on a clean `f32ed152`: **4 failures in 3
  files**, all in Partner / Supplier / Purchasing routes —
  `partner/pickups.test.ts` · `supplier/pos.test.ts` (×2) · `operation/to-order.test.ts`.

  **The cause is one class, not four bugs.** They are the defect S2.0 named on the web side:
  *a test asserting a shape the route already changed*. `partner/pickups.ts` deliberately
  dropped its `.eq("procurement_partner_id", …)` (the comment says so), but the test still
  mocks `from → select → eq → order`, so `.order()` lands on `undefined` and the route answers
  **500 where the test expects 200**.

  **Why this matters more than four red lines.** `apps/web` is green at 231 files / 2,724
  tests and `packages/shared` at 2,231, so the API suite is the one place a real regression
  could hide inside standing noise — which is the exact condition S2.0 existed to remove.
  There is **no CI in this repo** (no `.github/`), so nothing else will catch it.

  **It is a Purchasing-lane card, not an Orders one**, which is why S2.5 reported it rather
  than widening its own diff to three unrelated route-test repairs. It looks small — the fix
  is the mock chain, not the routes.

- `migration-0346-numbered-twice-and-one-is-unpushed` — ✅ **CLOSED 2026-08-13 by the Card 4
  lane, exactly as instructed below.** The unpushed file was mine: it is now in the repository
  as `supabase/migrations/0347_a_voided_payment_is_not_money.sql`, with a header stating that
  it was APPLIED as `0346` (tracker row `20260813034523`) and why it is numbered `0347` here.
  Card 3's committed 0346 was not touched. The repository now holds one 0346 and one 0347, so
  no future `ls` can take a taken number and no chat can skip a migration that was never
  checked in. **The tracker row still reads `0346_a_voided_payment_is_not_money`** — a name is
  not a number, and rewriting an applied row to make a document tidier is not a thing this
  repository does. The original finding is kept below, because the lesson (two lanes, one
  minute, one number) outlives the fix.

  **The finding as Card 3 recorded it — 🔴 Two different migrations are both
  numbered 0346 in production, and one of them is not in this repository.** Measured on the
  live tracker 2026-08-13 immediately after the Card 3 deploy:

  ```
  20260813034521  an_appointment_names_the_carrier_it_was_made_with   ← Card 3 (this repo)
  20260813034523  0346_a_voided_payment_is_not_money                  ← NOT in the repository
  ```

  **Two seconds apart, from two chats.** `origin/main` carries exactly one `034[0-9]` file for
  0346 — Card 3's. The other was applied straight to production by a parallel lane (the name
  reads as a Card 4 payment follow-up) and has not been pushed. CLAUDE.md §5.7 is explicit:
  **an applied migration missing from the repository is a P0.**

  **Card 3 did NOT touch it, and deliberately.** Red line 6 forbids altering a committed
  migration, and Card 3's 0346 is committed, applied and pushed — renaming it now would break
  the very rule that keeps the tracker honest. The other lane's file is also not this card's to
  write: only its author knows what it actually ran.

  **The rule for whoever picks it up** — almost certainly the Card 4 / payments lane, which is
  the one that ran it: **push the file, and renumber it to `0347`, not `0346`.** Card 3's 0346
  landed first and is already on `main`, so the second file is the one that moves. The tracker
  row already applied keeps its own name; what must stop is a repository with two 0346s, which
  is how a future `ls`-based number gets picked (red line 7) and how the next chat silently
  skips a migration that was never checked in.
