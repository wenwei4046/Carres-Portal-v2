# Phase 4.5a v3 Wake — Pre-flight Audit Notes

> **Task:** T1 of `docs/superpowers/plans/2026-05-05-phase-4.5a-v3-wake.md` — read-only verification before any migration is written.
> **Date:** 2026-05-05
> **Staging project_id:** `kfprgpjpaffedghytstl`
> **Branch:** `main`
> **Verdict:** **PROCEED** (zero-row precondition holds; 3 CASCADE casualties confirmed; literal sweep target ≈79 source-code hits across 14 files in `apps/` + `packages/`).

---

## awaiting_stock literal locations

### Methodology

Ran three greps:

```
grep -rn "'awaiting_stock'" apps/ packages/                 # single-quoted
grep -rn '"awaiting_stock"' apps/ packages/                 # double-quoted
grep -rn  "awaiting_stock"  apps/web/src/pages/logistics/LogisticsOrders.tsx
```

Build-artifact paths (`.dry-build`, `.wrangler/tmp`, `dist/`, `node_modules`) excluded — those regenerate from source on the next build, so they are not Task 5 inputs.

### Counts (source-only, build artifacts excluded)

| Quote style | Count |
|---|---|
| `'awaiting_stock'` (single) | 7 lines |
| `"awaiting_stock"` (double) | 36 lines |
| Total source occurrences (any context, including comments) | **123 lines** |
| Source occurrences excluding pure-comment `//` and ` *` lines | **79 lines** |

Spec target was ≥30 — confirmed comfortably above the floor. Source files containing the literal: **14**.

### Single-quoted hits (7 lines, 4 files)

```
apps/api/src/routes/logistics/orders.ts:484:                                  // source-stage guard now permits IN ('proceed_request', 'awaiting_stock').    [comment]
apps/api/src/routes/logistics/pos.ts:96:                                      //    logistics_stage='awaiting_logistics_action' OR 'awaiting_stock' (the   [comment]
apps/api/src/routes/logistics/pos.test.ts:1428:                                // Order A is pre-v3 / unsplit data: orders.logistics_stage='awaiting_stock'   [comment]
apps/web/src/pages/logistics/components/TransferReadyDialog.tsx:22:           // widens to IN ('proceed_request', 'awaiting_stock') AND requires a non-NULL  [comment]
packages/shared/src/schemas/logistics.ts:152:                                 // source-stage guard widens to IN ('proceed_request', 'awaiting_stock'), so   [comment]
packages/shared/src/schemas/logistics.ts:265:                                 // stage: z.enum(['all', 'placed', 'proceed_request', 'awaiting_stock', ...])  [LIVE: zod stage filter enum]
packages/shared/src/schemas/logistics.ts:329:                                 // every order currently in `logistics_stage='awaiting_stock'`.               [comment]
```

Live (non-comment) single-quoted target: **1** — `packages/shared/src/schemas/logistics.ts:265` (the zod `stage` enum used by the kanban filter and FE column id).

### Double-quoted hits (source files, abbreviated)

Live (non-comment) double-quoted targets fall into three buckets:

**Schema/types (3 files):**
- `packages/shared/src/db-types.ts:15` — `logistics_stage` union type
- `packages/shared/src/domain.ts:200` — domain-layer logistics_stage union
- `packages/shared/src/schemas/orders.ts:20` — zod enum

**API routes + tests (5 files):**
- `apps/api/src/routes/logistics/pos.ts:133` — `.in("logistics_stage", ["awaiting_logistics_action", "awaiting_stock"])` (LIVE filter widening per v3-S4.6)
- `apps/api/src/routes/logistics/orders.test.ts` — 3 fixture rows (lines 59, 313, 871)
- `apps/api/src/routes/logistics/pos.test.ts:985` — comment

**Web FE + tests (8 files / ~30+ hits):**
- `apps/web/src/lib/queries.ts:904, 933` — TS union literals
- `apps/web/src/pages/logistics/LogisticsOrders.tsx:47, 93, 139, 144, 145` — kanban column id, `stageOf()`, bulk-action filter
- `apps/web/src/pages/logistics/LogisticsOrders.test.tsx` — 14 fixtures (lines 95, 194, 211, 272, 273, 292, 305, 326, 427, 435, 569, 590, 616, 625, 642, 651)
- `apps/web/src/pages/logistics/LogisticsDashboard.tsx:169` — `<KanbanColumn stage="awaiting_stock">`
- `apps/web/src/pages/logistics/LogisticsDashboard.test.tsx:106, 175, 195` — 3 fixtures
- `apps/web/src/pages/logistics/components/OrderColumn.tsx:81` — `selectable = stage === "awaiting_stock"`
- `apps/web/src/pages/logistics/components/OrderDetailDrawer.tsx:290, 553` — derived stage logic
- `apps/web/src/pages/logistics/components/PipelineColumn.tsx:36, 67` — TS union + branch
- `apps/web/src/pages/logistics/components/StageChip.tsx:21` — TS union

### LogisticsOrders.tsx focused dump

```
apps/web/src/pages/logistics/LogisticsOrders.tsx:21: *   - Bulk action bar when ≥1 awaiting_stock orders selected
apps/web/src/pages/logistics/LogisticsOrders.tsx:22: *   - 6-column kanban (placed / proceed_request / awaiting_stock /
apps/web/src/pages/logistics/LogisticsOrders.tsx:30: *   awaiting_stock     → "PO open with supplier"   · action "Check stock"
apps/web/src/pages/logistics/LogisticsOrders.tsx:47:  { key: "awaiting_stock",    label: "Awaiting Stock",    hint: "PO open with supplier",   action: "Check stock" },
apps/web/src/pages/logistics/LogisticsOrders.tsx:86:  //   - else                → awaiting_stock (legacy proceed_order rows
apps/web/src/pages/logistics/LogisticsOrders.tsx:93:    return "awaiting_stock";
apps/web/src/pages/logistics/LogisticsOrders.tsx:100:      awaiting_stock: 0,
apps/web/src/pages/logistics/LogisticsOrders.tsx:118:      awaiting_stock: [],
apps/web/src/pages/logistics/LogisticsOrders.tsx:139:      .filter((o) => selectedDls.has(o.dl) && stageOf(o) === "awaiting_stock")
apps/web/src/pages/logistics/LogisticsOrders.tsx:144:    if (stageKey !== "awaiting_stock") return;
apps/web/src/pages/logistics/LogisticsOrders.tsx:145:    const stageOrders = ordersByStage.awaiting_stock;
```

LogisticsOrders.tsx alone holds **11 lines** to update (5 live string literals across the column-config tuple, `stageOf()` return, two map-key initializers, two filter predicates; 6 are comments/JSDoc).

### Files needing edit in T5 (consolidated)

```
apps/api/src/routes/logistics/orders.test.ts                     [test fixtures × 3]
apps/api/src/routes/logistics/orders.ts                          [comment only — drop or keep]
apps/api/src/routes/logistics/pos.test.ts                        [comment only]
apps/api/src/routes/logistics/pos.ts                             [.in() filter array — drop awaiting_stock or replace]
apps/web/src/lib/queries.ts                                      [TS union × 2]
apps/web/src/pages/logistics/LogisticsDashboard.test.tsx         [test fixtures × 3]
apps/web/src/pages/logistics/LogisticsDashboard.tsx              [stage prop × 1]
apps/web/src/pages/logistics/LogisticsOrders.test.tsx            [test fixtures × 14]
apps/web/src/pages/logistics/LogisticsOrders.tsx                 [column id + stageOf + filters × 5]
apps/web/src/pages/logistics/components/OrderColumn.tsx          [string equality × 1]
apps/web/src/pages/logistics/components/OrderDetailDrawer.tsx    [string equality × 2]
apps/web/src/pages/logistics/components/PipelineColumn.tsx       [TS union + branch × 2]
apps/web/src/pages/logistics/components/StageChip.tsx            [TS union × 1]
apps/web/src/pages/logistics/components/TransferReadyDialog.tsx  [comment only]
packages/shared/src/db-types.ts                                  [union — regen post-0040]
packages/shared/src/domain.ts                                    [union × 1]
packages/shared/src/schemas/logistics.ts                         [zod enum × 1 live + 3 comments]
packages/shared/src/schemas/orders.ts                            [zod enum × 1]
```

**14 source files**, 30+ live edits, plus comment refresh in ~6 files.

---

## pg_depend casualty list

Query (against staging Supabase project_id `kfprgpjpaffedghytstl`):

```sql
SELECT pg_describe_object(d.classid, d.objid, d.objsubid) AS object, d.deptype
  FROM pg_depend d
 WHERE d.refobjid = 'public.logistics_stage'::regtype
   AND d.deptype IN ('n', 'a');
```

Result:

| object | deptype |
|---|---|
| `column logistics_stage of table orders` | `n` |
| `column logistics_stage of table order_supplier_threads` | `n` |
| `function orders_rollup_stage(uuid)` | `n` |

**3 rows** — exactly matches the spec's expected casualty list. All three MUST be reproduced inside migration 0040 (`DROP TYPE … CASCADE` will nuke them; the migration must `CREATE TYPE … AS ENUM (…) WITHOUT awaiting_stock`, then `ALTER TABLE … ADD COLUMN logistics_stage <new type>` with the original DEFAULT/NULL semantics, repopulate from a pre-drop snapshot, and `CREATE OR REPLACE FUNCTION orders_rollup_stage(uuid)` against the new type — plus reattach the AFTER trigger from 0036).

deptype `n` = NORMAL dependency (default). No `a` (AUTO) rows surfaced — the `awaiting_stock` enum value itself is implied by the type's existence rather than an `a` edge, so the query returning only `n` rows is expected and matches the spec author's mental model.

---

## row counts

Query:

```sql
SELECT 'orders' AS tbl, count(*) AS n FROM orders WHERE logistics_stage = 'awaiting_stock'
UNION ALL
SELECT 'order_supplier_threads' AS tbl, count(*) AS n
  FROM order_supplier_threads WHERE logistics_stage = 'awaiting_stock';
```

Result:

| tbl | n |
|---|---|
| `orders` | **0** |
| `order_supplier_threads` | **0** |

Both zero. No data UPDATE step needed in 0040 — the type recreate is purely structural. v3 ships dormant per the v3-S4/S5 reviews, so `order_supplier_threads` has no production rows, and `orders.logistics_stage` is on `awaiting_logistics_action` (the v3 vocabulary) for every active row. Spec precondition holds.

---

## conclusion

**PROCEED** with Tasks 2-6.

Gate decisions:

1. **Zero-row precondition: PASS.** No data UPDATE step required in migration 0040. The type recreate is structural-only.
2. **CASCADE casualty list: PASS.** 3 dependents (orders.logistics_stage, order_supplier_threads.logistics_stage, orders_rollup_stage(uuid)). Migration 0040 must reproduce all three plus reattach the AFTER trigger added by 0036. No surprise casualties.
3. **Literal sweep scope: PASS.** 14 source files, ~30+ live edits, well above the spec's ≥30 floor. T5 author should:
   - Replace every live `'awaiting_stock'` / `"awaiting_stock"` with `'awaiting_logistics_action'` / `"awaiting_logistics_action"`.
   - Update kanban column id in `LogisticsOrders.tsx:47` (key from `awaiting_stock` to `awaiting_logistics_action`) — this is the FE column rename Task 5 calls out specifically.
   - Refresh comments in `orders.ts:484`, `pos.ts:96`, `pos.test.ts:1428`, `TransferReadyDialog.tsx:22`, `logistics.ts:152, 329`, `LogisticsOrders.tsx:21-30, 86` so docstrings match new vocab.
   - Drop the second array element in `pos.ts:133` `.in("logistics_stage", ["awaiting_logistics_action", "awaiting_stock"])` once 0040 has dropped the value, OR keep both during the soak window and drop in a follow-up commit (decide in T5 review).
4. **No blockers surfaced.** Audit query returned exactly the spec's expected shape on both row count and casualty list. Phase 4.5a is cleared to begin Task 2 (migration 0038 v2 RPC vocab sweep).

---

## T7 Staging Smoke

> **Date:** 2026-05-05 (collapsed soak per Loo's autonomous-run constraint)
> **Staging project_id:** `kfprgpjpaffedghytstl`
> **Migrations applied:** through `0040_logistics_stage_drop_awaiting_stock` (verified via `mcp__supabase__list_migrations`)
> **Local commit range:** `4a41604..d9df58d` on `main`, NOT pushed
> **Verdict:** **PROCEED to T8 prod deploy** — all 6 smoke checks PASS, full vitest + Playwright + static-grep regression green

### Smoke 1 — v3 confirm walkthrough (proceed_request → ready_to_dispatch)

Test order: `00000000-0000-0000-0000-000000001247` (`dl=1247`, demo seed). Two order_lines:
- `bedframe:l1202f:Queen` × 1 (supplier `…e1`, category `bedframe`)
- `mattress:carres-original:Queen` × 1 (supplier `…e2`, category `mattress`)

Test wrapped in `BEGIN … ROLLBACK` with `SET LOCAL request.jwt.claims` to impersonate `Daniel · Logistics` (`11111111-1111-1111-1111-000000000004`). Mutated `orders.status='proceed_order' / logistics_stage='proceed_request'`, then called `logistics_confirm_proceed_request_v3(...)`.

**Result jsonb (raw, captured pre-rollback):**

```json
{
  "dl": 1247,
  "po_id": null,
  "threads": [
    {"po_id": null, "stage": "ready_to_dispatch", "category": "bedframe",
     "sop_name": "STANDARD", "thread_id": "<uuid>", "supplier_id": "…e1"},
    {"po_id": null, "stage": "ready_to_dispatch", "category": "mattress",
     "sop_name": "STANDARD", "thread_id": "<uuid>", "supplier_id": "…e2"}
  ],
  "order_id": "00000000-0000-0000-0000-000000001247",
  "auto_skipped": true,
  "logistics_stage": "ready_to_dispatch"
}
```

Schema-shape assertions:

| Field | Expected | Actual | Verdict |
|---|---|---|---|
| `order_id` | matches input UUID | matches | PASS |
| `dl` | echo of `orders.dl` | `1247` | PASS |
| `logistics_stage` | one of `awaiting_logistics_action` / `ready_to_dispatch` | `ready_to_dispatch` (auto-skip branch) | PASS |
| `auto_skipped` | boolean | `true` (own warehouse `c1` covered both SKUs) | PASS |
| `po_id` | `null` (no ghost PO) | `null` | PASS |
| `threads` | array, populated | 2 threads (one per supplier×category) | PASS |

Post-RPC `orders.logistics_stage = 'ready_to_dispatch'` confirmed inside the same transaction → rollup-by-RPC-body works. (The 0036 trigger is also wired, but the RPC body itself sets `orders.logistics_stage` directly via the auto-skip branch, so the trigger fires as a re-confirmation rather than the primary writer.)

After ROLLBACK, order 1247 restored to `status='place' / logistics_stage=null`, `order_supplier_threads` row count = 0. **Zero permanent staging drift.**

**Verdict: PASS**

### Smoke 2 — enum drop verification

```sql
SELECT enumlabel FROM pg_enum WHERE enumtypid = 'public.logistics_stage'::regtype
 ORDER BY enumsortorder;
```

Returned 7 values, no `awaiting_stock`:

```
placed
proceed_request
awaiting_logistics_action
ready_to_dispatch
dispatched
waiting
delivered
```

Cast attempt `SELECT 'awaiting_stock'::logistics_stage;` → SQLSTATE `22P02` `invalid_text_representation` (expected failure).

**Verdict: PASS**

### Smoke 3 — `logistics_dashboard_summary` JSON-key rename

The MCP execute_sql session does not carry a logistics JWT, so a live invocation is gated by `RAISE 'forbidden: logistics only' USING errcode = '42501'` at line 11 of the function. Verified statically via `pg_get_functiondef`: the body builds `v_pipeline := jsonb_build_object('awaiting_logistics_action', …, 'ready_to_dispatch', …, 'dispatched', …)` — no `awaiting_stock` JSON key remains in the source. Migration `0039b_logistics_dashboard_summary_v3_key_rename` is correctly installed.

**Verdict: PASS (static-source)**

### Smoke 4 — `order_proceed` defensive recreate

Live call inside `BEGIN … ROLLBACK` against demo order 1247 (impersonating logistics):

```sql
SELECT (order_proceed('00000000-0000-0000-0000-000000001247'::uuid)).logistics_stage AS new_stage;
```

Returned `awaiting_logistics_action`. Function source verified via `pg_get_functiondef`: the `UPDATE orders SET logistics_stage = 'awaiting_logistics_action'` write is the only stage assignment in the body — no `awaiting_stock` literal. After ROLLBACK, order 1247 restored to `status=place / logistics_stage=null`.

**Verdict: PASS**

### Smoke 5 — five v3-flow RPCs callable

```sql
SELECT proname FROM pg_proc 
 WHERE pronamespace = 'public'::regnamespace 
   AND proname IN (
     'logistics_create_po',
     'logistics_create_pos_batch',
     'logistics_confirm_proceed_request_v3',
     'logistics_cancel_po',
     'logistics_issue_pos_for_order'
   );
```

Returned 5 rows. All v3-flow RPCs present.

**Verdict: PASS**

### Smoke 6 — static-grep regression test

```
pnpm --filter api test -- pos-vocab-v3-sweep
```

`apps/api/src/routes/logistics/pos-vocab-v3-sweep.test.ts` → 4/4 green (1 functional 22023 wrong_stage assertion + 3 file-body string sweeps for migrations 0038, 0038b, 0039b). Test suite total reported by vitest: `Test Files 20 passed, Tests 298 passed`.

**Verdict: PASS**

### Full vitest + Playwright

| Suite | Expected baseline | Actual | Verdict |
|---|---|---|---|
| `@carres/shared` vitest | 112 | 112 passed (6 files) | PASS |
| `apps/api` vitest | 298 | 298 passed (20 files) | PASS |
| `apps/web` vitest | 253 | 253 passed (22 files) | PASS |
| **Total vitest** | **663/663** | **663 passed** | **PASS** |
| Playwright E2E | 16/18 (2 known-failing baseline) | 16 passed / 2 failed | PASS (matches baseline) |

The 2 failing Playwright specs are the same pre-v3-wake baseline regressions:
- `e2e\dealer-new-order-submit.spec.ts:120 — Step 3 → Submit → ThankYou with order number`
- `e2e\dealer-orders.spec.ts:33 — clicking an order row opens the detail modal`

Neither is in the logistics flow, neither references the `awaiting_stock` literal. v3 wake did not regress E2E.

### Test order recap + restoration

- **Test order ID:** `00000000-0000-0000-0000-000000001247` (demo seed)
- **Original state:** `status='place'`, `logistics_stage=null`
- **State during test:** `status='proceed_order'`, `logistics_stage='proceed_request'` → `'ready_to_dispatch'` (inside transaction only)
- **Final state:** `status='place'`, `logistics_stage=null` (restored via `ROLLBACK`)
- **Threads created during test:** 2 (rolled back; final count = 0)
- **stock_balances.reserved deltas:** computed inside transaction (rolled back)
- **stock_movements + audit_log + order_history rows:** none survived ROLLBACK

### Anomalies

None.

### Verdict

**PROCEED to T8 prod deploy.** All 6 smoke checks PASS. v3 wake (migrations 0037-0040 + API confirm RPC swap + FE rename) is functionally correct on staging. Local commits `4a41604..d9df58d` ready for push + production rollout per T8 plan.
