# Phase 4.5 Chunk 2 Implementation Plan — Per-leg LP Schema Split + Tab UI + Stockpile + COGS

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land Phase 4.5's Chunk 2 — split `purchase_orders.delivery_partner_id` into procurement (PO) + customer (thread) legs; rewrite ~50 code touchpoints; add per-supplier tab UI at `/logistics/procurement/{slug}`; ship stockpile thresholds + alerts on `stock_balances`; ship COGS columns on `purchase_order_lines`; pre-stage Phase 6 supplier portal columns. Closes 4 carry-forwards.

**Architecture:** 7 additive + 1 IRREVERSIBLE migration (0049–0055) using `CREATE OR REPLACE` self-contained-body pattern from 0045/0034. 8+ RPCs rewritten in `0051_logistics_rpcs_chunk2.sql`. New Hono routes for stockpile alerts + per-tab procurement listings. New React Router 7 nested route shell `TabbedProcurementShell.tsx` with 3 child tabs. Migration 0052 (PO column drops) is IRREVERSIBLE and gated on explicit Loo approval at apply time.

**Tech Stack:** Supabase Postgres + plpgsql RPCs · Hono on Cloudflare Workers · Vite/React 18 + React Router 7 + TanStack Query 5 · vitest + jose JWT (unit/integration) · Playwright (E2E).

**Spec source:** `docs/superpowers/specs/2026-05-05-phase-4.5-chunk-2-design.md` v1 (will land in same commit as this plan).

**Branch:** Continue on `main` per recent v3 sprint practice + Chunk 1 precedent — verify with Loo before first commit. Alternative: `phase/4.5-chunk-2-tabs-stockpile-cogs` per CLAUDE.md §13.

**Predecessor commits:** `e60a5a7` (lp-address-column closure) on top of `phase-4.5-chunk-1-complete @ 210e222`.

**Exit tag:** `phase-4.5-chunk-2-complete` (final Phase 4.5 tag — Phase 5 Finance starts after).

---

## File Structure

### Create

**Migrations (7):**
- `supabase/migrations/0049_threads_customer_leg_columns.sql` — additive ADD COLUMNS on threads + partial index
- `supabase/migrations/0050_threads_customer_leg_backfill.sql` — UPDATE thread rows from PO
- `supabase/migrations/0051_logistics_rpcs_chunk2.sql` — CREATE OR REPLACE 8 RPCs to read from thread
- `supabase/migrations/0052_purchase_orders_drop_customer_leg.sql` — RENAME col + DROP 4 cols (IRREVERSIBLE — Loo-gated)
- `supabase/migrations/0053_stock_balances_thresholds.sql` — additive low/high_threshold cols + CHECK
- `supabase/migrations/0054_purchase_order_lines_cogs.sql` — additive cost + cost_source ENUM
- `supabase/migrations/0055_suppliers_phase6_prep.sql` — additive portal_enabled + contact_email

**Hono routes:**
- `apps/api/src/routes/logistics/procurement-tabs.ts` + `.test.ts` — `GET /api/logistics/procurement/{slug}` per-tab list endpoint
- `apps/api/src/routes/logistics/stock-alerts.ts` + `.test.ts` — `GET /api/logistics/stock-alerts` (calls new RPC)
- `apps/api/src/routes/logistics/thresholds.ts` + `.test.ts` — `POST /api/logistics/warehouses/{id}/skus/{sku}/threshold` (set low/high)
- `apps/api/src/routes/logistics/recent-cost.ts` + `.test.ts` — `GET /api/logistics/skus/{sku}/recent-cost` (for "prev_po" auto-fill)

**Frontend pages:**
- `apps/web/src/pages/logistics/procurement/TabbedProcurementShell.tsx` + `.test.tsx`
- `apps/web/src/pages/logistics/procurement/NiceFutureMattressTab.tsx` + `.test.tsx`
- `apps/web/src/pages/logistics/procurement/HoOKkASofaTab.tsx` + `.test.tsx`
- `apps/web/src/pages/logistics/procurement/HoOKkABedFrameTab.tsx` + `.test.tsx`

**Frontend components:**
- `apps/web/src/pages/logistics/components/SetThresholdDialog.tsx` + `.test.tsx`
- `apps/web/src/pages/logistics/components/StockAlertsTile.tsx` + `.test.tsx`
- `apps/web/src/pages/logistics/components/CogsLineEditor.tsx` + `.test.tsx`

**E2E (Sprint G):**
- `e2e/per-leg-lp-split.spec.ts` (`test.fixme()`)
- `e2e/tab-routing.spec.ts` (`test.fixme()`)
- `e2e/stockpile-alert-to-po.spec.ts` (`test.fixme()`)

**Reflection:**
- `docs/superpowers/plans/2026-05-06-phase-4.5-chunk-2-preflight-notes.md`
- `docs/phase-4.5-chunk-2-reflection.md`

### Modify

- `apps/api/src/routes/partner/pickups.ts` — JOIN through `order_supplier_threads.delivery_partner_id` instead of `purchase_orders.delivery_partner_id`
- `apps/api/src/routes/partner/dashboard.ts` — same JOIN swap
- `apps/api/src/routes/logistics/orders.ts` — partner filter swap
- `apps/api/src/routes/logistics/orders-print-do.test.ts` — fixture shape
- `apps/api/src/routes/logistics/codex-fixes.test.ts` — F1-F12 reference updates if any
- `apps/api/src/routes/logistics/pos.ts` — `createPoInput` + zod cost field; ApplyPo extends with cost
- `apps/web/src/lib/queries.ts` — types + new query keys for per-tab + alerts + threshold + cost
- `apps/web/src/lib/api.ts` — typed wrappers if needed
- `apps/web/src/App.tsx` — register `/logistics/procurement/:slug` nested routes
- `apps/web/src/pages/logistics/LogisticsProcurement.tsx` — slim → re-exports `TabbedProcurementShell` (or deleted entirely)
- `apps/web/src/pages/logistics/LogisticsProcurement.test.tsx` — split tests into per-tab files
- `apps/web/src/pages/logistics/components/OrderCard.tsx` — read `delivery_partner_id` from thread
- `apps/web/src/pages/logistics/components/OrderDetailDrawer.tsx` — same
- `apps/web/src/pages/logistics/components/CreatePOModal.tsx` — extend lines with cost + cost_source dropdown + "Suggest from alerts" button
- `apps/web/src/pages/logistics/components/PoDetailModal.tsx` — surface cost_source + cost in line table
- `apps/web/src/pages/logistics/LogisticsDashboard.tsx` — add StockAlertsTile
- `apps/web/src/pages/logistics/LogisticsWarehouse.tsx` — threshold inline edit + red dot indicator
- `apps/web/src/pages/logistics/LogisticsSidebar.tsx` (if exists) — nav link defaults to `nice-future`
- `packages/shared/src/db-types.ts` — `PurchaseOrderRow.delivery_partner_id → procurement_partner_id`; drop 4 customer-leg fields. `OrderSupplierThreadRow` adds 5 fields. `StockBalanceRow` adds 2. `PurchaseOrderLineRow` adds 2 + enum.
- `packages/shared/src/domain.ts` — same camelCase mirror updates
- `packages/shared/src/adapters.ts` — handle new fields snake↔camel
- `packages/shared/src/schemas/logistics.ts` — `createPoInput.lines` extends with `cost` + `costSource` + new `setThresholdInput`
- `packages/shared/src/sops.ts` — `PROCUREMENT_TAB_SLUGS` enum + slug derivation helper
- `packages/shared/src/index.ts` — barrel exports for new schemas + types
- `apps/api/src/index.ts` — register new route modules
- `CLAUDE.md` §17 — update phase status, test count, migration count, carry-forward closures

---

## Sprint A — Threads customer-leg columns (foundation)

Goal: land migrations 0049 + 0050 to give threads the 5 new columns and backfill from existing PO data. Tests stay green because RPCs still read from PO; thread cols are dormant write-ahead state.

- [ ] **Task 1** — Write `supabase/migrations/0049_threads_customer_leg_columns.sql`
  Apply via Supabase MCP `apply_migration` to staging `kfprgpjpaffedghytstl`.
  Acceptance: 5 cols + partial index `ost_rfd_pending_idx` exist; existing thread rows have NULL for all 5 fields.

- [ ] **Task 2** — Write `supabase/migrations/0050_threads_customer_leg_backfill.sql` + apply
  Idempotent — re-run safe via `WHERE t.delivery_partner_id IS NULL` guard. Should be a no-op on staging (no LP test rows exist yet) but exercises the SQL path.
  Acceptance: SELECT count(*) FROM order_supplier_threads WHERE delivery_partner_id IS NOT NULL = (count of PO-attached threads with non-NULL po.delivery_partner_id).

- [ ] **Task 3** — Update `packages/shared/src/db-types.ts` — `OrderSupplierThreadRow` ADD 5 fields:
  ```ts
  delivery_partner_id: string | null;
  confirm_delivery_date: string | null;
  request_for_delivery_at: string | null;
  partner_accepted_at: string | null;
  partner_rejected_at: string | null;
  ```
  Comment cites migration 0049. PurchaseOrderRow stays unchanged (until Sprint C drops cols).

- [ ] **Task 4** — Update `packages/shared/src/domain.ts` — `OrderSupplierThread` camelCase mirror.
  Update `packages/shared/src/adapters.ts` — bidirectional mapping for the 5 fields.
  Update `packages/shared/src/index.ts` — add type exports if not already covered.
  Acceptance: `pnpm --filter @carres/shared test` green (113+ baseline).

- [ ] **Task 5** — Sprint A reflection: write 1-paragraph note in preflight-notes about backfill row counts vs expectation. Commit Sprint A as one commit `feat(threads): add customer-leg columns + backfill (0049+0050)`.

---

## Sprint B — RPC rewrites (`0051_logistics_rpcs_chunk2.sql`)

Goal: rewrite the 8 RPCs that touch customer-leg fields so they read from thread instead of PO. PO columns still exist (drops in Sprint C) — RPCs become the single source of truth.

**Critical**: RPCs are SECURITY DEFINER → write a self-contained 0051 migration. Same pattern as 0045. Each rewrite must keep argument signatures stable EXCEPT where moving from `p_po_id` to `p_thread_id` (some RPCs change identity).

### RPCs to rewrite

1. `logistics_dispatch_customer_leg(p_thread_id uuid, ...)` — was `p_po_id text` in 0045:480. New signature takes thread ID. Writes RFD state to thread row.
2. `logistics_partner_accept_rfd(p_thread_id uuid)` — same signature change.
3. `logistics_partner_reject_rfd(p_thread_id uuid, p_reason text)` — same.
4. `logistics_resume_dispatch_from_waiting(p_thread_id uuid)` — same.
5. `logistics_assign_partner_and_dispatch(p_po_id text, ..., p_procurement_partner_id uuid)` — rename arg from `p_partner_id` to `p_procurement_partner_id` for clarity. Writes to `purchase_orders.procurement_partner_id` (read column still exists at this point as alias).
6. `lp_accept_inbound_delivery(p_po_id text)` — reads `procurement_partner_id` for partner-role gate (rename internal var only).
7. `lp_reject_inbound_delivery(p_po_id text, ...)` — same.
8. `logistics_receive_po_with_do(...)` — partner-role gate reads `procurement_partner_id` (rename).

- [ ] **Task 6** — Author `supabase/migrations/0051_logistics_rpcs_chunk2.sql` with all 8 RPCs above as `CREATE OR REPLACE`. Each function body inlined (no shared helper changes). Apply via MCP.
  Acceptance: `select count(*) from pg_proc where proname in (...)` = 8; calling `logistics_dispatch_customer_leg(p_thread_id := <existing>)` updates `order_supplier_threads.request_for_delivery_at`.

- [ ] **Task 7** — Update Hono routes that wrap these RPCs:
  - `apps/api/src/routes/partner/pickups.ts` — `acceptRfd` / `rejectRfd` pass `threadId`, not `poId`. Body shape: `{ threadId: string }` instead of `{ poId: string }`.
  - `apps/api/src/routes/logistics/dispatch-customer-leg.ts` — same: body takes `threadId`, route param is the order or thread.
  - `apps/api/src/routes/logistics/resume-dispatch.ts` — same.
  Update zod schemas in `packages/shared/src/schemas/logistics.ts` accordingly.
  Acceptance: api test suite green (336 → 336+ as new tests added).

- [ ] **Task 8** — Update Hono routes that READ `purchase_orders.delivery_partner_id` (will be renamed to `procurement_partner_id` in Sprint C, but the read SHAPE today still works):
  - Search-and-replace `delivery_partner_id` → `procurement_partner_id` in `apps/api/src/routes/partner/dashboard.ts` (partner JWT scope).
  - Same in `apps/api/src/routes/partner/pickups.ts` SELECT clauses.
  - Same in `apps/api/src/routes/logistics/orders.ts` partner filter.
  - These files now read the column under its new name; Sprint C migration applies the actual rename.

- [ ] **Task 9** — Update frontend reads:
  - `apps/web/src/pages/logistics/components/OrderCard.tsx` — read `thread.delivery_partner_id` not `po.delivery_partner_id`. Adjust the "assigned LP" pill source.
  - `apps/web/src/pages/logistics/components/OrderDetailDrawer.tsx` — same.
  - `apps/web/src/lib/queries.ts` — query type updates if needed.
  Acceptance: web test suite green (276 → 276+).

- [ ] **Task 10** — Write integration tests for thread-based RFD flow:
  - `apps/api/src/routes/logistics/dispatch-customer-leg.test.ts` — assert `threadId` is the new arg + RPC called with `p_thread_id`.
  - Update existing `apps/api/src/routes/partner/pickups.test.ts` to match new shape.
  Acceptance: ≥6 new test cases.

- [ ] **Task 11** — Sprint B verification + commit
  Tests: shared + api + web all green. Typecheck clean (modulo pre-existing carry-forward).
  Commit message: `feat(rpcs): chunk-2 RPC rewrites — customer-leg fields read from thread (0051)`.
  Tag: `phase-4.5-chunk-2-sprint-b-complete` (lightweight tag for rollback granularity).

---

## Sprint C — PO column drops (IRREVERSIBLE — Loo gate)

Goal: rename `purchase_orders.delivery_partner_id → procurement_partner_id` and DROP the 4 customer-leg timestamp columns. After this point there's no easy rollback to "two-leg same LP" model.

- [ ] **Task 12** — STOP. Pre-flight verification BEFORE writing 0052:
  - `grep -r "po.delivery_partner_id\|po\.partner_accepted_at\|po\.partner_rejected_at\|po\.request_for_delivery_at\|po\.confirm_delivery_date" apps packages` → must return 0 results
  - `grep -r "delivery_partner_id" supabase/migrations/0049 0050 0051` → confirm only thread references remain
  - Run full test suite — all green
  Acceptance: produce a 5-line "no remaining PO customer-leg readers" preflight report.

- [ ] **Task 13** — STOP. Surface to Loo before applying 0052:
  ```
  Migration 0052 is IRREVERSIBLE per CLAUDE.md §14 #1.
  - Will RENAME purchase_orders.delivery_partner_id → procurement_partner_id
  - Will DROP 4 columns: confirm_delivery_date, request_for_delivery_at,
    partner_accepted_at, partner_rejected_at
  - Will DROP partial index po_rfd_pending_idx
  Approve to proceed?
  ```
  Wait for explicit "yes" / "go" / "确认" before continuing. Do NOT auto-decide.

- [ ] **Task 14** — Write `supabase/migrations/0052_purchase_orders_drop_customer_leg.sql`:
  ```sql
  ALTER TABLE purchase_orders RENAME COLUMN delivery_partner_id TO procurement_partner_id;
  -- Drop CHECK constraint (was named po_outsource_xor_partner) and recreate against new name
  ALTER TABLE purchase_orders DROP CONSTRAINT IF EXISTS po_outsource_xor_partner;
  ALTER TABLE purchase_orders ADD CONSTRAINT po_outsource_xor_procurement_partner
    CHECK ((procurement_partner_id IS NULL) <> (outsource_partner_name IS NULL));
  ALTER TABLE purchase_orders
    DROP COLUMN IF EXISTS confirm_delivery_date,
    DROP COLUMN IF EXISTS request_for_delivery_at,
    DROP COLUMN IF EXISTS partner_accepted_at,
    DROP COLUMN IF EXISTS partner_rejected_at;
  DROP INDEX IF EXISTS po_rfd_pending_idx;
  ```
  Apply via MCP after Loo's explicit OK from Task 13.
  Acceptance: `\d purchase_orders` shows `procurement_partner_id` + 0 of the 4 dropped columns.

- [ ] **Task 15** — Update db-types + domain + adapters for renamed column:
  `PurchaseOrderRow.delivery_partner_id → procurement_partner_id`. Drop the 4 customer-leg fields.
  `PurchaseOrder` (domain) camelCase same shape.
  Acceptance: `pnpm --filter @carres/shared test` green; `pnpm --filter @carres/web typecheck` clean.

- [ ] **Task 16** — Sprint C verification + commit
  Run full test suite. Commit `feat(po): drop customer-leg cols + rename procurement_partner_id (0052)`.

---

## Sprint D — Stockpile thresholds + alerts

Goal: ship migration 0053 + alert RPC + dashboard tile + warehouse-page threshold edit + CreatePOModal "Suggest from alerts" button.

- [ ] **Task 17** — Write + apply `supabase/migrations/0053_stock_balances_thresholds.sql`:
  ```sql
  ALTER TABLE stock_balances
    ADD COLUMN IF NOT EXISTS low_threshold  int CHECK (low_threshold IS NULL OR low_threshold >= 0),
    ADD COLUMN IF NOT EXISTS high_threshold int CHECK (high_threshold IS NULL OR high_threshold >= 0);
  ALTER TABLE stock_balances
    ADD CONSTRAINT stock_balances_threshold_order
      CHECK (low_threshold IS NULL OR high_threshold IS NULL OR high_threshold >= low_threshold);
  ```
  Plus the RPC `logistics_stock_alerts()` returning `(sku, warehouse_id, qty, reserved, effective, low_threshold, shortage)` rows where `(qty - reserved) < low_threshold`.

- [ ] **Task 18** — Hono route `apps/api/src/routes/logistics/stock-alerts.ts` + test
  `GET /api/logistics/stock-alerts` calls the RPC. Logistics-only role guard. Returns `{ alerts: [] }`.
  Acceptance: ≥3 test cases (200 happy, 403 non-logistics, empty result).

- [ ] **Task 19** — Hono route `apps/api/src/routes/logistics/thresholds.ts` + test
  `POST /api/logistics/warehouses/:warehouseId/skus/:sku/threshold` body `{ low: int|null, high: int|null }`. UPDATEs stock_balances row (or INSERTs if missing).
  Acceptance: ≥4 test cases (set low only, set both, clear both, validation error).

- [ ] **Task 20** — `SetThresholdDialog.tsx` component
  Triggered from inline edit on LogisticsWarehouse row. Two number inputs + Save. Calls the threshold endpoint.
  Acceptance: ≥3 component tests.

- [ ] **Task 21** — `StockAlertsTile.tsx` component + LogisticsDashboard wire-up
  Tile shows count + top-3 alert SKUs. Click navigates to `/logistics/warehouse?alert=true`.

- [ ] **Task 22** — CreatePOModal extend with "Suggest from alerts"
  Button alongside existing "Auto-fill from awaiting stock". Pre-populates lines with `qty = (high_threshold || low_threshold * 2) - effective`.
  Acceptance: 1 new test case.

- [ ] **Task 23** — Sprint D commit + verify
  Commit `feat(stockpile): low/high thresholds + alerts RPC + UI (0053)`.

---

## Sprint E — COGS (cost + cost_source ENUM)

- [ ] **Task 24** — Write + apply `supabase/migrations/0054_purchase_order_lines_cogs.sql`:
  ```sql
  CREATE TYPE cost_source_enum AS ENUM ('hand_entered', 'prev_po', 'system_suggested');
  ALTER TABLE purchase_order_lines
    ADD COLUMN IF NOT EXISTS cost numeric(14,2) CHECK (cost IS NULL OR cost >= 0),
    ADD COLUMN IF NOT EXISTS cost_source cost_source_enum;
  ```
  No backfill — legacy rows stay NULL per CQ3.

- [ ] **Task 25** — Update `packages/shared/src/schemas/logistics.ts` — `createPoInput.lines[]` extends:
  ```ts
  cost: z.number().nonnegative(),
  costSource: z.enum(['hand_entered', 'prev_po', 'system_suggested']),
  ```
  Update `createPosBatchInput` accordingly. Update `db-types.ts` `PurchaseOrderLineRow` adds `cost: number | null` + `cost_source: CostSource | null`.

- [ ] **Task 26** — Update `logistics_create_po` + `logistics_create_pos_batch` RPCs in `0054_*.sql` (or a follow-up `0054b_*.sql`):
  Add validation: `IF p_cost IS NULL OR p_cost_source IS NULL THEN RAISE EXCEPTION ... USING ERRCODE='22023', DETAIL='cost_required'`. Persist `cost` + `cost_source` per line.

- [ ] **Task 27** — Hono route `apps/api/src/routes/logistics/recent-cost.ts` + test
  `GET /api/logistics/skus/:sku/recent-cost` returns `{ cost: number | null, lastPoId: string | null, lastReceivedAt: string | null }`.

- [ ] **Task 28** — `CogsLineEditor.tsx` component (sub-component used inside CreatePOModal)
  Per-line: cost input + dropdown for cost_source. "prev_po" auto-fills via the recent-cost RPC. "system_suggested" computes 110% of prev or shows "no historical data — please hand-enter".

- [ ] **Task 29** — Update CreatePOModal to use CogsLineEditor; update existing tests for new line shape.

- [ ] **Task 30** — Sprint E commit
  `feat(cogs): cost + cost_source on PO lines (0054)`.

---

## Sprint F — Tab UI shell + Phase 6 supplier prep

- [ ] **Task 31** — Write + apply `supabase/migrations/0055_suppliers_phase6_prep.sql`:
  ```sql
  ALTER TABLE suppliers
    ADD COLUMN IF NOT EXISTS portal_enabled boolean NOT NULL DEFAULT false,
    ADD COLUMN IF NOT EXISTS contact_email  text;
  ```

- [ ] **Task 32** — `packages/shared/src/sops.ts` — add `PROCUREMENT_TAB_SLUGS`:
  ```ts
  export const PROCUREMENT_TAB_SLUGS = ['nice-future', 'hookka-sofa', 'hookka-bedframe'] as const;
  export type ProcurementTabSlug = typeof PROCUREMENT_TAB_SLUGS[number];
  export function deriveProcurementSlug(supplierSlug: string, category: string): ProcurementTabSlug | null {
    if (supplierSlug === 'nice-future') return 'nice-future';
    if (supplierSlug === 'hookka' && category === 'sofa') return 'hookka-sofa';
    if (supplierSlug === 'hookka' && category === 'bedframe') return 'hookka-bedframe';
    return null;
  }
  ```
  + tests.

- [ ] **Task 33** — Hono route `apps/api/src/routes/logistics/procurement-tabs.ts` + test
  `GET /api/logistics/procurement/{slug}` filters PO list by supplier_slug + category. Validate `:slug` against the enum (422 on invalid).

- [ ] **Task 34** — Frontend route shell `apps/web/src/pages/logistics/procurement/TabbedProcurementShell.tsx` + 3 child tabs.
  Each child renders a per-channel slice of the current LogisticsProcurement page.
  Acceptance: navigating to `/logistics/procurement/hookka-sofa` shows only HoOKkA Sofa POs.

- [ ] **Task 35** — Update `apps/web/src/App.tsx` with nested routes (per spec §4.1).

- [ ] **Task 36** — Migrate existing `LogisticsProcurement.test.tsx` tests to per-tab test files. Most tests partition cleanly by supplier; shared tests stay on shell.

- [ ] **Task 37** — Sprint F commit
  `feat(ui): per-supplier tab routing + suppliers Phase 6 prep (0055)`.

---

## Sprint G — E2E + reflection + tag

- [ ] **Task 38** — Write 3 Playwright E2E specs as `test.fixme()` (Loo un-fixmes locally):
  - `e2e/per-leg-lp-split.spec.ts` — assigns Partner X procurement, Partner Y delivery; assert each sees only their leg
  - `e2e/tab-routing.spec.ts` — direct URL access to each `/procurement/{slug}` works; back/forward preserves
  - `e2e/stockpile-alert-to-po.spec.ts` — set threshold; receive shortage; alert appears; "Suggest from alerts" pre-fills CreatePOModal

- [ ] **Task 39** — Reflection doc `docs/phase-4.5-chunk-2-reflection.md`:
  Estimated vs actual time. Migration order surprises. Drift between spec v1 + actual schema. F1-F12-equivalent fixes (if any). Top 3 lessons.

- [ ] **Task 40** — Update CLAUDE.md §17:
  Test count → 770/770 (or actual). Migration count → 56 files. Tag mention `phase-4.5-chunk-2-complete`. 4 new closures + new chunk-2 carry-forwards.

- [ ] **Task 41** — Final commit + tag
  Squash-merge or single rebase commit `feat(chunk-2): per-leg LP split + tab UI + stockpile + COGS (Phase 4.5 complete)`. Annotated tag `phase-4.5-chunk-2-complete`. Push tag + commit to origin (Loo confirms first).

---

## Acceptance criteria (overall)

- All 7 migrations (0049–0055) applied to staging Supabase project_id `kfprgpjpaffedghytstl`
- `pnpm -r test` green: shared 117+ → ~125, api 338+ → ~360, web 276+ → ~290 — total 731 → ~770
- `pnpm typecheck` clean (modulo pre-existing `phase-4-orders-test-logistics-stage-null-type-drift`)
- 3 E2E specs land as `test.fixme()`
- Phase 4.5 fully tagged: `phase-4.5-chunk-2-complete` annotated tag pushed
- 4 carry-forwards closed: `procurement-vs-delivery-partner-field-split`, `supplier-tab-ui-rewrite`, `stockpile-advanced-flows`, `phase-9-po-cogs-source`
- §17 reflects new state

---

## Risk notes (per spec §13)

- Migration 0052 is IRREVERSIBLE — Task 13 is the explicit Loo-approval gate. Do NOT skip.
- Sprint B's RPC rewrites change argument signatures (`p_po_id → p_thread_id` for 4 RPCs). Hono routes + tests update in lockstep.
- Sprint D's `logistics_stock_alerts()` is a new RPC — confirm it doesn't conflict with any existing function name in pg_proc.
- Per-tab UI test redistribution (Task 36) may surface rendering bugs in components that assumed the single-page LogisticsProcurement shell. Watch for hidden coupling.

---

## Next steps after Chunk 2 ships

- Phase 5 Finance kickoff (separate spec + plan)
- Open carry-forwards to triage in Phase 5 prep: `lp-whitelist-tighten`, `cleanup-old-partner-confirm-receive`, `cleanup-at-own-wh-waiting-rename`, etc.

---

**End of Phase 4.5 Chunk 2 plan. Estimated ~41 tasks across 7 sprints.**
