# Chunk 2 Pre-flight Audit Notes

## Sprint A close-out (2026-05-06)

Sprint A landed 5 commits on `main` covering the schema + db-types + camelCase mirror foundation for the per-leg LP split (CQ1 option (b)):

- `ff4a8cf` feat(threads): add customer-leg columns + partial index (0049)
- `573f48a` feat(threads): backfill customer-leg fields from PO (0050)
- `503b1b1` feat(db-types): OrderSupplierThreadRow add customer-leg fields (0049)
- `dae374b` feat(shared): OrderSupplierThread domain + adapters customer-leg fields (0049)
- `0c578d3` fix(shared): tighten OrderSupplierThread.logisticsStage to non-null per migration 0033 NOT NULL

Two migrations applied to staging Supabase project_id `kfprgpjpaffedghytstl`:
- **0049_threads_customer_leg_columns.sql** — 5 nullable cols on `order_supplier_threads` (`delivery_partner_id`, `confirm_delivery_date`, `request_for_delivery_at`, `partner_accepted_at`, `partner_rejected_at`) + partial index `ost_rfd_pending_idx` mirroring 0044's `po_partner_rfd_pending_idx` shape.
- **0050_threads_customer_leg_backfill.sql** — idempotent UPDATE pulling those 5 fields from the parent `purchase_orders` row.

**Backfill row count observation:** `0 rows updated`. Expected: staging has no live customer-facing threads on partner-tagged POs yet (LP test users still pending Loo's local seed from Chunk 1). Backfill is idempotent, so re-running post-seed will fill correctly.

## Surprises / drift from spec

- **T4 closed a pre-existing camelCase mirror gap inline.** The plan assumed `OrderSupplierThread` (camelCase domain interface) and `orderSupplierThreadFromRow` adapter already existed since Phase 4 v3-S4. They did not — only the snake_case `OrderSupplierThreadRow` Row type existed in `db-types.ts`. T4 created both the interface in `domain.ts` (+41 lines) and the adapter in `adapters.ts` (+26 lines, with `+2` round-trip tests), packaged in a single commit (`dae374b`) covering both the gap closure and the new customer-leg fields. Treated as a pre-existing gap closed in lockstep, not new scope.
- **T4 typing tightening follow-up.** Code-review caught `OrderSupplierThread.logisticsStage` widened to `LogisticsStage | null`, but migration 0033 line 51 declares the underlying column NOT NULL. Fixed in `0c578d3` by replacing the inline union with the named `LogisticsStage` import — re-aligns the camelCase mirror with schema reality.
- **T1 doc typo in 0049 header comment.** Header said the partial index "mirrors 0044's `po_rfd_pending_idx`" — actual sibling index name in 0044 is `po_partner_rfd_pending_idx` (verified at `supabase/migrations/0044_po_dispatch_fields.sql:27`). Comment-only drift, no runtime impact. Fixed in this Sprint A close-out commit.

## Deltas

- **db-types.ts**: +13 lines (5 new optional fields on `OrderSupplierThreadRow`)
- **domain.ts**: +41 lines (new `OrderSupplierThread` camelCase interface + 5 customer-leg fields)
- **adapters.ts**: +26 lines (new `orderSupplierThreadFromRow` adapter)
- **Test count**: 731 → 733 (+2 round-trip tests in `packages/shared/src/adapters.test.ts`)

## Final verification (Sprint A close-out)

```
pnpm -r test
  packages/shared  119 passed (119)
  apps/api         338 passed (338)
  apps/web         276 passed (276)
  Total            733 passed
```

```
pnpm typecheck
  packages/shared  Done
  apps/web         Done
  apps/api         Failed (1 error, expected carry-forward)
    src/routes/logistics/orders.test.ts(119,21): error TS2322
      logistics_stage: null vs string
      → carry-forward `phase-4-orders-test-logistics-stage-null-type-drift`
```

Only the pre-existing carry-forward at `apps/api/src/routes/logistics/orders.test.ts:119` is unresolved. No new typecheck errors introduced by Sprint A.

## Sprint A foundation summary

Schema columns ready (0049) + backfill landed and idempotent (0050) + camelCase mirror exists with adapter round-trip coverage. Sprint B can start from this clean foundation — RPC rewrites in 0051 will read/write threads.{delivery_partner_id, request_for_delivery_at, partner_accepted_at, partner_rejected_at, confirm_delivery_date} as the canonical store, with PO mirror-write deferred until Sprint C migration 0052 (the irreversible drop, gated on Loo's at-apply approval).

---

## Sprint B close-out (2026-05-06)

Sprint B landed **5 commits** on `main` covering customer-leg RPC rewrites + Hono route wrappers + frontend thread reads + integration test suite for the thread-based RFD flow:

- `a9ff78e` feat(rpcs): chunk-2 RPC rewrites — customer-leg fields read from thread (0051) — T6
- `804756a` feat(api): chunk-2 Hono routes wrap thread-based customer-leg RPCs (T7)
- `03ab830` feat(web): chunk-2 frontend reads customer-leg LP from thread (T9)
- `f82005e` refactor(web): chunk-2 T9 review fixes — non-nullable threads type + all-null test
- `7bc684d` test(api): chunk-2 integration tests for thread-based RFD flow (T10)

**RPC scope:** 4 customer-leg RPCs rewritten in `0051_logistics_rpcs_chunk2.sql` (`logistics_partner_accept_rfd`, `logistics_partner_reject_rfd`, `logistics_dispatch_customer_leg`, `logistics_resume_dispatch`). Procurement-leg RPCs (`logistics_assign_partner_and_dispatch`, `lp_accept_inbound_delivery`, `lp_reject_inbound_delivery`, `logistics_receive_po_with_do`) **deferred to Sprint C T13.5** in `0053_logistics_rpcs_chunk2_part2.sql` because their bodies need to land **after** the column rename in 0052. Master plan T8 ("rename `delivery_partner_id` → `procurement_partner_id` in API code") deferred to Sprint C T8' (post-0052) for the same reason.

**API zod schemas:** 4 new schemas added in T7 to `packages/shared/src/schemas/logistics.ts`:
- `partnerAcceptRfdInput`
- `partnerRejectRfdInput`
- `dispatchCustomerLegInput`
- `resumeDispatchInput`

## Test delta

- **Sprint A end → Sprint B mid (T6+T7):** 733 → 760 (+27 from RPC + Hono integration tests)
- **Sprint B mid → Sprint B end (T9 + T9 fix + T10):** 760 → 771 (+11)
- **Total Sprint B contribution:** +38 tests

Per-workspace counts (verified by running `pnpm --filter @carres/shared test` and full `pnpm -r test`):
- `packages/shared` 139 (was 119 at Sprint A end, **+20** — net of T7 schema additions)
- `apps/api` 352 (was 338 at Sprint A end, **+14** — T7 route handlers + T10 integration cases)
- `apps/web` 280 (was 276 at Sprint A end, **+4** — T9 added 3 OrderCard/Drawer tests + T9 review fix added 1 all-null test)

## Final verification (Sprint B close-out)

```
pnpm -r test
  packages/shared  139 passed (139)
  apps/api         352 passed (352)
  apps/web         280 passed (280)
  Total            771 passed
```

```
pnpm typecheck
  packages/shared  Done
  apps/web         Done
  apps/api         Failed (1 error, pre-existing carry-forward)
    src/routes/logistics/orders.test.ts(119,21): error TS2322
      logistics_stage: null vs string
      → carry-forward `phase-4-orders-test-logistics-stage-null-type-drift`
```

Only the pre-existing carry-forward at `apps/api/src/routes/logistics/orders.test.ts:119` is unresolved. No new typecheck errors introduced by Sprint B.

## Carry-forwards closed during Sprint B

- **`phase-4-detail-partner-name-join`** ✅ closed (bonus closure during T9). The OrderCard LP pill rendering surfaces partner UUID slug to the kanban, which is what the M5.0 carry-forward originally tracked. **Caveat:** the pill currently renders an 8-char UUID slug (`partner_id.slice(0, 8)`) rather than the partner's display name. Full partner-name resolution remains a separate concern from this carry-forward and is out of Chunk 2 scope.

## Carry-forwards opened during Sprint B

### `phase-4.5-chunk-2-route-mount-middleware-leak` (medium severity)

Opened during T10 spec review. Findings:

- Hono v4's `.route()` with overlapping mount paths shares middleware across sibling sub-routers (verified empirically during T10 spec review).
- In `apps/api/src/index.ts:57-59`, three sub-routers mount at `/logistics/pos`: `logisticsPosRouter`, `lpInboundRouter`, `dispatchCustomerLegRouter`. The first has a blanket `*` middleware that 403s anything except `role === "logistics"`.
- Result: the inline role allowlists in `dispatch-customer-leg.ts:40` (`["logistics","principal"]`) and `lp-inbound.ts:33` (`["logistics","principal","partner"]`) are partially dead — only `logistics` reaches them.
- Same pattern at `index.ts:54-55` for `logisticsOrdersRouter` + `resumeDispatchRouter`.
- Docstrings at `dispatch-customer-leg.ts:24-27` and `lp-inbound.ts:25-27` claim sibling-router middleware isolation — incorrect.

**Fix paths (pick one, deferred to a follow-up sprint):**
1. (a) Drop the redundant inline allowlists and treat the upstream blanket as the single source of truth (and update docstrings).
2. (b) Replace blanket `*` middleware with per-route middleware in `logisticsPosRouter`/`logisticsOrdersRouter` so siblings get clean middleware boundaries.
3. (c) Mount affected routers at non-overlapping paths.

**Severity rationale:** Medium. Not a security issue (more restrictive than intended — `principal` and `partner` are denied where the inline allowlist would have accepted them). But the advertised intent diverges from runtime behavior, which will trip the next person reading those docstrings.

### Bonus minor improvements (not opened as carry-forwards)

Two of the new 403/422 tests in T10 assert only `res.status` not body shape; could be tightened to `expect(body).toMatchObject({error: "forbidden"})` style. Footnoted here as low-priority test polish — not worth a dedicated carry-forward entry.

## Sprint B summary

Customer-leg RFD flow now reads and writes from `order_supplier_threads` end-to-end (DB RPC → Hono → frontend → tests). Procurement leg untouched (still reads `purchase_orders.delivery_partner_id`). The PO mirror columns (`request_for_delivery_at`, `partner_accepted_at`, `partner_rejected_at`, `confirm_delivery_date`) remain in place but unread by Sprint B code — Sprint C T14 drops them along with renaming the procurement column.

Tests green (771/771). Typecheck clean modulo the pre-existing carry-forward. Working tree clean before tag.

---

## Sprint C T12 preflight (2026-05-06)

Verified at commit `f1cf96c` on `main` (working tree clean). 5 PO columns checked: `delivery_partner_id` (will be RENAMED to `procurement_partner_id`), `confirm_delivery_date`, `request_for_delivery_at`, `partner_accepted_at`, `partner_rejected_at` (4 DROPPED).

Strict resume-plan grep `"po\.delivery_partner_id|po\.partner_accepted_at|po\.partner_rejected_at|po\.request_for_delivery_at|po\.confirm_delivery_date"` against `apps/web` + `packages/shared`: **0 matches** (apps/web), **0 matches** (packages/shared excluding `adapters.test.ts` which exercises `purchaseOrderFromRow` round-trip — expected per T15 cleanup whitelist). Broader sweep on bare `snake_case` + `camelCase` field reads in `apps/web` + `packages/shared` surfaces only: (a) `LogisticsOrderThreadRow` reads from embedded `order_supplier_threads` (added in 0049, not affected by 0052) — `OrderCard.tsx`, `OrderDetailDrawer.tsx`, kanban tests 25-28; (b) `LogisticsOrderListRow.delivery_partner_id` / `LogisticsOrderDetailOrder.delivery_partner_id` — these read `orders.delivery_partner_id` (different table from `purchase_orders`, untouched by 0052); (c) `PurchaseOrder` camelCase mirror in `domain.ts` / `adapters.ts` / `schemas/orders.ts` — expected, T15 amends after 0052 applies; (d) `PartnerPickupsPage.tsx` consumes the procurement-leg-flagged `GET /api/partner/pickups` endpoint, which resume-plan line 229 explicitly schedules for T8' (post-0052) — both the API SELECT and the FE row-shape will be migrated there in lockstep.

Verdict: ✅ NO LEAKS — safe to apply 0052. Sprint B baseline confirmed: `pnpm typecheck` shows only the pre-existing `phase-4-orders-test-logistics-stage-null-type-drift` carry-forward at `apps/api/src/routes/logistics/orders.test.ts:119` (unchanged from Sprint B close-out). Proceeding to T13 (Loo gate).

## Sprint C T13 IRREVERSIBLE pre-approval (2026-05-06)

[PRE-APPROVED 2026-05-06 §1] Migration 0052 IRREVERSIBLE (DROP COLUMN x4 +
RENAME COLUMN) authorized by Loo's in-conversation OK at session start.
See `docs/superpowers/plans/2026-05-06-phase-4.5-chunk-2-autonomous-run.md` §1
audit trail. Per CLAUDE.md §14 #1 + #7, this is the explicit single-instance
permission required to DROP `confirm_delivery_date`, `request_for_delivery_at`,
`partner_accepted_at`, `partner_rejected_at` and RENAME `delivery_partner_id`
→ `procurement_partner_id`. Original resume-plan T13 STOP-and-ask gate
CONVERTED to log-and-proceed per autonomous-run agenda §3. Proceeding to
T13.5 (author 0053).

## Sprint C T13.6 — Pre-0052 RPC + trigger body audit (2026-05-06)

Performed BEFORE applying 0052 to characterize the runtime risk Postgres
function bodies face when columns disappear. Postgres function bodies are
stored as plpgsql TEXT (`pg_proc.prosrc`); they don't recompile on
`ALTER TABLE … RENAME COLUMN` or `… DROP COLUMN`. Functions whose bodies
reference any of the 5 affected `purchase_orders` columns will fail at
CALL time after 0052 applies.

### Audit results

Two separate sweeps:
1. `grep -nE "^create or replace function" supabase/migrations/*.sql` —
   inventory every plpgsql definition.
2. `grep -nE "delivery_partner_id|confirm_delivery_date|request_for_delivery_at|partner_accepted_at|partner_rejected_at"` —
   pinpoint at-risk references. Each hit was disambiguated against table
   context (`orders.delivery_partner_id`, `order_supplier_threads.delivery_partner_id`
   are NOT at-risk — only `purchase_orders.*` is).

### Verdict (CREATE OR REPLACE semantics: latest-defining migration wins)

| Definer | Function | Active caller? | Already in 0053? | Verdict |
|---|---|---|---|---|
| 0019:836 | `logistics_assign_partner` | yes | n/a | ✅ SAFE — writes `orders.delivery_partner_id`, different table |
| 0019:1425 | `logistics_assign_pickup_partner` | NO (superseded by `logistics_assign_partner_and_dispatch`) | no | C — dead code |
| 0019:1503 | `logistics_reassign_po_warehouse` | yes (`pos.ts:621`) | n/a | ✅ SAFE — body has 0 at-risk refs |
| 0003:76 | `po_issue` | NO (superseded) | no | C — dead code |
| 0003:178 | `order_dispatch` | NO + different table | n/a | ✅ SAFE |
| 0034:274 | `logistics_supplier_ready_confirm` | yes | n/a | ✅ SAFE — body has 0 at-risk refs |
| 0034:366 | `partner_confirm_receive` | NO (already tracked as `phase-4.5-cleanup-old-partner-confirm-receive`) | no | C — dead code |
| 0034:1171 | `logistics_attach_pod_do` | listed in `sops.ts` but no Hono route + no FE; deferred to Phase 7 | no | C — dead-but-named-in-SOP |
| 0045:30 | `partner_accept_dispatch` | NO (superseded by 0051; `logistics.ts:307` says "now-dropped") | no | C — dead code |
| 0045:117 | `partner_reject_dispatch` | NO (superseded by 0051; `logistics.ts:325` says "now-dropped") | no | C — dead code |
| 0045:195 | `lp_accept_inbound_delivery` | yes | YES | ✅ A — covered by 0053 |
| 0045:267 | `partner_reject_customer` | yes | YES | ✅ A — covered by 0053 |
| **0045:351** | **`logistics_relocate_warehouse`** | **YES — `lp-inbound.ts:69`, `WarehouseRelocateDialog.tsx`, integration tests** | **NO** | **❌ B — extended 0053 with rewritten body (RPC #5)** |
| 0045:434 | `logistics_resume_from_waiting` | yes (referenced in SOP map; superseded in 0051 by `logistics_resume_dispatch_from_waiting` for thread scope) | n/a | ✅ SAFE — body has 0 at-risk refs |
| 0045:501 | `logistics_dispatch_customer_leg` | superseded by 0051:108 | n/a | ✅ SAFE — 0051 is the live def, no at-risk refs |
| 0045:614 | `logistics_receive_po_with_do` | yes | YES | ✅ A — covered by 0053 |
| **0046:64** | **`enforce_partner_po_column_whitelist` (TRIGGER FN)** | **YES — fires BEFORE UPDATE on `purchase_orders` for every UPDATE** | **NO** | **❌ B — extended 0053 with rewritten body (RPC #6)** |

### Hidden severity discovery

The trigger function `enforce_partner_po_column_whitelist` is the highest-
severity gap caught by this audit. The function body references
`NEW.delivery_partner_id` (renamed) AND `NEW.request_for_delivery_at`
(dropped). It fires on EVERY UPDATE to `purchase_orders` for every role —
not just LP. Without rewrite, after 0052 applies, ALL UPDATEs by every role
would fail at trigger fire time because Postgres can't resolve the dropped
+ renamed `NEW.<col>` against the post-0052 row type. The trigger short-
circuits early (line 1: `IF role <> 'partner' THEN RETURN NEW`), but
Postgres still validates the entire function body at trigger fire time.
Adding RPC #6 to 0053 closes this.

### 0053 file extension summary

`supabase/migrations/0053_logistics_rpcs_chunk2_part2.sql` extended from 4 RPCs
to **6 entries** (5 RPCs + 1 trigger function):

1. `logistics_assign_partner_and_dispatch` (existing, unchanged)
2. `lp_accept_inbound_delivery` (existing, unchanged)
3. `partner_reject_customer` (existing, unchanged)
4. `logistics_receive_po_with_do` (existing, unchanged)
5. `logistics_relocate_warehouse(text, uuid)` — APPENDED (105 lines).
   Body lifted verbatim from 0045:351-425; 5 column-ref swaps per file
   header. Local var renamed `v_new_delivery_partner` →
   `v_new_procurement_partner` for readability (internal-only). jsonb output
   key `new_delivery_partner_id` → `new_procurement_partner_id`. Existing FE
   integration tests + Hono route in `lp-inbound.ts` will need test fixture
   updates in T15 to track the new jsonb key.
6. `enforce_partner_po_column_whitelist()` — APPENDED (51 lines).
   Body lifted from 0046:64-100; one column ref swapped
   (`NEW.delivery_partner_id` → `NEW.procurement_partner_id`); one column ref
   removed (`NEW.request_for_delivery_at` line — column dropped). RAISE
   message updated to reflect new whitelist. Trigger
   `partner_po_update_whitelist_trg` from 0046:103 stays bound by function
   NAME, so CREATE OR REPLACE preserves the binding (no DROP/CREATE TRIGGER
   needed).

### Carry-forward opened: `phase-4.5-chunk-2-stale-pre-0051-rpcs`

The 6 Category C functions in the table above (`logistics_assign_pickup_partner`,
`po_issue`, `partner_confirm_receive`, `logistics_attach_pod_do`,
`partner_accept_dispatch`, `partner_reject_dispatch`) all have bodies that
will fail at hypothetical CALL time post-0052. Cleanup deferred — no live
caller, no risk. A future sprint will either (a) DROP the functions outright
or (b) rewrite the bodies with column-name swaps if they're needed for
backwards-compat. Tracked via this carry-forward so we don't forget when
Phase 7 wires `logistics_attach_pod_do` (which IS named in `sops.ts` SOP map
for the `dispatched -> delivered` transition).

The trigger-function fix (RPC #6) implicitly tightens the LP whitelist to a
single allowed column (`customer_rejection`) — the original 4-allowed set
included three columns that 0052 drops. The error message no longer mentions
the dropped columns. This subtly tightens LP write surface, but the LP role
no longer touches PO at all in Chunk 2 (customer-leg RFD moved to threads),
so functionally a no-op for current LP code paths.

Verdict: ✅ Runtime-safe to apply 0052 + 0053 in sequence per T14. All
ACTIVE callers are covered.

## Sprint C T14 IRREVERSIBLE migration apply (2026-05-06)

Both migrations applied to staging Supabase project_id `kfprgpjpaffedghytstl` in
sequence. T14 ships under the §1 pre-approval. CLAUDE.md §14 #1 + #7 satisfied.

### 0052 first-attempt failure + recovery

First `apply_migration` of 0052 failed with `23514: check constraint
"po_outsource_xor_procurement_partner" of relation "purchase_orders" is
violated by some row`. Root cause: the resume-plan T14 SQL spec encoded a
strict XOR (`(procurement_partner_id IS NULL) <> (outsource_partner_name IS
NULL)`) — but staging holds 4 of 6 PO rows with BOTH columns NULL (legitimate
pre-assign state — see assign RPC sets one of the two on first dispatch).
Original 0030:75 used the looser "at most one" semantics
(`(outsource_partner_name IS NULL) OR (delivery_partner_id IS NULL)`) which
permits both-NULL. Migration tx aborted atomically; verified no schema state
drift via `pg_constraint` + `information_schema.columns` + `pg_indexes`
(column still `delivery_partner_id`, original `po_outsource_xor_partner`
intact, `po_partner_rfd_pending_idx` intact).

Fix: align the new constraint to the original "at most one" semantics
(swap `<>` for `OR`). The runtime XOR enforcement stays in the
`logistics_assign_partner_and_dispatch` RPC body (see 0053 entry #1 lines
129-133). Table-level CHECK is the wider invariant; RPC is the narrower
runtime gate.

### Apply sequence (post-fix)

1. **0052** — applied as `purchase_orders_drop_customer_leg`. Effects:
   - `DROP INDEX po_partner_rfd_pending_idx` (pre-emptive — partial-index
     predicate referenced 3 of 4 columns being dropped).
   - `RENAME COLUMN delivery_partner_id -> procurement_partner_id`.
   - `DROP CONSTRAINT po_outsource_xor_partner` then
     `ADD CONSTRAINT po_outsource_xor_procurement_partner` with
     "at-most-one" semantics.
   - `DROP COLUMN` (4): `confirm_delivery_date`, `request_for_delivery_at`,
     `partner_accepted_at`, `partner_rejected_at`.
2. **0053** — applied as `logistics_rpcs_chunk2_part2`. Effects:
   - `CREATE OR REPLACE FUNCTION` for 5 RPCs (`logistics_assign_partner_and_dispatch`,
     `lp_accept_inbound_delivery`, `partner_reject_customer`,
     `logistics_receive_po_with_do`, `logistics_relocate_warehouse`).
   - `CREATE OR REPLACE FUNCTION` for `enforce_partner_po_column_whitelist`
     trigger function (preserves trigger binding by NAME).
   - `REVOKE` + `GRANT EXECUTE` to `authenticated` on the 5 RPCs.

### Post-apply schema verification (raw SQL output)

```sql
SELECT column_name FROM information_schema.columns
 WHERE table_schema='public' AND table_name='purchase_orders'
   AND column_name IN ('delivery_partner_id', 'procurement_partner_id',
                       'confirm_delivery_date', 'request_for_delivery_at',
                       'partner_accepted_at', 'partner_rejected_at');
-- Returned 1 row: procurement_partner_id

SELECT proname FROM pg_proc WHERE proname IN (...);  -- 6 names
-- Returned 6 rows: enforce_partner_po_column_whitelist,
--   logistics_assign_partner_and_dispatch, logistics_receive_po_with_do,
--   logistics_relocate_warehouse, lp_accept_inbound_delivery,
--   partner_reject_customer

SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
 WHERE conrelid='public.purchase_orders'::regclass AND contype='c';
-- Returned: po_outsource_xor_procurement_partner,
--   CHECK (((outsource_partner_name IS NULL) OR (procurement_partner_id IS NULL)))

SELECT indexname FROM pg_indexes
 WHERE schemaname='public' AND tablename='purchase_orders' AND indexname LIKE 'po_partner%';
-- Returned: po_partner_idx (existing 0001 index, auto-tracking the renamed
--   column). po_partner_rfd_pending_idx is gone.
```

✅ All expected. Schema state matches T14 spec.

### Drift from resume-plan SQL

The resume plan §Sprint C Task 14 (lines 180-191) literal SQL used strict
XOR for the constraint. The applied 0052 uses "at most one" (OR semantics)
to match production data state and original 0030:75 invariant. Future
spec amendments should update line 185 to reflect this. Carry-forward not
opened — the docstring above 0052's constraint section explains the
rationale inline.

## Sprint C close-out (2026-05-06)

3 commits delivered Sprint C:
- `92f3dd6` feat(po): drop customer-leg cols + rename procurement_partner_id (0052+0053) — T14
- `14f29a4` feat(shared): types track procurement_partner_id rename (0052) — T15
- `a152fa8` refactor(api,web): rename delivery_partner_id → procurement_partner_id in PO references (T8')

### Migrations applied to staging Supabase project_id `kfprgpjpaffedghytstl`

- **0052** IRREVERSIBLE — RENAME `purchase_orders.delivery_partner_id` → `procurement_partner_id`, DROP 4 customer-leg cols (`confirm_delivery_date`, `request_for_delivery_at`, `partner_accepted_at`, `partner_rejected_at`), DROP partial index `po_partner_rfd_pending_idx`, swap CHECK constraint name. **Constraint predicate amended** from spec's strict XOR (`<>`) to original 0030:75 at-most-one (`OR`) to accept staging's 4 rows in legitimate pre-assign state. Pre-approval cited per §1.
- **0053** — 6 entries: 4 procurement-leg RPC bodies updated to read `procurement_partner_id`, plus `logistics_relocate_warehouse` rewrite (catches active warehouse-relocate flow), plus `enforce_partner_po_column_whitelist` trigger fn rewrite (catches every UPDATE on purchase_orders for every role). T13.6 audit prevented silent breakage.

### Sprint C tests + typecheck

Pre-Sprint-C baseline: 771/771 green (Sprint B close-out).
Post-Sprint-C: 771/771 green (no test count change — type renames don't add tests; T8' replaced one test assertion in PartnerPickupsPage.test.tsx with an equivalent shape but kept count).

Typecheck: clean modulo pre-existing carry-forward `phase-4-orders-test-logistics-stage-null-type-drift` at `apps/api/src/routes/logistics/orders.test.ts:119` (unchanged from Sprint B).

### Carry-forwards opened in Sprint C

1. **`phase-4.5-chunk-2-stale-pre-0051-rpcs`** (from T13.6 audit) — 6 dead-code Category C functions whose bodies now reference dropped/renamed cols and would fail at hypothetical CALL time. No live caller. Cleanup deferred. Notable members: `partner_accept_dispatch` (0045:30), `partner_reject_dispatch` (0045:117), `logistics_assign_pickup_partner` (0019:1425), `partner_confirm_receive` (0034:366), `logistics_attach_pod_do` (0034:1171 — revisit when Phase 7 wires `dispatched → delivered` SOP), `po_issue` (0003:76).

2. **`phase-4.5-chunk-2-partner-rfd-page-rebuild`** (from T8' observed during partner page changes) — `apps/web/src/pages/partner/PartnerPickupsPage.tsx` lost its RFD-pending indicator + Accept/Reject dialog launcher when the 4 customer-leg PO fields disappeared. The page still lists procurement-leg POs with sup_status chips, but the customer-leg RFD UI for partners now needs to be rebuilt as a thread-aware page (use the existing `{ threadId }` body endpoints `/api/partner/pickups/:threadId/accept-rfd` + `/api/partner/pickups/:threadId/reject-rfd` from T7). Sprint D-onwards or post-Chunk-2 follow-up.

3. **`phase-4.5-chunk-2-dispatch-dialog-wiring-drift`** (pre-existing, surfaced during T8' grep) — `apps/web/src/pages/logistics/components/DispatchPartnerDialog.tsx` sends snake_case body + uses old `:id/dispatch-customer-leg` URL; `apps/web/src/pages/partner/components/PartnerRequestForDeliveryDialog.tsx` calls `:po/accept-rfd` not the new `accept-rfd` route. Pre-existing Chunk 1 wiring inconsistencies. Should be cleaned up before Phase 5 Finance kickoff.

4. **`phase-4.5-chunk-2-seed-sql-stale`** (pre-existing, surfaced during T8' grep) — `supabase/seed.sql` references old PO column names + already-broken `sku`/`qty` columns dropped by 0017. Stale seed file. Out of T8' scope but worth fixing before any new dev needs to seed staging.

### Sprint C → Sprint D handoff

Sprint D opens with stockpile thresholds + alerts (T17-T23), additive only — no IRREVERSIBLE migrations remaining in Chunk 2. Next migration number: `0054_stock_balances_thresholds.sql`.
