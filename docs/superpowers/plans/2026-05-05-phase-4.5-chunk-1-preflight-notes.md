# Chunk 1 Pre-flight Audit Notes

## Pre-flight query results
- at_own_wh_waiting rows: 0 (expected 0) — PASS
- partner role users: 1 (expected 0 — LP role dormant before Chunk 1) — UNEXPECTED, but **explained by seed.sql, not blocking**

### Notes on the partner-role count discrepancy
The single partner-role row is the dev seed account, not a real production partner:
- `app_users.id` = `11111111-1111-1111-1111-000000000007`
- `email` = `partner@carres.com`
- `name` = "JT Express dispatcher"
- `partner_id` = `00000000-0000-0000-0000-0000000000f1` (linked to seeded `delivery_partners` row "JT Express Logistics")
- `created_at` = `2026-05-02 06:31:13` (project bootstrap day, not recent)

Source confirmed in `supabase/seed.sql`:
- Line 348-350: auth.users insert for `partner@carres.com` (password "111")
- Line 383: app_users insert for the partner role with the same UUID

There are 2 PO references to the seeded partner_id `00000000-0000-0000-0000-0000000000f1` (also seed data, expected). No production partner activity on staging yet.

The plan's "expected 0" assumption did not account for `seed.sql` having pre-seeded a partner test account. Functionally this is **safe for Chunk 1** because:
1. The role is wired via `app_role` JWT claim, but no Hono routes accept partner role yet.
2. Migration 0041 only adds CHECK + INDEX on `app_users.partner_id`; the seeded row already has `partner_id NOT NULL`, so the CHECK will pass.
3. RLS policies for partner role were added in 0033 (`ost_partner_read`) and the auth helper in 0029 (`app_partner_id()`); both already work against this seed row, just had no production traffic.

**Recommendation to controller:** proceed with Chunk 1 as planned. The seed account is the platform-shipped LP test fixture, not unmanaged drift.

## Cross-references confirmed
- 0034:366-451 partner_confirm_receive — advances threads to 'dispatched'; Chunk 1 writes NEW lp_accept_inbound_delivery for Sofa pre-flight (Codex F1)
- 0034:467 partner_reject_customer — strict p_reason ≥4 chars; Chunk 1 EXTENDS to make optional (C1.9)
- 0034:584 logistics_relocate_warehouse — Chunk 1 EXTENDS for pre-receive semantic + F9 invariant
- 0034:730 logistics_receive_po_with_do — Chunk 1 MODIFIES for Sofa Reject branch
- 0034:938 sets sup_status='delivered' on full receive — Chunk 1 normal path matches this; Sofa Reject path uses 'at_warehouse_waiting' from 0043
- 0036:91 rolls 'waiting' as 'dispatched' — Chunk 1 0047 amends to drop 'waiting' from that CASE (Codex F2)
- 0033:96 ost_partner_read on threads — already exists; Chunk 1 0046 does NOT add partner_sees_own_threads (Codex F8)
- 0001:141 app_users.partner_id — already exists; Chunk 1 0041 only adds CHECK + INDEX
- 0001 stock_movements columns: id, sku, warehouse_id, qty, kind, ref, note, by_role, by_user_id, occurred_at — Chunk 1 0046 joins via `ref` text (Codex F5)
- 0001 purchase_orders.dl int → orders.dl — Chunk 1 0046 trigger references NEW.dl (Codex F6)
- po_sup_status enum: pending, acknowledged, in_production, shipped, delivered, ready_for_pickup, pickup_assigned, pickup_accepted, picked_up, reassign_needed, ready_confirm_sent, partner_confirmed, customer_rejected, relocated, at_partner_wh, at_own_wh_waiting (NO 'received') — Chunk 1 receive RPC sets 'delivered' on normal, 'at_warehouse_waiting' on Reject (Codex F3)

## Cross-reference verification details
Each reference was spot-read in the migrations directly and confirmed against the cited file:line. The 0001 file is named `0001_init.sql` (not `0001_initial_schema.sql`); enum lives at line 30, partner_id column at line 141, stock_movements columns at line 221-232, orders.dl at line 243, purchase_orders.dl FK at line 324, sup_status default at line 330, delivery_partner_id at line 331. All match what Chunk 1 plan/spec assume.

The 6 v3 po_sup_status values (`ready_confirm_sent` … `at_own_wh_waiting`) are added additively in 0030_po_sup_status_v3.sql:50-55 — base 0001 enum has only 10 v2 values, no `'received'` value anywhere in either layer. Codex F3 stands.

0034 RPC line numbers verified:
- `partner_confirm_receive` declaration at line 366; threads advanced to 'dispatched' at lines 421-424.
- `partner_reject_customer` declaration at line 467; reason length guard at line 497-500.
- `logistics_relocate_warehouse` declaration at line 584.
- `logistics_receive_po_with_do` declaration at line 730; full-receive branch sets sup_status='delivered' at line 938.

0036 rollup `bool_and(... 'dispatched', 'delivered', 'waiting') -> 'dispatched'` confirmed at line 91.

0033 `ost_partner_read` partner thread read RLS confirmed at line 96-107 (existed since v3-S4, no addition needed in Chunk 1 0046 — Codex F8 is correct).
