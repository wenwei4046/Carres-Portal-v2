# AutoCount (retired reference — behavior heritage only)
## Copy ✅
- Vocabulary discipline (see docs/COPY-STANDARD.md: "Report" singular, "Outstanding" printed, "Ordered"/"Received").
- Explicit export wording ("Export Excel", not "Export").
- Grid heritage our operators already know: header sort toggle, per-column filter, Best Fit.
## Don't copy ❌
- The UI chrome (dated; 2990 already modernized it).
- Remark 1–4 free-text columns — this is the disease that created the Google-Sheet workflow. NEVER add free remark columns to a register.
- Layout Manager (team saved one layout in years).
- AutoCount itself is RETIRED as a data source (owner ruling): no import paths, no CSV compensation features.
