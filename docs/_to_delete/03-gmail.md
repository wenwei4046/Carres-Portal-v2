# Gmail (desktop)
## Copy ✅
- Selection model: checkbox column + select-all dropdown; toolbar becomes CONTEXTUAL bulk bar on selection ("N selected · actions").
- Reading pane (right-of-list) = our Detail Panel precedent; list stays usable.
- Filter chips: active conditions shown as dismissable chips; the chip row does not exist when nothing is filtered.
## Don't copy ❌
- Search-only model (no columns/sort — mail is not a register).
- Its bulk actions are workflow (archive/spam) — ours are view-only per Law 8.
