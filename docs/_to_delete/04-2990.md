# 2990 Backend (our own repo — the primary engine source)
2990 = AutoCount behavior + modern typography. It already IS the "combination" of the other references, built for our own operators. When in doubt, match 2990.
## Copy 100% (the Register ENGINE — mechanics, not business)
- Filter popup: header ▼ → Find… + Select all + Select invert + checkbox values; date presets; number min/max.
- Columns chooser with count (22/42) + Reset layout + per-user persistence.
- Column resize / reorder / hide. Frozen columns.
- Selection column + selection bar: "1 selected · Clear · Export (1)".
- Export Excel = current view; with selection = selected rows. (Real export verified: visible columns only, filtered rows only, plain sheet.)
- Density: row ~28px · body fs-12 · header fs-10 · sticky header · virtualized rows.
- Source: 2990s/apps/backend/src/components/DataGrid.tsx (1,551 lines). PORT THE MECHANICS into the Carres shared Register component — do not import 2990 business types/API/columns/actions.
## Don't copy ❌
- KPI cards (Total/Revenue/Outstanding/Paid) — Dashboard's job.
- New Sales Order form / Scan Order / SO Maintenance — 2990 creates orders in the backend; Carres creates them in Sales Portal. This wing does not exist for us.
- Breadcrumbs (our left nav already locates the user).
- Its business columns/status rules/types/hooks.
